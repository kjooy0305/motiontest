MC.addO({"dungeons:custom-elements":{"h":"<div class=\"tw\"><table><thead><tr><th><b>DISCLAIMER</b></th></tr></thead><tbody><tr><td><i>The following is a feature only available in Mythic Dungeons 1.3.0+!</i></td></tr></tbody></table></div>\n<p>Mythic Dungeons has an extensive API for creating your own functions, triggers, and conditions for use in dungeons. We call these &quot;Dungeon Elements&quot;. Many of them share code, and require a little work to setup in order to support the in-game menus needed to access them. Here, we will explore concepts that apply to all elements.</p>\n<p>Once you have read this page, continue to the element you want to build: <a href=\"#\" class=\"wl\" data-p=\"custom-functions\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Custom-Functions\">Custom Functions</a>, <a href=\"#\" class=\"wl\" data-p=\"custom-triggers\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Custom-Triggers\">Custom Triggers</a> or <a href=\"#\" class=\"wl\" data-p=\"custom-conditions\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Custom-Conditions\">Custom Conditions</a>.</p>\n<blockquote><p>_<b>Note:</b> dungeon elements are compiled against the full plugin jar, not the slim API artifact published to <code>mvn.lumine.io</code>. <code>MenuButton</code>, which every element has to return from <code>buildMenuButton()</code>, lives outside the published packages. See <a href=\"#\" class=\"wl\" data-p=\"introduction-to-api\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Introduction-to-API\">Introduction to API</a>._</p></blockquote>\n<h3>Important Annotations</h3>\n<h4>Declaring an element</h4>\n<p>Mark your class with the annotation matching its kind. This is what package scanning looks for.</p>\n<pre class=\"cb\" data-lang=\"java\"><code>@DeclaredFunction  public class FunctionMessage extends DungeonFunction { }\n@DeclaredTrigger   public class TriggerChat     extends DungeonTrigger  { }\n@DeclaredCondition public class ConditionChance extends TriggerCondition { }</code></pre>\n<p>All three are runtime-retained type annotations with no attributes. They do nothing on their own: they are only read when a package is scanned (see <span class=\"ilink\">Registering your elements</span> below). If you register a class one at a time instead, the annotation is optional.</p>\n<h4>Saved Fields</h4>\n<p>Mythic Dungeons will automatically save and load values stored on a Dungeon Element when players configure them in-game, however you must specify what you want to save and load! This is indicated with the <code>@SavedField</code> annotation.</p>\n<pre class=\"cb\" data-lang=\"java\"><code>@SavedField private String message; // Mythic Dungeons will save and load this value.\nprivate int delay; // Mythic Dungeons will NOT save and load this value.</code></pre>\n<p>Saved fields will also be displayed in the menus of meta elements, such as the multi-function. <a href=\"https://i.imgur.com/LpMGOEx.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></p>\n<p>The <b>Java field name</b> is the config key, both when saving and when loading. Renaming a saved field therefore orphans every value already written into existing dungeons.</p>\n<blockquote><p>_<code>@SavedField</code> accepts a <code>legacyNames</code> attribute (<code>@SavedField(legacyNames = {&quot;oldName&quot;})</code>). It is honoured by the plugin&#x27;s general-purpose serializer, but <b>not</b> by dungeon elements: element loading matches on the field name alone. To rename a saved field on an element, override <code>initLegacyFields(Map)</code> as described below._</p></blockquote>\n<h4>Hidden Fields</h4>\n<p>But what if you don&#x27;t want your saved field to appear in the menus of meta elements? This is achieved with the <code>@Hidden</code> annotation.</p>\n<pre class=\"cb\" data-lang=\"java\"><code>@SavedField private String message; // Mythic Dungeons will display this in the meta element menu.\n@Hidden @SavedField private int delay; // Mythic Dungeons will NOT display this in the meta element menu.</code></pre>\n<p><code>@Hidden</code> affects display only. The field is still saved and loaded exactly as before. It is honoured in the five editor windows that list an element&#x27;s parameters: the condition editor, the gate-trigger editor, the multi-function editor, the multi-function trigger editor, and the conditional-else editor. Anywhere else, it has no effect.</p>\n<h4>Relocatable</h4>\n<p><code>@Relocatable</code> exists in <code>api.annotations</code>, with a <code>precise()</code> attribute for double-precision rotation. <b>It is currently inert.</b> The two methods on <code>DungeonElement</code> that read it, for rotating and shifting a <code>Location</code> field, are commented out in the source. Do not rely on it to move your element&#x27;s locations. Procedural room rotation is handled elsewhere, and <code>DungeonFunction.offset(int x, int z)</code> shifts every <code>Location</code>-typed <code>@SavedField</code> on a function without consulting <code>@Relocatable</code> at all.</p>\n<h3>Required Constructors</h3>\n<p>Due to constraints with automatically saving and loading elements, <b>all dungeon elements must include two constructors.</b></p>\n<ul><li>A constructor with no parameters: <code>public FunctionMessage()</code></li><li>A constructor taking a  map as a parameter: <code>public FunctionMessage(Map&lt;String, Object&gt; config)</code></li></ul>\n<pre class=\"cb\" data-lang=\"java\"><code>    public FunctionMessage() {\n        super(&quot;Message&quot;); // Parameter for the element&#x27;s unique name.\n    }\n\n    public FunctionMessage(Map&lt;String, Object&gt; config) {\n        super(&quot;Message&quot;, config); // Parameter for the element&#x27;s unique name and the config map.\n    }</code></pre>\n<p>The first constructor is used to create a brand new instance of the element with default options. The second constructor is used to load an existing element and all of its saved fields, which are stored in the map.</p>\n<p>Within these constructors, you can configure certain details of your element, such as what category it belongs to, its colour when displayed, whether it requires a target, etc. What options are available for different kinds of elements will be explored in their tutorials.</p>\n<p>_You shouldn&#x27;t ever need to call these constructors yourself! They are only required for Mythic Dungeons&#x27; internal systems to be able to use your custom element!_</p>\n<p>Two hard requirements come from the way registration works, and both fail loudly rather than silently:</p>\n<ul><li><b>The no-argument constructor must be public.</b> Registration instantiates your class with <code>getDeclaredConstructor().newInstance()</code> purely to ask it for its menu button.</li><li><b><code>buildMenuButton()</code> must be declared on your own class.</b> It is looked up with <code>getDeclaredMethod</code>, which does not see inherited methods. Inheriting it from an intermediate base class of your own is not enough; override it on the concrete class.</li></ul>\n<p>If either is missing you get <code>ERROR :: The &lt;function|trigger|condition&gt; &lt;ClassName&gt; has a misconfigured menu button declaration!</code> in the console. If <code>buildMenuButton()</code> is declared but returns <code>null</code>, you get <code>... does not have a menu button!</code> instead and the element is dropped from the browser menu. (For conditions that second message says &quot;function selection menu&quot;; that wording is a copy-paste artefact in the source, it does mean the condition browser.)</p>\n<h3>Loading Hooks</h3>\n<p>Beyond the constructors, <code>DungeonElement</code> gives you three hooks that run during field loading. All three are called from <code>initFields()</code>, in this order, immediately after every <code>@SavedField</code> has been populated from the config map:</p>\n<div class=\"tw\"><table><thead><tr><th>Hook</th><th>Override it when</th></tr></thead><tbody><tr><td><code>protected void initLegacyFields()</code></td><td>You need to read raw keys out of the saved config map without the map being passed in.</td></tr><tr><td><code>protected void initLegacyFields(Map&lt;String, Object&gt; config)</code></td><td>You renamed a saved field and want old dungeons to keep working.</td></tr><tr><td><code>protected void initAdditionalFields()</code></td><td>You need to finish initialising objects that are stored on your element and have their own fields to set up.</td></tr></tbody></table></div>\n<p>The renaming case is the common one. If your field <code>message</code> was renamed to <code>msg</code>:</p>\n<pre class=\"cb\" data-lang=\"java\"><code>    @Override\n    public void initLegacyFields(Map&lt;String, Object&gt; config) {\n        if (config.containsKey(&quot;message&quot;)) this.msg = (String) config.get(&quot;message&quot;);\n    }</code></pre>\n<blockquote><p>_On functions and triggers, only the <code>Map</code> form is yours to override. <code>DungeonFunction</code> and <code>DungeonTrigger</code> both declare <code>initLegacyFields()</code> and <code>initAdditionalFields()</code> as <code>final</code> and use them internally, for the trigger/location fields and the retrigger/condition fields respectively. Conditions may override all three._</p></blockquote>\n<h3>Element Lifecycle</h3>\n<p>Elements are long-lived objects that get enabled and disabled with the instance they belong to. The two hooks you are meant to override are <code>onEnable()</code> and <code>onDisable()</code>, present on functions, triggers and conditions alike.</p>\n<div class=\"tw\"><table><thead><tr><th>Stage</th><th>What happens</th></tr></thead><tbody><tr><td>Construction</td><td>One of your two constructors runs.</td></tr><tr><td><code>init()</code></td><td>Saved fields are loaded and the hotbar menu is built. A function also initialises its trigger here, and a trigger its conditions. Runs once; a second call is ignored.</td></tr><tr><td><code>enable(...)</code></td><td>The element is bound to its instance and location, then your <code>onEnable()</code> runs. Functions and triggers are also registered as listeners on that instance at this point.</td></tr><tr><td>Execution</td><td>A function&#x27;s <code>runFunction(...)</code>, a trigger&#x27;s firing path, or a condition&#x27;s <code>check(...)</code>.</td></tr><tr><td><code>disable()</code></td><td>Listener registration is torn down (functions and triggers), the menu is unregistered, and your <code>onDisable()</code> runs.</td></tr></tbody></table></div>\n<p>Use <code>onEnable()</code> for anything that needs to start with the dungeon, such as a repeating task, and <code>onDisable()</code> to cancel exactly that. <b>Anything you start in <code>onEnable()</code> and do not cancel in <code>onDisable()</code> outlives the instance</b>, and because every element holds a reference to its instance, that keeps the dungeon world in memory. See <a href=\"#\" class=\"wl\" data-p=\"memory-leaks\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Memory-Leaks\">Avoiding Memory Leaks</a>.</p>\n<h3>Listening to Bukkit Events</h3>\n<p>Functions and triggers may declare <code>@EventHandler</code> methods directly on the element class:</p>\n<pre class=\"cb\" data-lang=\"java\"><code>    @EventHandler(priority = EventPriority.LOW)\n    public void onChat(AsyncPlayerChatEvent event) {\n        if (!instance.isPlayerInInstance(event.getPlayer())) return;\n        // ...\n    }</code></pre>\n<p>You do <b>not</b> register these yourself, and the class does not implement <code>Listener</code>. Mythic Dungeons scans the class at registration time and wires each handler through a shared dispatcher, which then invokes your method on every enabled copy of your element across every active instance. The declared <code>priority</code> is honoured.</p>\n<p>Because your method is called for every copy of the element in every running dungeon, the first thing it must do is decide whether this event is any of its business. Check the instance, as above.</p>\n<blockquote><p>_<b>Conditions do not get this.</b> <code>TriggerCondition</code> registration does no event wiring at all, so an <code>@EventHandler</code> on a condition is dead code. Conditions are evaluated on demand through <code>check(...)</code>._</p></blockquote>\n<h3>Registering your elements</h3>\n<p>Nothing is registered automatically. From your own plugin&#x27;s <code>onEnable()</code>, ask Mythic Dungeons to scan your package:</p>\n<pre class=\"cb\" data-lang=\"java\"><code>    MythicDungeons md = MythicDungeons.inst();\n\n    md.registerFunctions(&quot;com.example.myplugin.functions&quot;);\n    md.registerTriggers(&quot;com.example.myplugin.triggers&quot;);\n    md.registerConditions(&quot;com.example.myplugin.conditions&quot;);</code></pre>\n<p>Each call scans that package for classes annotated with the matching <code>@Declared...</code> annotation and registers every one it finds. You can also register a single class, in which case the annotation is not needed:</p>\n<pre class=\"cb\" data-lang=\"java\"><code>    md.registerFunction(FunctionMessage.class);\n    md.registerTrigger(TriggerChat.class);\n    md.registerCondition(ConditionChance.class);</code></pre>\n<p><code>ConfigurationSerialization.registerClass</code> is called for you by all three; do not call it yourself.</p>\n<p>Register on your plugin&#x27;s enable, and make sure Mythic Dungeons loads first by adding it to <code>depend</code> in your <code>plugin.yml</code>. Registering a trigger after Mythic Dungeons has already built its menus works: the trigger picker is updated in place. The function and condition browsers are built once at startup, so a function or condition registered late is usable but may not appear in its browser until the next restart.</p>\n<p><a href=\"#\" class=\"wl\" data-p=\"gui-menus\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/GUI-Menus\">**Next up:** GUI Menus --&gt;</a></p>"},"dungeons:events":{"h":"<p>Mythic Dungeons fires Bukkit-style events for every important dungeon and party state change. Listen to them the same way you&#x27;d listen to any vanilla event. Register a <code>Listener</code> and add <code>@EventHandler</code>-annotated methods. See <a href=\"#\" class=\"wl\" data-p=\"introduction-to-api\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Introduction-to-API\">Introduction to API</a> for the registration boilerplate.</p>\n<blockquote><p>_Package roots:_</p>\n<ul><li><code>net.playavalon.mythicdungeons.api.events.dungeon</code>: dungeon-lifecycle and trigger events</li><li><code>net.playavalon.mythicdungeons.api.events.dungeon.rooms</code>: procedural room events</li><li><code>net.playavalon.mythicdungeons.api.events.party</code>: party-system events</li></ul></blockquote>\n<p>Every event on this page is constructed and dispatched by the plugin. There is no dead event class in the API.</p>\n<hr>\n<h5>Inheritance overview</h5>\n<p>Most dungeon events extend a common abstract base, giving listeners these inherited accessors:</p>\n<div class=\"tw\"><table><thead><tr><th>Base class</th><th>Inherited getters</th></tr></thead><tbody><tr><td><code>DungeonEvent</code></td><td><code>getInstance()</code> -&gt; <code>AbstractInstance</code> (may be <code>null</code>), <code>getDungeon()</code> -&gt; <code>AbstractDungeon</code></td></tr><tr><td><code>MythicPartyEvent</code></td><td><code>getParty()</code> -&gt; <code>MythicParty</code></td></tr></tbody></table></div>\n<p>So every event documented below as <code>extends DungeonEvent</code> automatically exposes <code>getInstance()</code> and <code>getDungeon()</code>, and every <code>MythicPartyEvent</code> exposes <code>getParty()</code>.</p>\n<p><code>getInstance()</code> is <code>null</code> when the event was built from the dungeon-only constructor <code>DungeonEvent(AbstractDungeon)</code>, which <code>PlayerStartDungeonEvent</code> and <code>PlayerFinishDungeonEvent</code> both expose. The instance constructor also tolerates a <code>null</code> instance, in which case <code>getDungeon()</code> is <code>null</code> too. Null-check both before dereferencing.</p>\n<p>Both bases own the <code>HandlerList</code> for their whole family, and no subclass declares its own. Register your handler against the exact subclass you care about, or against <code>DungeonEvent</code> / <code>MythicPartyEvent</code> to receive every event in that family. A handler declared for one concrete subclass is never invoked for a sibling subclass, despite the shared list.</p>\n<p><code>MythicPartyEvent</code> has a second constructor, <code>MythicPartyEvent(MythicParty party, boolean isAsync)</code>, for subclasses that need to be dispatched asynchronously. <code>AsyncMythicPartyChatEvent</code> is the only event that uses it.</p>\n<p><code>HotbarSetEvent</code> is the one exception to all of the above: it extends <code>org.bukkit.event.Event</code> directly and owns its own <code>HandlerList</code>.</p>\n<blockquote><p>_Types used below:_ <code>MythicPlayer</code> is <code>net.playavalon.mythicdungeons.player.MythicPlayer</code>, <code>MythicParty</code> is <code>net.playavalon.mythicdungeons.player.party.partysystem.MythicParty</code>, <code>IDungeonParty</code> is <code>net.playavalon.mythicdungeons.api.party.IDungeonParty</code>, <code>Hotbar</code> is <code>net.playavalon.mythicdungeons.player.Hotbar</code>, <code>DungeonDifficulty</code> is <code>net.playavalon.mythicdungeons.api.parents.DungeonDifficulty</code>, <code>AbstractDungeon</code> is <code>net.playavalon.mythicdungeons.api.parents.dungeons.AbstractDungeon</code>, <code>AbstractInstance</code> and <code>InstancePlayable</code> are in <code>net.playavalon.mythicdungeons.api.parents.instances</code>, <code>DungeonTrigger</code> is <code>net.playavalon.mythicdungeons.api.parents.elements.DungeonTrigger</code>, and <code>InstanceRoom</code> / <code>ConnectorDoor</code> / <code>DoorAction</code> / <code>RoomEmptyCause</code> live in <code>net.playavalon.mythicdungeons.api.generation.rooms</code>.</p></blockquote>\n<hr>\n<h5>All events at a glance</h5>\n<div class=\"tw\"><table><thead><tr><th>Event</th><th>Package</th><th>Extends</th><th>Cancellable</th><th>Async</th></tr></thead><tbody><tr><td><code>DungeonStartEvent</code></td><td><code>...events.dungeon</code></td><td><code>DungeonEvent</code></td><td>No</td><td>No</td></tr><tr><td><code>DungeonEndEvent</code></td><td><code>...events.dungeon</code></td><td><code>DungeonEvent</code></td><td>No</td><td>No</td></tr><tr><td><code>PlayerStartDungeonEvent</code></td><td><code>...events.dungeon</code></td><td><code>DungeonEvent</code></td><td>No</td><td>No</td></tr><tr><td><code>PlayerFinishDungeonEvent</code></td><td><code>...events.dungeon</code></td><td><code>DungeonEvent</code></td><td>No</td><td>No</td></tr><tr><td><code>PlayerLeaveDungeonEvent</code></td><td><code>...events.dungeon</code></td><td><code>DungeonEvent</code></td><td>Yes</td><td>No</td></tr><tr><td><code>DungeonDisposeEvent</code></td><td><code>...events.dungeon</code></td><td><code>DungeonEvent</code></td><td>Yes</td><td>No</td></tr><tr><td><code>TriggerFireEvent</code></td><td><code>...events.dungeon</code></td><td><code>DungeonEvent</code></td><td>Yes</td><td>No</td></tr><tr><td><code>RemoteTriggerEvent</code></td><td><code>...events.dungeon</code></td><td><code>DungeonEvent</code></td><td>Yes</td><td>No</td></tr><tr><td><code>DungeonStatusChangeEvent</code></td><td><code>...events.dungeon</code></td><td><code>DungeonEvent</code></td><td>No</td><td>No</td></tr><tr><td><code>DungeonDifficultyChangeEvent</code></td><td><code>...events.dungeon</code></td><td><code>DungeonEvent</code></td><td>No</td><td>No</td></tr><tr><td><code>DungeonGenerateLootEvent</code></td><td><code>...events.dungeon</code></td><td><code>DungeonEvent</code></td><td>Yes</td><td>No</td></tr><tr><td><code>HotbarSetEvent</code></td><td><code>...events.dungeon</code></td><td><code>Event</code></td><td>Yes</td><td>No</td></tr><tr><td><code>RoomDoorChangeEvent</code></td><td><code>...events.dungeon.rooms</code></td><td><code>DungeonEvent</code></td><td>No</td><td>No</td></tr><tr><td><code>RoomTransitionEvent</code></td><td><code>...events.dungeon.rooms</code></td><td><code>DungeonEvent</code></td><td>No</td><td>No</td></tr><tr><td><code>MythicPartyCreateEvent</code></td><td><code>...events.party</code></td><td><code>MythicPartyEvent</code></td><td>No</td><td>No</td></tr><tr><td><code>MythicPartyJoinEvent</code></td><td><code>...events.party</code></td><td><code>MythicPartyEvent</code></td><td>Yes</td><td>No</td></tr><tr><td><code>MythicPartyLeaveEvent</code></td><td><code>...events.party</code></td><td><code>MythicPartyEvent</code></td><td>No</td><td>No</td></tr><tr><td><code>MythicPartyKickEvent</code></td><td><code>...events.party</code></td><td><code>MythicPartyEvent</code></td><td>Yes</td><td>No</td></tr><tr><td><code>AsyncMythicPartyChatEvent</code></td><td><code>...events.party</code></td><td><code>MythicPartyEvent</code></td><td>Yes</td><td>Yes</td></tr></tbody></table></div>\n<p><code>AsyncMythicPartyChatEvent</code> is the only event flagged async. The one caveat is <code>DungeonDisposeEvent</code>: it is a synchronous event, but while the plugin is shutting down an off-thread <code>dispose()</code> no longer hops to the main thread, so it can be dispatched on the calling thread.</p>\n<hr>\n<h3>Dungeon Events</h3>\n<h5>DungeonStartEvent</h5>\n<p>Fired once per instance, at the end of the instance&#x27;s <code>startGame()</code>. That happens when the <b>Start Dungeon</b> function&#x27;s ready-check completes (lobby dungeons), one tick after the world loads (classic dungeons without a lobby, and all procedural dungeons), or when a player re-enters onto a save point. Never fired for edit instances.</p>\n<p><b>Extends:</b> <code>DungeonEvent</code> | <b>Cancellable:</b> No</p>\n<pre class=\"cb\" data-lang=\"java\"><code>new DungeonStartEvent(AbstractInstance instance, List&lt;MythicPlayer&gt; mythicPlayers)</code></pre>\n<p><b>Getters:</b></p>\n<ul><li><code>List&lt;MythicPlayer&gt; getMythicPlayers()</code>: the instance&#x27;s live player list, not a snapshot. Copy it if you need it later, or it will shrink under you as players leave.</li><li><code>Collection&lt;Player&gt; getPlayers()</code>: a fresh <code>ArrayList</code> of the same players as Bukkit <code>Player</code>s, rebuilt on every call.</li></ul>\n<p>At this point functions are initialised, every player has been teleported to the start point with a dungeon respawn set, the participant list has been recorded, the time limit is computed, and the instance ticker is already scheduled.</p>\n<hr>\n<h5>DungeonEndEvent</h5>\n<p>Fired as the <b>last</b> player is being removed from the instance, whatever caused the removal (timeout, leave, kick, finish). It fires <i>before</i> that player is removed and <i>before</i> <code>PlayerLeaveDungeonEvent</code>, so <code>getGamePlayers()</code> still contains them. An instance nobody ever entered never fires it.</p>\n<p><b>Extends:</b> <code>DungeonEvent</code> | <b>Cancellable:</b> No</p>\n<pre class=\"cb\" data-lang=\"java\"><code>new DungeonEndEvent(AbstractInstance instance)</code></pre>\n<p><b>Getters:</b></p>\n<ul><li><code>List&lt;MythicPlayer&gt; getGamePlayers()</code>: the instance&#x27;s live player list, including the player currently leaving. Copy it if you need it after the event; read a tick later it will be empty.</li><li><code>List&lt;Player&gt; getPlayers()</code>: a snapshot of the same players as Bukkit <code>Player</code>s, built once when the event is constructed.</li><li><code>IDungeonParty getParty()</code>: the dungeon party of the first player in that list, if they have one.</li><li><code>boolean isEditInstance()</code>: <code>true</code> when the instance was in edit mode.</li><li><code>boolean isPlayInstance()</code>: <code>true</code> when the instance was a play instance.</li></ul>\n<blockquote><p><b>Note:</b> If a listener cancels the <code>PlayerLeaveDungeonEvent</code> that follows, this event has already been delivered even though the dungeon keeps running.</p></blockquote>\n<hr>\n<h5>PlayerStartDungeonEvent</h5>\n<p>Fired as the last step of adding a player to a play instance, once they are fully inside. Edit instances use a different code path, so this event <b>never</b> fires for them.</p>\n<p><b>Extends:</b> <code>DungeonEvent</code> | <b>Cancellable:</b> No</p>\n<pre class=\"cb\" data-lang=\"java\"><code>new PlayerStartDungeonEvent(InstancePlayable instance, MythicPlayer mPlayer)\nnew PlayerStartDungeonEvent(AbstractDungeon dungeon, MythicPlayer mPlayer)</code></pre>\n<p><b>Getters:</b></p>\n<ul><li><code>MythicPlayer getMPlayer()</code>: the entering player as a Mythic Dungeons player.</li><li><code>Player getPlayer()</code>: the same player as a Bukkit <code>Player</code>.</li></ul>\n<p>By the time listeners run, the player is in the instance roster, their instance reference is set, their inventory has been saved (or protected from a Multiverse swap), they have been teleported to their save point, their gamemode has been applied and their lives have been seeded.</p>\n<p>The <code>AbstractDungeon</code> constructor is part of the public API and is not used inside the plugin. An event built that way has a <code>null</code> <code>getInstance()</code>.</p>\n<hr>\n<h5>PlayerFinishDungeonEvent</h5>\n<p>Fired when a player formally completes the dungeon. Two producers: the <b>Finish Dungeon</b> function (only for targets still in the instance) and the <a href=\"#\" class=\"wl\" data-p=\"mythic-mobs-integration/mechanics/dungeoncomplete\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Mythic-Mobs-Integration/Mechanics/DungeonComplete\"><code>dungeoncomplete</code></a> mechanic (aliases <code>dcomplete</code>, <code>dwin</code>).</p>\n<p><b>Extends:</b> <code>DungeonEvent</code> | <b>Cancellable:</b> No</p>\n<pre class=\"cb\" data-lang=\"java\"><code>new PlayerFinishDungeonEvent(InstancePlayable instance, MythicPlayer mPlayer)\nnew PlayerFinishDungeonEvent(AbstractDungeon dungeon, MythicPlayer mPlayer)</code></pre>\n<p><b>Getters:</b></p>\n<ul><li><code>MythicPlayer getMPlayer()</code></li><li><code>Player getPlayer()</code></li></ul>\n<p>Both producers fire it <i>before</i> the player is marked as having finished, before loot cooldowns are applied, before rewards are pushed and before the optional removal from the instance. As with the start event, the <code>AbstractDungeon</code> constructor leaves <code>getInstance()</code> <code>null</code>.</p>\n<hr>\n<h5>PlayerLeaveDungeonEvent</h5>\n<p>Fired when a player is removed from a dungeon instance by any means (finish, manual leave, kick, disconnect, time limit, reload).</p>\n<p><b>Extends:</b> <code>DungeonEvent</code> | <b>Cancellable:</b> Yes</p>\n<pre class=\"cb\" data-lang=\"java\"><code>new PlayerLeaveDungeonEvent(AbstractInstance instance, MythicPlayer mPlayer)</code></pre>\n<p><b>Getters:</b></p>\n<ul><li><code>MythicPlayer getMPlayer()</code></li><li><code>Player getPlayer()</code></li><li><code>boolean isEditMode()</code>: <code>true</code> if the player was leaving an edit-mode instance.</li></ul>\n<p>Cancelling this event only blocks the leave on the three paths that ask for it: the <b>Leave Dungeon</b> function, the <code>/leave</code> command, and the <a href=\"#\" class=\"wl\" data-p=\"mythic-mobs-integration/mechanics/dungeonkick\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Mythic-Mobs-Integration/Mechanics/DungeonKick\"><code>dungeonkick</code></a> mechanic run with <code>silent=true</code>. Every other exit path (disconnect, party removal, time limit, <b>Finish Dungeon</b>, <code>dungeonkick</code> without <code>silent</code>, <code>/md</code> admin commands, reload and shutdown) removes the player regardless of the cancel flag.</p>\n<hr>\n<h5>DungeonDisposeEvent</h5>\n<p>Fired right before an instance is disposed (world unloaded, files cleaned up). Cancel to keep the instance alive, for example, if you&#x27;re still serializing state.</p>\n<p><b>Extends:</b> <code>DungeonEvent</code> | <b>Cancellable:</b> Yes</p>\n<pre class=\"cb\" data-lang=\"java\"><code>new DungeonDisposeEvent(AbstractInstance instance)</code></pre>\n<p>No additional getters beyond the inherited <code>getInstance()</code> / <code>getDungeon()</code>.</p>\n<p>It only fires when the instance has no players left and is not already disposing, and only <i>after</i> the instance has been detached from its dungeon. Cancelling re-attaches it and aborts the disposal; a later <code>dispose()</code> call fires the event again. Mythic Dungeons itself listens to this event to admit the next party from the queue.</p>\n<blockquote><p><b>Note:</b> Disposal normally hops to the main thread first, but that hop is skipped while the plugin is disabling, so during a shutdown this event can arrive on whatever thread called <code>dispose()</code>.</p></blockquote>\n<hr>\n<h5>TriggerFireEvent</h5>\n<p>Fired every time a dungeon trigger is about to run its function chain. Cancel to stop the function from running and to suppress the trigger&#x27;s own <code>onTrigger</code> handling.</p>\n<p><b>Extends:</b> <code>DungeonEvent</code> | <b>Cancellable:</b> Yes</p>\n<pre class=\"cb\" data-lang=\"java\"><code>new TriggerFireEvent(InstancePlayable instance, DungeonTrigger trigger)\nnew TriggerFireEvent(MythicPlayer dPlayer, DungeonTrigger trigger)</code></pre>\n<p><b>Getters:</b></p>\n<ul><li><code>DungeonTrigger getTrigger()</code>: the trigger that fired.</li><li><code>MythicPlayer getDPlayer()</code>: the player who caused the trigger, <code>null</code> for instance-scope firings.</li><li><code>@Nullable Player getPlayer()</code>: same as above as a Bukkit <code>Player</code>, or <code>null</code>.</li></ul>\n<p>The event object is created <i>before</i> the trigger&#x27;s conditions are evaluated and is handed to those conditions first. It is only dispatched once the whole condition set passes and the trigger&#x27;s configured delay has elapsed. With &quot;Wait For Conditions&quot; on and &quot;Allow Retrigger&quot; off, the trigger keeps re-checking once per second until the conditions pass, and dispatch happens on the pass, not on the original stimulus.</p>\n<hr>\n<h5>RemoteTriggerEvent</h5>\n<p>Fired by the <b>Signal Sender</b> function and by the <a href=\"#\" class=\"wl\" data-p=\"mythic-mobs-integration/mechanics/dungeonsignal\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Mythic-Mobs-Integration/Mechanics/DungeonSignal\"><code>dungeonsignal</code></a> mechanic (alias <code>dsignal</code>). <b>Signal Receiver</b> triggers listen to it and fire when the signal name, the range and the room all match.</p>\n<p><b>Extends:</b> <code>DungeonEvent</code> | <b>Cancellable:</b> Yes</p>\n<pre class=\"cb\" data-lang=\"java\"><code>new RemoteTriggerEvent(@NotNull String triggerName, DungeonTrigger trigger, @NotNull InstancePlayable instance)\nnew RemoteTriggerEvent(@NotNull String triggerName, DungeonTrigger trigger, @NotNull InstancePlayable instance, double range, @Nullable Location origin)\nnew RemoteTriggerEvent(@NotNull String triggerName, DungeonTrigger trigger, @NotNull InstancePlayable instance, double range, @Nullable Location origin, @Nullable MythicPlayer triggerPlayer)</code></pre>\n<p><b>Getters:</b></p>\n<ul><li><code>String getTriggerName()</code>: the signal name being dispatched.</li><li><code>DungeonTrigger getTrigger()</code>: the source trigger object. The <code>dungeonsignal</code> mechanic passes <code>null</code> here.</li><li><code>double getRange()</code> / <code>setRange(double)</code>: radius for receivers; <code>0</code> means dungeon-wide.</li><li><code>@Nullable Location getOrigin()</code>: origin location for radius checks.</li><li><code>@Nullable MythicPlayer getMythicPlayer()</code>: the player who caused the signal, if there was one.</li><li><code>@Nullable Player getPlayer()</code>: same as a Bukkit <code>Player</code>, or <code>null</code>.</li></ul>\n<blockquote><p><b>Note:</b> This event implements <code>Cancellable</code>, but <b>Signal Receiver</b> triggers never read the cancel flag, and neither does the shared dispatcher that invokes them. Cancelling does not suppress a receiver at any event priority. The flag is only useful to other plugins&#x27; listeners.</p></blockquote>\n<p>The <b>Mythic Signal</b> function does not fire this event. It signals nearby Mythic Mobs directly.</p>\n<hr>\n<h5>DungeonStatusChangeEvent</h5>\n<p>Fired when the instance&#x27;s status string changes, via the <b>Dungeon Status</b> function or <code>InstancePlayable#setStatus(String)</code>. Not fired when the new value equals the old one. Always delivered on the main thread, one tick later if the change came from an async thread.</p>\n<p><b>Extends:</b> <code>DungeonEvent</code> | <b>Cancellable:</b> No</p>\n<pre class=\"cb\" data-lang=\"java\"><code>new DungeonStatusChangeEvent(InstancePlayable instance, @Nullable String previousStatus, @Nullable String newStatus)</code></pre>\n<p><b>Getters:</b></p>\n<ul><li><code>@Nullable String getPreviousStatus()</code></li><li><code>@Nullable String getNewStatus()</code></li></ul>\n<p>The instance already carries the new status when listeners run.</p>\n<hr>\n<h5>DungeonDifficultyChangeEvent</h5>\n<p>Fired when the instance&#x27;s difficulty changes: via the <b>Dungeon Difficulty</b> function, via <code>InstancePlayable#setDifficulty(DungeonDifficulty)</code>, and once per instance when a newly created instance is stamped with the difficulty the players picked. Not fired when the new value equals the old one. Same main-thread dispatch as <code>DungeonStatusChangeEvent</code>.</p>\n<p><b>Extends:</b> <code>DungeonEvent</code> | <b>Cancellable:</b> No</p>\n<pre class=\"cb\" data-lang=\"java\"><code>new DungeonDifficultyChangeEvent(InstancePlayable instance, @Nullable DungeonDifficulty previousDifficulty, @Nullable DungeonDifficulty newDifficulty)</code></pre>\n<p><b>Getters:</b></p>\n<ul><li><code>@Nullable DungeonDifficulty getPreviousDifficulty()</code>: <code>null</code> on the initial stamp.</li><li><code>@Nullable DungeonDifficulty getNewDifficulty()</code></li></ul>\n<hr>\n<h5>DungeonGenerateLootEvent</h5>\n<p>Fired once per item rolled by a <b>Loot Table Rewards</b> function, after Mythic Mobs item resolution and before the item is placed in the reward chest GUI. Not fired by the <b>Rewards</b> or <b>Random Rewards</b> functions, and not fired at all in an edit instance or when the player has already claimed that chest.</p>\n<p><b>Extends:</b> <code>DungeonEvent</code> | <b>Cancellable:</b> Yes</p>\n<pre class=\"cb\" data-lang=\"java\"><code>new DungeonGenerateLootEvent(AbstractInstance instance, Player player, ItemStack item, String lootTableNamespace)</code></pre>\n<p><b>Getters:</b></p>\n<ul><li><code>Player getPlayer()</code>: the player the loot is being rolled for.</li><li><code>ItemStack getGeneratedItem()</code> / <code>setGeneratedItem(ItemStack)</code>: the rolled item. Replace it to change the drop; setting it to <code>null</code> skips it.</li><li><code>String getLootTableNamespace()</code>: the name of the loot table the item came from.</li></ul>\n<p>Cancelling skips this item only; the rest of the roll continues.</p>\n<hr>\n<h5>HotbarSetEvent</h5>\n<p>Fired when a player&#x27;s edit-mode hotbar is about to be replaced, for example, when switching between trigger-edit and function-edit hotbars. Cancel to keep the current hotbar.</p>\n<p><b>Extends:</b> <code>Event</code> (NOT <code>DungeonEvent</code>) | <b>Cancellable:</b> Yes</p>\n<pre class=\"cb\" data-lang=\"java\"><code>new HotbarSetEvent(Hotbar hotbar, MythicPlayer aPlayer)</code></pre>\n<p><b>Getters:</b></p>\n<ul><li><code>@Nullable Hotbar getOldHotbar()</code>: the top of the player&#x27;s previous-hotbar stack, if any. This is not necessarily the hotbar they are wearing right now.</li><li><code>Hotbar getNewHotbar()</code>: the hotbar being applied. It is <code>null</code> on the restore path, before the player has ever had a hotbar saved and again after a restore has consumed the saved one.</li><li><code>void setNewHotbar(Hotbar)</code>: other listeners see the replacement, but the hotbar actually given to the player is unaffected. Use cancellation rather than substitution.</li><li><code>MythicPlayer getMythicPlayer()</code></li><li><code>Player getPlayer()</code></li></ul>\n<p>Cancelling stops the swap on all three of the plugin&#x27;s call sites. Two of those three fire one tick after the hotbar change was requested.</p>\n<hr>\n<h3>Room Events</h3>\n<p>_Rooms only exist in <b>procedural</b> dungeons._</p>\n<h5>RoomDoorChangeEvent</h5>\n<p>Fired when a connector door in a procedural room is opened or closed.</p>\n<p><b>Extends:</b> <code>DungeonEvent</code> | <b>Cancellable:</b> No</p>\n<pre class=\"cb\" data-lang=\"java\"><code>new RoomDoorChangeEvent(AbstractInstance instance, InstanceRoom room, ConnectorDoor door, DoorAction action)</code></pre>\n<p><b>Getters:</b></p>\n<ul><li><code>InstanceRoom getRoom()</code>: the room containing the door.</li><li><code>ConnectorDoor getDoor()</code>: the door itself.</li><li><code>DoorAction getAction()</code>: <code>OPEN</code> or <code>CLOSE</code>.</li></ul>\n<p>This event is notification-only: the blocks have already been changed and the sound has already played when it fires. It is also skipped entirely when the door cannot resolve its instance, so do not treat it as a guaranteed record of every door movement.</p>\n<hr>\n<h5>RoomTransitionEvent</h5>\n<p>Fired when a player enters or leaves a procedural room. <code>FIRST_ENTER</code> fires just before the matching <code>ENTER</code>, and <code>EMPTIED</code> just after the matching <code>LEAVE</code>, so a listener can pick whichever edge it needs. Driven by move, teleport, world change, quit and death, all observed at <code>MONITOR</code>.</p>\n<p><b>Extends:</b> <code>DungeonEvent</code> | <b>Cancellable:</b> No</p>\n<pre class=\"cb\" data-lang=\"java\"><code>new RoomTransitionEvent(AbstractInstance instance, InstanceRoom room, Player player, RoomTransitionEvent.Type type,\n                        RoomEmptyCause cause, int playerCount, int previousPlayerCount)</code></pre>\n<p><b>Getters:</b></p>\n<ul><li><code>InstanceRoom getRoom()</code>: the room being entered or left.</li><li><code>Player getPlayer()</code>: the player who moved.</li><li><code>RoomTransitionEvent.Type getType()</code>: <code>ENTER</code>, <code>LEAVE</code>, <code>FIRST_ENTER</code> or <code>EMPTIED</code>.</li><li><code>RoomEmptyCause getCause()</code>: <code>MOVE</code>, <code>TELEPORT</code>, <code>WORLD_CHANGE</code>, <code>QUIT</code> or <code>DEATH</code>.</li><li><code>int getPlayerCount()</code> / <code>int getPreviousPlayerCount()</code>: room occupancy after and before the transition.</li></ul>\n<p>The room&#x27;s occupancy counters are already updated when the event fires. Enter events require the player to be in a procedural instance, but the leave path is looser: a player who has a cached room and is no longer in a procedural instance (or in no instance at all) still gets a final <code>LEAVE</code>, carrying the room&#x27;s own instance.</p>\n<hr>\n<h3>Party Events</h3>\n<p>_These events belong to the built-in party system. They only fire when <code>General.PartyPlugin</code> in <code>config.yml</code> is <code>Default</code> or <code>DungeonParties</code>. With Parties, PartyAndFriends or Heroes selected, Mythic Dungeons wraps that plugin&#x27;s parties instead and none of these are fired._</p>\n<h5>MythicPartyCreateEvent</h5>\n<p>Fired after a new dungeon party is created. Dispatch is deferred by one tick, so the party already exists and has its leader when listeners run.</p>\n<p><b>Extends:</b> <code>MythicPartyEvent</code> | <b>Cancellable:</b> No</p>\n<pre class=\"cb\" data-lang=\"java\"><code>new MythicPartyCreateEvent(MythicParty party, MythicPlayer host)</code></pre>\n<p><b>Getters:</b></p>\n<ul><li><code>MythicPlayer getHostPlayer()</code>: the player who created the party (also the initial leader).</li></ul>\n<hr>\n<h5>MythicPartyJoinEvent</h5>\n<p>Fired when a player joins an existing party, synchronously, before the player is added. Cancel to block the join.</p>\n<p><b>Extends:</b> <code>MythicPartyEvent</code> | <b>Cancellable:</b> Yes</p>\n<pre class=\"cb\" data-lang=\"java\"><code>new MythicPartyJoinEvent(MythicParty party, MythicPlayer player)</code></pre>\n<p><b>Getters:</b></p>\n<ul><li><code>MythicPlayer getJoiningPlayer()</code>: the player joining the party.</li></ul>\n<blockquote><p><b>Note:</b> Removal from the player&#x27;s previous party happens <i>before</i> this event on every join path, and that removal also ejects them from any dungeon they were running. Cancelling therefore leaves the player in no party at all, and out of their old dungeon, rather than restoring the previous state.</p></blockquote>\n<hr>\n<h5>MythicPartyLeaveEvent</h5>\n<p>Fired whenever a player is removed from a party: <code>/party leave</code>, a kick, a disband, joining another party, the offline timeout, or the API <code>removeFromParty</code>. Dispatch is deferred by one tick, so the player is already out of the party when listeners run.</p>\n<p><b>Extends:</b> <code>MythicPartyEvent</code> | <b>Cancellable:</b> No</p>\n<pre class=\"cb\" data-lang=\"java\"><code>new MythicPartyLeaveEvent(MythicParty party, MythicPlayer leavingPlayer)</code></pre>\n<p><b>Getters:</b></p>\n<ul><li><code>MythicPlayer getLeavingPlayer()</code>: the player leaving the party.</li></ul>\n<hr>\n<h5>MythicPartyKickEvent</h5>\n<p>Fired when a player is kicked from a party, synchronously, before the removal. In practice this means <code>/party kick &lt;name&gt;</code>. Cancel to block the kick.</p>\n<p><b>Extends:</b> <code>MythicPartyEvent</code> | <b>Cancellable:</b> Yes</p>\n<pre class=\"cb\" data-lang=\"java\"><code>new MythicPartyKickEvent(MythicParty party, MythicPlayer kickedPlayer, MythicPlayer whoKicked)</code></pre>\n<p><b>Getters:</b></p>\n<ul><li><code>MythicPlayer getKickedPlayer()</code>: the player being kicked.</li><li><code>MythicPlayer getWhoKicked()</code>: the party&#x27;s current leader (kicks are always issued as the leader).</li></ul>\n<blockquote><p><b>Note:</b> Mythic Dungeons&#x27; own listener runs at <code>NORMAL</code> priority and never checks the cancel flag, so it pulls the kicked player out of their running dungeon whatever you do. Cancelling keeps them in the party but does not put them back in the dungeon, at any priority.</p></blockquote>\n<hr>\n<h5>AsyncMythicPartyChatEvent</h5>\n<p>Fired when a player sends a message through party chat. Cancel to suppress the message, or mutate it via <code>setMessage(...)</code>.</p>\n<p><b>Extends:</b> <code>MythicPartyEvent</code> | <b>Cancellable:</b> Yes | <b>Async:</b> Yes</p>\n<pre class=\"cb\" data-lang=\"java\"><code>new AsyncMythicPartyChatEvent(MythicParty party, String message)</code></pre>\n<p><b>Getters:</b></p>\n<ul><li><code>String getMessage()</code> / <code>setMessage(String)</code>: the chat content. Colour codes have already been translated before the event is constructed, so this is the formatted text, not the player&#x27;s raw input.</li></ul>\n<p>Cancelling suppresses delivery to party members, the console log line and any party-spy copies. <code>setMessage(...)</code> is honoured by all three.</p>\n<blockquote><p><b>Important:</b> Because this is an async event, do not call Bukkit-thread-only APIs from your handler unless you bounce the work back to the main thread.</p></blockquote>\n<hr>\n<h3>Deprecated helpers</h3>\n<p><code>DungeonTrigger#triggerPlayer(MythicPlayer)</code> and <code>DungeonTrigger#triggerParty(InstanceClassic)</code> are marked <code>@Deprecated</code> and are not called anywhere in the plugin. Each builds a <code>TriggerFireEvent</code> and then discards it without dispatching, before delegating to <code>trigger()</code>, which fires the trigger with no player context. No listener sees a <code>TriggerFireEvent</code> from either of them. Do not use them to fire a trigger from your own plugin.</p>"},"dungeons:functions":{"h":"<p>Functions perform various actions and events in the dungeon. To those familiar with Mythic Mobs, these are similar to mechanics. This includes spawning mobs, sending messages, playing sounds, running commands, and even performing Mythic skills!</p>\n<p>Common Function Options</p>\n<hr>\n<p>All functions have a set of configurable options when you create or edit them. Common options that are seen across many different functions are listed here. Their default values are often different depending on the function in question.</p>\n<ul><li><b>BACK</b>   <i>Applies to all functions.</i> Returns to the previous hotbar menu.</li><li><b>Trigger: [Trigger Name]</b>   <i>Applies to all functions.</i> Opens the trigger&#x27;s hotbar menu. Reads <code>NONE</code> if the function has no trigger, and clicking it in that state only prints a warning. Most functions are given the <code>Dungeon Start</code> trigger automatically if you leave the trigger picker without choosing one. <b>Allow Block Place/Break</b> and <b>Door Controller</b> are the exceptions and are allowed to run without a trigger.</li><li><b>Target Type</b>   <i>Only available on select functions.</i> Cycles through target options. The full list, in cycle order, is: <code>NONE</code>, <code>PLAYER</code>, <code>PARTY</code>, <code>ROOM</code>. <code>NONE</code> - The function targets nobody. <i>(Skipped if the function requires a target.)</i> <code>PLAYER</code> - If the trigger was activated by a specific player, the function targets that player. <i>(Skipped if the selected trigger doesn&#x27;t have a player associated with it.)</i> <code>PARTY</code> - Targets every party member who is still inside the instance. If the triggering player isn&#x27;t in a party, only that player is targeted. If the trigger has no player at all, the function targets <b>everyone in the instance</b> and the button relabels itself <code>ALL PLAYERS</code>. <code>ROOM</code> - Targets every player standing inside the room this function was placed in. <i>(Procedural dungeons only; skipped everywhere else.)</i></li></ul>\n<p>Functions that hide this button always use their built-in target type: Signal Sender, Player Reviver, Dungeon Status, Dungeon Difficulty, Mob Spawner, NPC Spawner, Place Redstone Block, Door Controller, Allow Block Place/Break, Hologram, Moving Block, Moving Block Cluster, Mythic Signal, Function Repeater, Dungeon Variable, and Room Door Controller. Note that Hologram only hides it while it is freshly placed; after the dungeon is saved and reloaded the button reappears, but the value it sets is ignored.</p>\n<p>Functions List</p>\n<hr>\n<p>Every function&#x27;s own options are documented below the table, under <a href=\"#\" class=\"wl\" data-p=\"functions\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Functions\">Function Options</a>.</p>\n<div class=\"tw\"><table><thead><tr><th>Icon</th><th>Name</th><th>Category</th><th>Description</th></tr></thead><tbody><tr><td><a href=\"https://i.imgur.com/SQbt5pf.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Start Dungeon</b></td><td><i>Dungeon</i></td><td>Starts the dungeon. <i>Required if your dungeon has a lobby!</i></td></tr><tr><td><a href=\"https://i.imgur.com/JHmLnxm.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Finish Dungeon</b></td><td><i>Dungeon</i></td><td><b>RECOMMENDED</b> Formally finishes the dungeon for the player or players.</td></tr><tr><td><a href=\"https://i.imgur.com/snDmE1k.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Leave Dungeon</b></td><td><i>Dungeon</i></td><td>Sends the player or players out of the dungeon without completing it.</td></tr><tr><td><a href=\"https://i.imgur.com/14ymqFg.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Respawn Checkpoint</b></td><td><i>Dungeon</i></td><td>Sets a respawn checkpoint for the player or players.</td></tr><tr><td><a href=\"https://i.imgur.com/feyEUwC.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Signal Sender</b></td><td><i>Dungeon</i></td><td>Broadcasts a silent, configured signal to all &quot;Signal Receiver&quot; triggers.</td></tr><tr><td><a href=\"https://i.imgur.com/QqZIQ15.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Lives Editor</b></td><td><i>Dungeon</i></td><td>Modifies the player or players lives.</td></tr><tr><td><a href=\"https://i.imgur.com/xyGGseB.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Player Reviver</b></td><td><i>Dungeon</i></td><td>Allows bringing another player back from death.</td></tr><tr><td><a href=\"https://i.imgur.com/1BxYZ26.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Chunk Loader</b></td><td><i>Dungeon</i></td><td>When triggered, force-loads the chunk this function is in, plus an optional radius of chunks around it. Trigger again to release them.</td></tr><tr><td><a href=\"https://i.imgur.com/MKlkBRh.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Dungeon Status</b></td><td><i>Dungeon</i></td><td>Sets the dungeons &quot;status&quot; to a configurable text. Similar to Mythic Mobs stances.</td></tr><tr><td><a href=\"https://i.imgur.com/3eKOgeL.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Dungeon Difficulty</b></td><td><i>Dungeon</i></td><td>Allows changing the difficulty of the dungeon on the fly.</td></tr><tr><td><a href=\"https://i.imgur.com/ICSGaqm.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Message Sender</b></td><td><i>Player</i></td><td>Sends a configured message to the player or players.</td></tr><tr><td><a href=\"https://i.imgur.com/cS4n7C7.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Teleporter</b></td><td><i>Player</i></td><td>Teleports the player or players to a configured location.</td></tr><tr><td><a href=\"https://i.imgur.com/hHbHJ0K.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Title Sender</b></td><td><i>Player</i></td><td>Sends a configured title message to the player or players.</td></tr><tr><td></td><td><b>Boss Bar</b></td><td><i>Player</i></td><td>Shows a boss bar to the target player(s) with a configurable title, colour, style, and progress. Progress can follow a dungeon variable or a placeholder. Triggering it toggles the bar on or off.</td></tr><tr><td><a href=\"https://i.imgur.com/9JyLFHO.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Item Dispenser</b></td><td><i>Player</i></td><td>Gives or drops a configured item at the function&#x27;s location.</td></tr><tr><td><a href=\"https://i.imgur.com/vQcqJ9i.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Key Dispenser</b></td><td><i>Player</i></td><td>Gives one player a configured key item. Defaults to a dungeon key item. <i>Note: Similar to Item Dispenser, however it reports a key being found to the players and gives the key to the party leader if the function is set to target the party.</i></td></tr><tr><td><a href=\"https://i.imgur.com/1idXCEF.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Rewards</b></td><td><i>Player</i></td><td>Opens a rewards inventory the player can take items from.</td></tr><tr><td><a href=\"https://i.imgur.com/2lBYrfD.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Random Rewards</b></td><td><i>Player</i></td><td>Opens a rewards inventory that randomizes its contents.</td></tr><tr><td><a href=\"https://i.imgur.com/8F95xsI.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Loot Table Rewards</b></td><td><i>Player</i></td><td>Opens a rewards inventory that randomizes its contents based on a loot table. With MythicMobs installed it accepts a MythicMobs drop table name as well.</td></tr><tr><td><a href=\"https://i.imgur.com/KlJTbgy.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Mob Spawner</b></td><td><i>Location</i></td><td>Spawns configured mobs at the function&#x27;s location.</td></tr><tr><td><a href=\"https://i.imgur.com/QqZIQ15.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>NPC Spawner</b></td><td><i>Location</i></td><td>Spawns configured NPC at the function&#x27;s location. _Only available with Citizens installed. Experimental feature! Stable, but not reliable as of yet!_</td></tr><tr><td><a href=\"https://i.imgur.com/AoWkosU.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Block Editor</b></td><td><i>Location</i></td><td>Places or removes a block at the function&#x27;s location.</td></tr><tr><td><a href=\"https://i.imgur.com/bDfRRNX.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Sound Player</b></td><td><i>Location</i></td><td>Plays a configured sound.</td></tr><tr><td><a href=\"https://i.imgur.com/lKfaM32.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Place Redstone Block</b></td><td><i>Location</i></td><td>Places a redstone block at the function&#x27;s location.</td></tr><tr><td><a href=\"https://i.imgur.com/WlMoWZQ.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Door Controller</b></td><td><i>Location</i></td><td>Makes a door lockable and unlockable with triggers.</td></tr><tr><td><a href=\"https://i.imgur.com/4PYBkKZ.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Allow Block Place/Break</b></td><td><i>Location</i></td><td>Allows placing or breaking blocks, and optionally using buckets, at the function&#x27;s location. Only takes effect once it has been triggered.</td></tr><tr><td><a href=\"https://i.imgur.com/MKlkBRh.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Hologram</b></td><td><i>Location</i></td><td>_(1.3.0+)_ Displays a text hologram at a specified location.</td></tr><tr><td><a href=\"https://i.imgur.com/Vx2qiqp.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Moving Block</b></td><td><i>Location</i></td><td>Turns the block at this location into a moving block, which can be configured to slowly move somewhere else when triggered.</td></tr><tr><td><a href=\"https://i.imgur.com/NzC5MMy.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Moving Block Cluster</b></td><td><i>Location</i></td><td>Turns a group of blocks into a group of moving blocks. Works similarly to moving block, but allows selecting many blocks.</td></tr><tr><td></td><td><b>Room Door Controller</b></td><td><i>Room</i></td><td>_(Procedural dungeons only)_ Opens, closes, or toggles named doors in the room this function is placed in. Pairs with the <a href=\"#\" class=\"wl\" data-p=\"triggers\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Triggers\">Room Door trigger</a> and the <a href=\"#\" class=\"wl\" data-p=\"mythic-mobs-integration/mechanics/roomdoor\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Mythic-Mobs-Integration/Mechanics/RoomDoor\"><code>roomdoor</code> mythic mechanic</a>.</td></tr><tr><td><a href=\"https://i.imgur.com/sWvmSgq.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Mythic Skill</b></td><td><i>Meta</i></td><td>Executes a Mythic Skill from the function&#x27;s location.</td></tr><tr><td><a href=\"https://i.imgur.com/feyEUwC.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Mythic Signal</b></td><td><i>Meta</i></td><td>Sends a Mythic Mobs signal to nearby mobs. (For use with the ~onSignal MM trigger.)</td></tr><tr><td><a href=\"https://i.imgur.com/C7h6Uq6.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Command Sender</b></td><td><i>Meta</i></td><td>Sends a command either from the player/players or console.</td></tr><tr><td><a href=\"https://i.imgur.com/l34PTuS.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Multi-Function</b></td><td><i>Meta</i></td><td>Allows running multiple functions at the same location and from the same trigger.</td></tr><tr><td><a href=\"https://i.imgur.com/4U25SOy.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Function Repeater</b></td><td><i>Meta</i></td><td>Runs a function a configured number of times on an interval.</td></tr><tr><td><a href=\"https://i.imgur.com/av1WoSb.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Delayed Function</b></td><td><i>Meta</i></td><td>Runs a function after a specified number of ticks have passed.</td></tr><tr><td><a href=\"https://i.imgur.com/1BxYZ26.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Function Randomizer</b></td><td><i>Meta</i></td><td>Randomly chooses from a list of functions provided to run.</td></tr><tr><td><a href=\"https://i.imgur.com/AlCRfHe.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Function Sequencer</b></td><td><i>Meta</i></td><td>Runs a list of functions one at a time in order each time it&#x27;s triggered.</td></tr><tr><td></td><td><b>Conditional Function</b></td><td><i>Meta</i></td><td>Checks its own conditions when triggered, then runs its THEN functions if they pass or its ELSE functions if they don&#x27;t.</td></tr><tr><td><a href=\"https://i.imgur.com/YRJ3W56.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></td><td><b>Dungeon Variable</b></td><td><i>Meta</i></td><td>Sets, adds to, or subtracts from an instance-scoped variable. Best used with the <b>Variable Comparison</b> condition. Variables can be used in message senders with the following format: <code>[my_variable_here]</code>. Also readable from Mythic skills via <code>&lt;dungeon.var.X&gt;</code>. See <a href=\"#\" class=\"wl\" data-p=\"mythic-mobs-integration/variables\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Mythic-Mobs-Integration/Variables\">Variables</a>.</td></tr><tr><td></td><td><b>Player Variable</b></td><td><i>Meta</i></td><td>Sets, adds to, or subtracts from a variable scoped to each target player. Optionally persists between dungeon runs. Read it back with the <b>Player Variable Comparison</b> condition.</td></tr></tbody></table></div>\n<p>Function Options</p>\n<hr>\n<p>Each function&#x27;s own options, in the order they appear on the hotbar. Defaults are what a freshly placed function starts with.</p>\n<h4>Dungeon</h4>\n<h5>Start Dungeon</h5>\n<p>No options. If the trigger fires with more than one target at once the dungeon starts immediately; otherwise every player in the instance has to trigger it before it starts.</p>\n<h5>Finish Dungeon</h5>\n<ul><li><b>Leave Dungeon</b> - <code>ENABLED</code> by default. When on, the player is sent to the dungeon&#x27;s exit location after finishing. Turn it off to keep them inside.</li></ul>\n<h5>Leave Dungeon</h5>\n<p>No options.</p>\n<h5>Respawn Checkpoint</h5>\n<ul><li><b>Player Direction</b> - Sets the direction players face when they respawn here to the direction you are currently looking. Default <code>0</code> yaw / <code>0</code> pitch.</li><li><b>Use as Save Point</b> - <code>DISABLED</code> by default. When on, a player who leaves without finishing will start here next time. Ignored in procedural dungeons.</li></ul>\n<h5>Signal Sender</h5>\n<ul><li><b>Signal Name</b> - Default <code>trigger</code>. The name Signal Receiver triggers listen for.</li><li><b>Signal Range</b> - Default <code>0</code>, meaning infinite. Distance in blocks the signal reaches.</li></ul>\n<h5>Lives Editor</h5>\n<ul><li><b>Set Lives</b> - Default <code>1</code>. The amount used by the change mode below.</li><li><b>Set Lives Change Mode</b> - Cycles <code>ADD</code> (default), <code>SET</code>, <code>MULTIPLY</code>.</li></ul>\n<p>Requires <code>PlayerLives</code> to be <code>1</code> or more in the dungeon config; otherwise the function logs a warning and does nothing. Changes that would take a player to <code>0</code> lives or below are ignored.</p>\n<h5>Player Reviver</h5>\n<ul><li><b>Max Revives</b> - Default <code>1</code>. <code>0</code> means unlimited.</li><li><b>Lives After Revival</b> - Default <code>1</code>. Minimum <code>1</code>.</li></ul>\n<h5>Chunk Loader</h5>\n<ul><li><b>Radius</b> - Default <code>0</code>, meaning just the chunk the function is in. Otherwise force-loads a square of chunks with this radius around it.</li></ul>\n<p>Triggering it again releases the chunks. All chunks it holds are released when the instance shuts down.</p>\n<h5>Dungeon Status</h5>\n<ul><li><b>Set Status</b> - No default. Free text; read back with the Dungeon Status condition.</li></ul>\n<h5>Dungeon Difficulty</h5>\n<ul><li><b>Set Difficulty</b> - No default. Must match one of the difficulty level names in the dungeon config. Unknown names are ignored.</li></ul>\n<h4>Player</h4>\n<h5>Message Sender</h5>\n<ul><li><b>Message Type</b> - Cycles <code>CHAT</code> (default) and <code>ACTION_BAR</code>.</li><li><b>Edit Message</b> - Default <code>DEFAULT</code>. Supports colour codes, PlaceholderAPI placeholders, and <code>[dungeon_variable]</code> substitution.</li></ul>\n<h5>Teleporter</h5>\n<ul><li><b>Set Location</b> - No default; the function logs an error when the dungeon loads and does nothing until set. Click once to see the current target (marked with particles), click again to store the spot you are standing on. The prompt times out after 10 seconds.</li></ul>\n<p>This function always requires a target, so <code>NONE</code> is not selectable.</p>\n<h5>Title Sender</h5>\n<ul><li><b>Edit Title</b> - Default <code>&quot; &quot;</code> (a single space).</li><li><b>Edit Subtitle</b> - Default <code>&quot; &quot;</code>.</li><li><b>Duration</b> - Default <code>80</code> ticks.</li><li><b>Fade-In Ticks</b> - Default <code>10</code>.</li><li><b>Fade-Out Ticks</b> - Default <code>10</code>.</li></ul>\n<p>Title and subtitle both support PlaceholderAPI and <code>[dungeon_variable]</code> substitution.</p>\n<h5>Boss Bar</h5>\n<ul><li><b>Title</b> - Default <code>&amp;fBoss Bar</code>. Supports colour codes, PlaceholderAPI, and <code>[dungeon_variable]</code> substitution.</li><li><b>Color</b> - Cycles Bukkit&#x27;s bar colours. Default <code>RED</code>.</li><li><b>Style</b> - Cycles Bukkit&#x27;s bar styles. Default <code>SOLID</code>.</li><li><b>Progress Source</b> - Empty by default. A variable or placeholder that drives the bar fill, for example <code>[boss_hp]</code> or <code>%md_dungeon_time_left_seconds%</code>. Type <code>clear</code> to go back to the fixed value.</li><li><b>Fixed Progress</b> - Default <code>1.0</code>. Used when no progress source is set, or when the source doesn&#x27;t resolve to a number. Clamped to <code>0.0</code>-<code>1.0</code>.</li><li><b>Source Is Percent</b> - <code>DISABLED</code> by default, meaning the source is read as <code>0.0</code>-<code>1.0</code>. Turn it on if your source produces <code>0</code>-<code>100</code>.</li><li><b>Visible by Default</b> - <code>ENABLED</code> by default. Triggering the function toggles visibility.</li></ul>\n<p>The bar refreshes once a second while visible.</p>\n<h5>Item Dispenser</h5>\n<ul><li><b>Choose Item</b> - Opens the item picker. No default item; the function does nothing until one is chosen.</li><li><b>Toggle Drop</b> - <code>DISABLED</code> by default (item is given to the target). When on, the item is dropped at the function&#x27;s location instead. The item is also dropped if there are no targets.</li><li><b>Toggle Notification</b> - <code>ENABLED</code> by default. Sends the &quot;You got X&quot; message.</li></ul>\n<p>MythicMobs items are regenerated from their MythicMobs definition when the function fires.</p>\n<h5>Key Dispenser</h5>\n<p>Has all three Item Dispenser options, plus:</p>\n<ul><li><b>Toggle Taking Key Out of Dungeon</b> - <code>ENABLED</code> by default. When turned off, the key is tagged as a dungeon item and is stripped when the player leaves.</li></ul>\n<p>Defaults to a <code>Dungeon Key</code> (a renamed tripwire hook carrying the key tag). With the default <code>PLAYER</code> target type there is only ever one target and that player gets the key. If you set the target type to <code>PARTY</code> and the function ends up with more than one target, the key goes to the party leader, or to the first eligible member still in the instance if the leader is gone. Unless the trigger allows retriggering, the function disables itself after handing out the key.</p>\n<h5>Rewards</h5>\n<ul><li><b>Edit Rewards</b> - Opens a 45-slot inventory. Whatever is in it when you close it becomes the reward list.</li><li><b>Exp Reward</b> - Default <code>0</code>.</li><li><b>Exp Levels Reward</b> - Default <code>0</code>.</li><li><b>Cooldown Settings</b> - Opens a sub-hotbar:<ul><li><b>Toggle Cooldown Enabled</b> - Cooldowns are on by default.</li><li><b>Toggle Custom Cooldown</b> - <code>DISABLED</code> by default; the dungeon&#x27;s own cooldown settings are used. Turning it on reveals the three options below.</li><li><b>Type</b> - Cycles <code>TIMER</code> (default), <code>HOURLY</code>, <code>DAILY</code>, <code>WEEKLY</code>, <code>MONTHLY</code>.</li><li><b>Cooldown in Minutes</b> / <b>Hour of Day</b> - Default <code>0</code>. Shows as minutes for <code>TIMER</code>, otherwise as the hour of day the reward resets.</li><li><b>Day of Week</b> / <b>Day of Month</b> - Default <code>1</code>. Only shown for <code>WEEKLY</code> and <code>MONTHLY</code>.</li></ul></li></ul>\n<h5>Random Rewards</h5>\n<p>Has all the Rewards options, plus:</p>\n<ul><li><b>Min Rewards</b> - Default <code>1</code>.</li><li><b>Max Rewards</b> - Default <code>3</code>.</li></ul>\n<p>Rolls a random count between the two, plus the difficulty level&#x27;s bonus loot, and hands out that many distinct items from the reward list.</p>\n<h5>Loot Table Rewards</h5>\n<ul><li><b>Set Loot Table</b> - Empty by default. The unique name of a loot table created with <code>/md loot create</code>. With MythicMobs installed, a MythicMobs drop table name is accepted too and is used directly.</li><li><b>Exp Reward</b> - Default <code>0</code>.</li><li><b>Exp Levels Reward</b> - Default <code>0</code>.</li><li><b>Cooldown Settings</b> - Same sub-hotbar as Rewards.</li></ul>\n<p>There is no &quot;Edit Rewards&quot; button here; the contents come entirely from the loot table.</p>\n<p>There are two implementations behind this one name. With MythicMobs installed the browser gives you the MythicMobs-aware version, which is the one that also accepts a MythicMobs drop table name. Without MythicMobs you get the plain version, which only understands Mythic Dungeons loot tables. They look identical in the function browser and in the editor. A dungeon saved before MythicMobs was installed keeps loading after you install it, because the plain version stays loadable alongside the Mythic one. Going the other way is not safe: if you remove MythicMobs from a server whose dungeons already use the MythicMobs-aware version, the MythicMobs-aware class is no longer registered and those dungeons fail to load with an error naming it.</p>\n<h4>Location</h4>\n<h5>Mob Spawner</h5>\n<ul><li><b>Mob Name</b> - Default <code>zombie</code>. A vanilla entity type or a MythicMobs mob ID. MythicMobs IDs are resolved first and are matched case-insensitively.</li><li><b>Mythic Mob Level</b> - Default <code>0</code>. Only shown when MythicMobs is installed. Accepts a range written as <code>3-6</code> or <code>3to6</code>.</li><li><b>Amount</b> - Default <code>1</code>. How many mobs this spawner produces per activation.</li><li><b>Spawn Delay</b> - Default <code>0</code> ticks before the first spawn.</li><li><b>Spawn Interval</b> - Default <code>0</code> ticks between spawns (treated as <code>1</code>).</li><li><b>Mob Direction</b> - Sets the spawn yaw to the direction you are currently looking. Default <code>0</code>.</li></ul>\n<p>If the dungeon uses difficulty levels, the amount, health, damage, and Mythic level are all scaled by the active difficulty (and by any per-mob override on it). Mobs spawned by this function have their despawn mode forced to <code>NEVER</code> so kill counters can&#x27;t be broken by chunk unloads.</p>\n<h5>NPC Spawner</h5>\n<p><i>Only available with Citizens installed.</i></p>\n<ul><li><b>NPC ID</b> - Default <code>-1</code>. The Citizens NPC ID to clone and spawn.</li><li><b>NPC Direction</b> - Sets the spawn yaw to the direction you are currently looking. Default <code>0</code>.</li></ul>\n<h5>Block Editor</h5>\n<ul><li><b>Block Type</b> - Default <code>AIR</code>. A Bukkit material name, <code>mythic:&lt;id&gt;</code> for a MythicCrucible custom block, or (in remover mode only) <code>ANY</code>.</li><li><b>Toggle Remover</b> - <code>DISABLED</code> by default, meaning the function places the block. Turn it on to remove it instead.</li><li><b>Block Direction</b> - Default <code>NORTH</code>. Accepts a <code>BlockFace</code> for directional and rotatable blocks, an <code>Axis</code> (<code>X</code>/<code>Y</code>/<code>Z</code>) for orientable blocks such as logs, and a <code>Rail.Shape</code> for rails. Not usable in remover mode or on blocks that have no direction.</li></ul>\n<p>The function renames itself to <code>&lt;BLOCK&gt; Placer</code> or <code>&lt;BLOCK&gt; Remover</code> in the editor.</p>\n<h5>Sound Player</h5>\n<ul><li><b>Set Sound</b> - Empty by default; the function does nothing until set. Any sound key, for example <code>minecraft:block.note_block.bell</code>.</li><li><b>Set Category</b> - Default <code>MASTER</code>. Any Bukkit <code>SoundCategory</code>. An unrecognised value silently falls back to <code>MASTER</code>.</li><li><b>Set Pitch</b> - Default <code>1.0</code>.</li><li><b>Set Volume</b> - Default <code>1.0</code>.</li><li><b>Toggle Sound Location</b> - <code>DISABLED</code> by default, meaning the sound plays at each target player&#x27;s own position. Turn it on to play it at the function&#x27;s location.</li></ul>\n<h5>Place Redstone Block</h5>\n<ul><li><b>Delay</b> - Default <code>0</code> ticks before the first pulse.</li><li><b>Repeat Count</b> - Default <code>1</code>. How many redstone pulses to send.</li><li><b>Repeat Interval</b> - Default <code>5</code> ticks between pulses.</li></ul>\n<p>Each pulse places a redstone block and removes it one tick before the next pulse. The final pulse leaves the redstone block in place.</p>\n<h5>Door Controller</h5>\n<ul><li><b>Start Locked</b> - <code>ENABLED</code> by default.</li><li><b>Auto Open/Close</b> - <code>ENABLED</code> by default. When on, unlocking also opens the door and locking also closes it.</li><li><b>Hide Key Name</b> - <code>DISABLED</code> by default. When on, the locked-door message won&#x27;t name the key that opens it. (The key is only ever named when the function&#x27;s trigger is a Key Item Detector.)</li></ul>\n<p>Triggering the function flips the lock state. While locked, interacting with the door (or the block directly above or below it) is cancelled and the player is told the door is locked. This function does not need a trigger.</p>\n<h5>Allow Block Place/Break</h5>\n<ul><li><b>Toggle Place</b> - <code>ENABLED</code> by default.</li><li><b>Toggle Break</b> - <code>ENABLED</code> by default.</li><li><b>Toggle Bucket</b> - <code>DISABLED</code> by default. When on, emptying a bucket here follows the Place setting and filling one follows the Break setting.</li></ul>\n<p>This function does not need a trigger, but it only takes effect once it has been triggered. If you leave it without a trigger it is given a <code>Dungeon Start</code> trigger when the dungeon loads, so it is active from the beginning.</p>\n<h5>Hologram</h5>\n<ul><li><b>Set Location</b> - Defaults to the function&#x27;s own block. Click once to see the current spot (marked with particles), click again to place the hologram 1.75 blocks above where you are standing. The prompt times out after 10 seconds.</li><li><b>Hologram Message</b> - No default. Use <code>\\n</code> for extra lines.</li><li><b>View Distance</b> - Default <code>25</code> blocks.</li><li><b>Visible by Default</b> - <code>ENABLED</code> by default. Triggering the function toggles visibility.</li></ul>\n<h5>Moving Block</h5>\n<ul><li><b>Set Destination</b> - No default; the function logs an error at dungeon load if it is unset. Click once to see the current destination, click again to set it to the block you are looking at (within 10 blocks).</li><li><b>Movement Speed</b> - Default <code>0.0</code> blocks per second, which means the block will not move until you set it.</li><li><b>Become Physical</b> - <code>DISABLED</code> by default. When on, the block turns back into a real block on arrival and is picked up again on the next trigger. Container contents are not preserved.</li></ul>\n<p>Triggering it while it is moving pauses it; triggering a paused block resumes it; triggering a block that has arrived sends it back the other way. Vertically moving blocks will not carry players unless <code>allow-flight</code> is <code>true</code> in <code>server.properties</code>.</p>\n<h5>Moving Block Cluster</h5>\n<p>Has all the Moving Block options, plus:</p>\n<ul><li><b>Select Area</b> - Right-click sets corner 1, left-click sets corner 2. Every non-air, non-liquid block in the box is added to the cluster.</li><li><b>Remove Area</b> - Same corner controls; removes every stored block inside the box.</li></ul>\n<h4>Meta</h4>\n<h5>Command Sender</h5>\n<ul><li><b>Set Command</b> - Empty by default; the function logs a warning and does nothing until set. Enter the command without the leading <code>/</code>.</li><li><b>Command Sender</b> - Cycles <code>CONSOLE</code> (default) and <code>PLAYER</code>. A third option, <code>PLAYER_FORCED</code>, only appears when <code>General.CommandFunctionAsOp</code> is <code>true</code> in the plugin&#x27;s <code>config.yml</code>; it runs the command as the player even if they lack permission.</li><li><b>Toggle &#x27;For Each Player&#x27;</b> - <code>DISABLED</code> by default, meaning the command runs once. Turn it on to run it once per target.</li></ul>\n<p>PlaceholderAPI placeholders in the command are resolved against the target player when PlaceholderAPI is installed.</p>\n<h5>Mythic Skill</h5>\n<p><i>Only available with MythicMobs installed.</i></p>\n<ul><li><b>Skill Name</b> - Default <code>NONE</code>. Must be an existing MythicMobs skill; unknown names are rejected when you type them.</li></ul>\n<p>The skill is cast from the function&#x27;s location. The function renames itself to <code>Skill: &lt;name&gt;</code> in the editor.</p>\n<h5>Mythic Signal</h5>\n<p><i>Only available with MythicMobs installed.</i></p>\n<ul><li><b>Signal Name</b> - Default <code>NONE</code>. The signal string sent to <code>~onSignal</code> skill triggers.</li><li><b>Range</b> - Default <code>15</code> blocks.</li><li><b>Mob Filter</b> - A comma-separated list of MythicMobs mob types that should receive the signal. Set one; see the note below.</li></ul>\n<p>The function renames itself to <code>Signal: &lt;name&gt;</code> in the editor. Note: on the current build a Mythic Signal with an empty mob filter reaches no mobs at all, and changing the filter adds to the old one instead of replacing it. Set the filter once, to the exact mob types you want, and re-place the function if you need to change it.</p>\n<h5>Multi-Function</h5>\n<ul><li><b>Add Function</b> - Opens the function browser and appends the chosen function. Limit 54.</li><li><b>Edit Function</b> - Opens the list of contained functions for editing.</li><li><b>Remove Function</b> - Opens the list of contained functions for removal.</li><li><b>Change Triggers</b> - Opens the trigger editor for the contained functions.</li></ul>\n<p>Contained functions only run from the Multi-Function if they have no trigger of their own.</p>\n<h5>Function Repeater</h5>\n<ul><li><b>Set Function</b> - Opens the function browser. Holds a single function; choosing another replaces it.</li><li><b>Edit Function</b> - Opens the stored function&#x27;s hotbar.</li><li><b>Start Delay</b> - Default <code>0</code> ticks.</li><li><b>Repeater Interval</b> - Default <code>20</code> ticks between runs.</li><li><b>Max Repeats</b> - Default <code>0</code>, meaning it repeats until the dungeon ends. If the trigger allows retriggering, the counter resets when the limit is reached.</li></ul>\n<h5>Delayed Function</h5>\n<ul><li><b>Set Function</b> - Opens the function browser. Holds a single function.</li><li><b>Edit Function</b> - Opens the stored function&#x27;s hotbar.</li><li><b>Start Delay</b> - Default <code>0</code> ticks.</li></ul>\n<p>Targets are re-checked when the delay elapses; players who left the instance in the meantime are dropped.</p>\n<h5>Function Randomizer</h5>\n<p>Has the four Multi-Function options. Picks exactly one of the contained functions at random each time it is triggered. Unlike Multi-Function, the chosen function runs whether or not it has a trigger of its own.</p>\n<h5>Function Sequencer</h5>\n<p>Has the four Multi-Function options, plus:</p>\n<ul><li><b>Loop Sequence</b> - <code>ENABLED</code> by default. When off, the function disables itself after running the last entry.</li></ul>\n<h5>Conditional Function</h5>\n<ul><li><b>Conditions</b> - Opens the conditions editor for this function&#x27;s own condition list.</li><li><b>Add THEN Function</b> / <b>Edit THEN Functions</b> / <b>Remove THEN Function</b> - Manage the branch that runs when the conditions pass.</li><li><b>ELSE Functions</b> - Opens a sub-hotbar with Add / Edit / Remove for the branch that runs when the conditions fail.</li></ul>\n<p>As with Multi-Function, branch functions only run from here if they have no trigger of their own.</p>\n<h5>Dungeon Variable</h5>\n<ul><li><b>Edit Name</b> - No default. Colour codes are stripped.</li><li><b>Edit Mode</b> - Cycles <code>SET</code> (default), <code>ADD</code>, <code>SUBTRACT</code>.</li><li><b>Value</b> - No default. In <code>ADD</code> and <code>SUBTRACT</code> modes it must be a number; a non-numeric value at runtime logs a warning and skips the change.</li></ul>\n<p>The variable is scoped to the running instance. Read it back with the <b>Variable Comparison</b> condition, with <code>[my_variable]</code> inside message and title text, or from Mythic skills with <code>&lt;dungeon.var.my_variable&gt;</code>. A variable that was never written reads as <code>0</code> only when Mythic Mobs is not installed; with Mythic Mobs installed it reads as the literal text <code>UNDEFINED</code>, so set your variables on the Dungeon Start trigger or read them with a default, <code>[my_variable|0]</code>. See <a href=\"#\" class=\"wl\" data-p=\"mythic-mobs-integration/variables\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Mythic-Mobs-Integration/Variables\">Variables</a>. The write is applied one tick after the trigger fires.</p>\n<h5>Player Variable</h5>\n<ul><li><b>Edit Name</b> - No default. Colour codes are stripped.</li><li><b>Edit Mode</b> - Cycles <code>SET</code> (default), <code>ADD</code>, <code>SUBTRACT</code>.</li><li><b>Value</b> - No default. Numeric in <code>ADD</code> and <code>SUBTRACT</code> modes.</li><li><b>Persist Across Runs</b> - <code>DISABLED</code> by default. When on, the value is saved to the player and restored on their next run.</li></ul>\n<p>This function always requires a target. Read the value back with the <b>Player Variable Comparison</b> condition. Note that the <code>[my_variable]</code> substitution used by Message Sender, Title Sender, and Boss Bar reads dungeon variables only, not player variables.</p>\n<h4>Room</h4>\n<h5>Room Door Controller</h5>\n<p><i>Procedural dungeons only.</i></p>\n<ul><li><b>Door Name</b> - Default <code>all</code>, meaning every door in the room that has a room on the other side. Otherwise the name of a single connector door.</li><li><b>Action</b> - Cycles <code>TOGGLE</code> (default), <code>OPEN</code>, <code>CLOSE</code>.</li><li><b>Keep Entrance Open</b> - <code>ENABLED</code> by default. Only shown when the door name is <code>all</code>; keeps the door the player entered through open.</li></ul>"},"dungeons:functions-yml":{"h":"<p>Mythic Dungeons features an extensive in-game editor to set up all your functions, triggers and conditions. However, there may be some situations where you don&#x27;t want to - or perhaps, can&#x27;t - get in-game to edit the dungeon&#x27;s functions.</p>\n<p>Where the file lives</p>\n<hr>\n<p>Where a dungeon&#x27;s functions are stored depends on its type.</p>\n<ul><li><b>Classic</b> and <b>Continuous</b> dungeons keep every function in a single <code>functions.yml</code> in the map folder, <code>plugins/MythicDungeons/maps/&lt;DungeonName&gt;/functions.yml</code>.</li><li><b>Procedural</b> dungeons have no <code>functions.yml</code>. Each room stores its own functions under a <code>Functions:</code> key inside <code>plugins/MythicDungeons/maps/&lt;DungeonName&gt;/rooms/&lt;RoomName&gt;.yml</code>, and their locations are relative to the room, not the world. See <a href=\"#\" class=\"wl\" data-p=\"procedural-dungeons\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Procedural-Dungeons\">Procedural Dungeons</a>.</li></ul>\n<p>File format</p>\n<hr>\n<p><code>functions.yml</code> has two top-level keys: a <code>Version</code> number, and a <code>Functions</code> list.</p>\n<p>Every entry in the list is a Bukkit-serialized object. The <code>==</code> key holds the full class name of the function, and every other key is named exactly after the field it stores. A function&#x27;s trigger is nested inside it under <code>trigger</code>, and that trigger&#x27;s conditions are nested inside the trigger under <code>conditions</code>.</p>\n<pre class=\"cb\" data-lang=\"yaml\"><code>Version: 1\nFunctions:\n- ==: net.playavalon.mythicdungeons.dungeons.functions.FunctionSpawnMythicMob\n  mob: DrunkenRaider\n  delay: 0\n  levelString: &#x27;0&#x27;\n  interval: 0\n  targetType: &amp;id001\n    ==: net.playavalon.mythicdungeons.api.parents.FunctionTargetType\n    value: NONE\n  location:\n    ==: org.bukkit.Location\n    x: 188.0\n    y: -42.0\n    z: 45.0\n    pitch: 0.0\n    yaw: 0.0\n  trigger:\n    ==: net.playavalon.mythicdungeons.dungeons.triggers.TriggerDistance\n    delayTicks: 0\n    forEachPlayer: false\n    limitToRoom: false\n    count: 2\n    allowRetrigger: false\n    radius: 100.0\n    conditions: []\n  maxCount: 1\n  yaw: -89.0</code></pre>\n<p>Notes</p>\n<hr>\n<ul><li><code>location</code> is always stored without a world; the world is filled in when an instance is created.</li><li>Key order inside an entry is not meaningful and will change whenever the dungeon is saved.</li><li>Any key you leave out falls back to the function&#x27;s built-in default, listed in <a href=\"#\" class=\"wl\" data-p=\"functions\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Functions\">Function Options</a>.</li><li>YAML anchors (<code>&amp;id001</code> / <code>*id001</code>) will appear when several functions share an identical value such as a target type. They are written by the serializer and are safe to leave alone.</li><li>If a function&#x27;s class can&#x27;t be found when the dungeon loads, the whole file fails to load and the dungeon reports an error naming the class. This most often happens after uninstalling a plugin that added a custom function.</li><li>Edit the file only while the server is stopped, or with the dungeon unloaded. Saving a dungeon in-game rewrites the whole file and will discard your changes.</li></ul>"},"dungeons:gui-menus":{"h":"<div class=\"tw\"><table><thead><tr><th><b>DISCLAIMER</b></th></tr></thead><tbody><tr><td><i>The following is a feature only available in Mythic Dungeons 1.3.0+!</i></td></tr></tbody></table></div>\n<p>In order to make dungeon elements easy to customize and edit in-game, Mythic Dungeons uses both a chest-based GUI for selecting elements, and a hotbar-based menu for modifying options on those elements.</p>\n<h3>Browser Menu Buttons</h3>\n<p>In order for your custom elements to even be accessible by server owners, you must first create a menu button for it. This is done through the provided <code>buildMenuButton()</code> method.</p>\n<pre class=\"cb\" data-lang=\"java\"><code>    @Override\n    public MenuButton buildMenuButton() {\n        // Create a new menu button that uses the paper item as its base.\n        MenuButton functionButton = new MenuButton(Material.PAPER); \n        // Set the display name of the item.\n        functionButton.setDisplayName(&quot;&amp;aMessage Sender&quot;);\n\n        // Add some lore to describe the function.\n        functionButton.addLore(&quot;&amp;eSends a chat or action bar&quot;);\n        functionButton.addLore(&quot;&amp;emessage to the target player(s).&quot;);\n\n        // Finally, return the button you&#x27;ve created so the menu can use it.\n        return functionButton;\n    }</code></pre>\n<p><b>This code produces this menu button.</b></p>\n<p><a href=\"https://i.imgur.com/Kw5ABUK.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></p>\n<h4>MenuButton reference</h4>\n<p><code>MenuButton</code> wraps a single <code>ItemStack</code>. The same class is used for browser buttons and for hotbar buttons.</p>\n<div class=\"tw\"><table><thead><tr><th>Member</th><th>What it does</th></tr></thead><tbody><tr><td><code>new MenuButton(Material mat)</code></td><td>Build from a material.</td></tr><tr><td><code>new MenuButton(ItemStack item)</code></td><td>Build from an existing item, so you can pre-set NBT, custom model data, and so on.</td></tr><tr><td><code>void setDisplayName(String name)</code></td><td>Set the name. Colour codes (<code>&amp;a</code>, hex) are parsed for you.</td></tr><tr><td><code>void addLore(String line)</code></td><td>Append one lore line, colourised.</td></tr><tr><td><code>void setLoreLine(int index, String line)</code></td><td>Replace an existing lore line.</td></tr><tr><td><code>void removeLore(int index)</code></td><td>Delete one lore line.</td></tr><tr><td><code>void clearLore()</code></td><td>Delete all lore.</td></tr><tr><td><code>void setAmount(int amount)</code></td><td>Set the stack size. The item is clamped to 1-99.</td></tr><tr><td><code>void setEnchanted(boolean enchanted)</code></td><td>Apply or remove the enchant glow. Very handy for showing a toggle&#x27;s state.</td></tr><tr><td><code>ItemStack getItem()</code></td><td>The finished item.</td></tr><tr><td><code>String getDisplayName()</code> / <code>int getAmount()</code> / <code>boolean isEnchanted()</code></td><td>Read the current state. <code>getDisplayName()</code> gives the already-colourised name, and <code>getAmount()</code> gives the raw value you passed, not the clamped one.</td></tr></tbody></table></div>\n<blockquote><p>_<b>Caution:</b> <code>setLoreLine</code> is meant to log and swallow an out-of-range index, but its guard catches the wrong exception type and never fires. Calling it with an index past the end of the lore throws out of <code>buildButton()</code>, which aborts the whole menu build and leaves the player&#x27;s hotbar half-drawn. Call <code>addLore</code> until the line exists, or rebuild the lore with <code>clearLore()</code> and <code>addLore(...)</code>._</p></blockquote>\n<h3>The Hotbar Menu</h3>\n<p>Hotbar menus take up a lot of space in the class, however they are relatively straightforward! Simply populate the <code>buildHotbarMenu()</code> method with options and Mythic Dungeons will do the rest. Let&#x27;s break it down...</p>\n<p>For every option you want to have in the hotbar menu, you need to call <code>menu.addMenuItem</code>, and then provide details about how your button will behave. There are a couple pre-made menu item types to help simplify the process, or you can make your own.</p>\n<blockquote><p>_<b>Do not assign <code>menu</code> yourself.</b> The base-class javadoc on <code>buildHotbarMenu()</code> tells you to &quot;Assign <code>menu</code> with a new HotbarMenu object&quot;. That instruction is out of date. By the time your <code>buildHotbarMenu()</code> is called, <code>menu</code> has already been created and the element&#x27;s base buttons have already been added to it. Just call <code>menu.addMenuItem(...)</code>. Creating a new <code>HotbarMenu</code> here throws away the Back button and everything else the base class put there._</p></blockquote>\n<h4>Nine slots, and no more</h4>\n<p>A hotbar has nine slots, and each menu item takes one. Items are placed in the order you add them, starting at slot 0, and <b>anything past slot 8 is silently dropped</b>: the button never appears, and its option becomes unreachable in-game.</p>\n<p>Your element&#x27;s base class has already used some of those slots before <code>buildHotbarMenu()</code> runs:</p>\n<div class=\"tw\"><table><thead><tr><th>Element</th><th>Base buttons</th><th>Slots left for you</th></tr></thead><tbody><tr><td>Function</td><td>Back, Trigger, Target Type</td><td>6</td></tr><tr><td>Function with <code>setAllowChangingTargetType(false)</code></td><td>Back, Trigger</td><td>7</td></tr><tr><td>Trigger</td><td>Back, Trigger Conditions</td><td>7</td></tr><tr><td>Condition</td><td>Back, invert toggle</td><td>7</td></tr></tbody></table></div>\n<p>Calling <code>addRoomLimitToggleButton()</code> in a trigger&#x27;s <code>buildHotbarMenu()</code> costs one more slot.</p>\n<p>If you need more options than that, build a second <code>HotbarMenu</code> with <code>HotbarMenu.create()</code> and add a menu item that switches the player onto it with <code>MythicDungeons.inst().getMythicPlayer(player).setHotbar(subMenu)</code>. Switching menus automatically pushes the menu the player was on onto their menu stack, so a Back button (<code>aPlayer.previousHotbar()</code>) returns to it. The optional second argument, <code>setHotbar(subMenu, true)</code>, additionally moves the player&#x27;s selected slot back to the first button. That is how the rewards functions handle large option sets.</p>\n<blockquote><p>_Always create menus with the static <code>HotbarMenu.create()</code>, never <code>new HotbarMenu()</code>. <code>HotbarMenu</code> is abstract, and <code>create()</code> picks the Paper-specific subclass when the server supports it._</p></blockquote>\n<h4>MenuItem</h4>\n<p>The default menu entry. All it requires is that you provide a button and some code to run when the button is selected!</p>\n<pre class=\"cb\" data-lang=\"java\"><code>        menu.addMenuItem(new MenuItem() {\n            // Build the hotbar menu button. Works similarly to the function&#x27;s menu button.\n            // You can also customize the quantity of the item, and whether it&#x27;s enchanted!\n            @Override\n            public void buildButton() {\n                button = new MenuButton(Material.KNOWLEDGE_BOOK);\n                button.setDisplayName(&quot;&amp;d&amp;lExample Option&quot;);\n            }\n\n            // What to do when the button is selected with right-click.\n            @Override\n            public void onSelect(PlayerEvent event) {\n                Player player = event.getPlayer();\n\n                player.sendMessage(&quot;Player has selected this button!&quot;);\n            }\n        });</code></pre>\n<h5>The hooks you can override</h5>\n<p><code>buildButton()</code> and <code>onSelect(PlayerEvent)</code> are abstract, so you must implement both. The rest are optional and empty by default.</p>\n<div class=\"tw\"><table><thead><tr><th>Hook</th><th>Fires when</th></tr></thead><tbody><tr><td><code>void buildButton()</code></td><td>The menu is drawn or redrawn. Assign the <code>button</code> field here. Called on every redraw, so this is where a button&#x27;s lore or glow reflects the option&#x27;s current value.</td></tr><tr><td><code>void onSelect(PlayerEvent event)</code></td><td>The player <b>right-clicks</b> while holding this slot.</td></tr><tr><td><code>void onClick(PlayerEvent event)</code></td><td>The player <b>left-clicks</b> while holding this slot. Empty by default, so a button without an override simply does nothing on left-click. Useful for a second action on the same button, for example right-click to cycle forwards and left-click to cycle backwards.</td></tr><tr><td><code>void onChat(AsyncPlayerChatEvent event)</code></td><td>The player sends a chat message while this slot is selected and chat listening is on. <code>ChatMenuItem</code> implements this for you.</td></tr><tr><td><code>void onHover(PlayerItemHeldEvent event)</code></td><td>The player scrolls onto this slot.</td></tr><tr><td><code>void onUnhover(PlayerItemHeldEvent event)</code></td><td>The player scrolls off this slot.</td></tr></tbody></table></div>\n<p>The menu is rebuilt after every select, click and chat input, so a button&#x27;s appearance updates itself without any extra work from you.</p>\n<blockquote><p>_<b>Caution:</b> there is a matching set of <code>addSelectAction</code> / <code>addClickAction</code> / <code>addChatAction</code> / <code>addHoverAction</code> methods for attaching extra callbacks without subclassing. Avoid <code>addHoverAction</code>: the unhover dispatch runs the <b>hover</b> action list, so an action added this way also fires when the player scrolls away. The overridable <code>onUnhover(...)</code> hook above is unaffected and works correctly._</p></blockquote>\n<h4>ChatMenuItem</h4>\n<p>Chat menu items add an extra option for accepting chat input. Otherwise, they work the same as a normal menu button.</p>\n<pre class=\"cb\" data-lang=\"java\"><code>        menu.addMenuItem(new ChatMenuItem() {\n\n            @Override\n            public void buildButton() {\n                button = new MenuButton(Material.MAP);\n                button.setDisplayName(&quot;&amp;d&amp;lEdit Message&quot;);\n            }\n\n            @Override\n            public void onSelect(Player player) {\n                player.sendMessage(Util.colorize(debugPrefix + &quot;&amp;eWhat should the message say?&quot;));\n                player.sendMessage(Util.colorize(debugPrefix + &quot;&amp;eCurrent message: &amp;6&quot; + message));\n            }\n\n            // What to do when the player sends a chat message after selecting this button.\n            @Override\n            public void onInput(Player player, String message) {\n                FunctionMessage.this.message = message;\n\n                player.sendMessage(Util.colorize(debugPrefix + &quot;&amp;aSet message to &#x27;&amp;6&quot; + message + &quot;&amp;a&#x27;&quot;));\n            }\n        });</code></pre>\n<p>Note the two different <code>onSelect</code> signatures. <code>ChatMenuItem</code> implements <code>onSelect(PlayerEvent)</code> itself (that is where it turns chat listening on) and gives you a simpler <code>onSelect(Player)</code> to override instead. Use <code>onSelect(Player)</code> to prompt the player for what you want, and <code>onInput(Player, String)</code> to consume their answer.</p>\n<p><code>onInput</code> runs back on the main thread, and the player&#x27;s menu is redrawn straight afterwards, so you can set fields directly without scheduling anything yourself. The message you receive has already had its colour codes parsed.</p>\n<p>If a select should <i>not</i> start listening for chat (say the button is currently disabled), call <code>setCancelled(true)</code> from inside <code>onSelect(Player)</code>. The flag is reset at the start of every select.</p>\n<h4>ToggleMenuItem</h4>\n<p>For an on/off option, <code>ToggleMenuItem</code> saves you the redraw. It gives you the same simplified <code>onSelect(Player)</code>, and refreshes the player&#x27;s hotbar for you once your code returns, so the button immediately shows its new state.</p>\n<pre class=\"cb\" data-lang=\"java\"><code>        menu.addMenuItem(new ToggleMenuItem() {\n\n            @Override\n            public void buildButton() {\n                button = new MenuButton(Material.REDSTONE_TORCH);\n                button.setDisplayName(&quot;&amp;d&amp;lCase Sensitive&quot;);\n                button.setEnchanted(caseSensitive); // the glow shows the current state\n            }\n\n            @Override\n            public void onSelect(Player player) {\n                caseSensitive = !caseSensitive;\n            }\n        });</code></pre>\n<p>Pairing <code>setEnchanted(...)</code> in <code>buildButton()</code> with the flip in <code>onSelect(...)</code> is the standard pattern, and is what every built-in toggle does.</p>\n<p><b>Click to see a full example</b></p>\n<pre class=\"cb\" data-lang=\"java\"><code>    @Override\n    public void buildHotbarMenu() {\n        // First, we add our example menu item.\n        menu.addMenuItem(new MenuItem() {\n            @Override\n            public void buildButton() {\n                button = new MenuButton(Material.KNOWLEDGE_BOOK);\n                button.setDisplayName(&quot;&amp;d&amp;lExample Option&quot;);\n            }\n\n            @Override\n            public void onSelect(PlayerEvent event) {\n                Player player = event.getPlayer();\n\n                player.sendMessage(&quot;Player has selected this button!&quot;);\n            }\n        });\n\n        // Then, we add our chat menu item. These will appear in the hotbar in the order we add them.\n        menu.addMenuItem(new ChatMenuItem() {\n            @Override\n            public void buildButton() {\n                button = new MenuButton(Material.MAP);\n                button.setDisplayName(&quot;&amp;d&amp;lEdit Message&quot;);\n            }\n\n            @Override\n            public void onSelect(Player player) {\n                player.sendMessage(Util.colorize(debugPrefix + &quot;&amp;eWhat should the message say?&quot;));\n                player.sendMessage(Util.colorize(debugPrefix + &quot;&amp;eCurrent message: &amp;6&quot; + message));\n            }\n\n            @Override\n            public void onInput(Player player, String message) {\n                FunctionMessage.this.message = message;\n\n                player.sendMessage(Util.colorize(debugPrefix + &quot;&amp;aSet message to &#x27;&amp;6&quot; + message + &quot;&amp;a&#x27;&quot;));\n            }\n        });\n    }</code></pre>\n<h4>Hiding a menu item</h4>\n<p>A menu item can remove itself from the bar. Leave <code>button</code> as <code>null</code> in <code>buildButton()</code> and the slot is drawn empty instead:</p>\n<pre class=\"cb\" data-lang=\"java\"><code>        menu.addMenuItem(new ToggleMenuItem() {\n\n            @Override\n            public void buildButton() {\n                // Only offer this option in a procedural dungeon.\n                if (instance.as(InstanceEditableProcedural.class) == null) {\n                    button = null;\n                    return;\n                }\n\n                button = new MenuButton(Material.STRUCTURE_BLOCK);\n                button.setDisplayName(&quot;&amp;d&amp;lLimit To Room&quot;);\n            }\n\n            @Override\n            public void onSelect(Player player) {\n                // ...\n            }\n        });</code></pre>\n<p>Because <code>buildButton()</code> runs on every redraw, an item can appear and disappear as the element&#x27;s state changes. The slot is still reserved either way, so a hidden item still counts against the nine.</p>\n<h4>HotbarMenu reference</h4>\n<p>You will mostly use <code>addMenuItem</code>, but the rest of the surface is small:</p>\n<div class=\"tw\"><table><thead><tr><th>Member</th><th>What it does</th></tr></thead><tbody><tr><td><code>static HotbarMenu create()</code></td><td>Build a menu. Always use this rather than the constructor.</td></tr><tr><td><code>void addMenuItem(MenuItem item)</code></td><td>Append to the next free slot.</td></tr><tr><td><code>void addMenuItem(int slot, MenuItem item)</code></td><td>Place at an explicit slot (0-8).</td></tr><tr><td><code>void removeMenuItem(int slot)</code></td><td>Drop the item at that slot.</td></tr><tr><td><code>HashMap&lt;Integer, MenuItem&gt; getMenuItems()</code></td><td>The items, keyed by slot.</td></tr><tr><td><code>void buildMenu()</code></td><td>Redraw every button. Called for you after a select, click or chat input.</td></tr><tr><td><code>void updateMenu(MythicPlayer aPlayer)</code></td><td>Redraw and push the result to that player&#x27;s hotbar.</td></tr><tr><td><code>int getSelected()</code></td><td>The slot the player last acted on.</td></tr></tbody></table></div>\n<p>While it may look complicated, this system allows for an enormous amount of flexibility and power when creating menus and customizable options. If you&#x27;re really clever, you can even create nested hotbar menus and hide-able menu items, such as what&#x27;s seen in the rewards functions! Play around and get creative!</p>\n<p><a href=\"#\" class=\"wl\" data-p=\"custom-functions\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Custom-Functions\">**Next up:** Custom Functions --&gt;</a></p>"},"dungeons:methods":{"h":"<p>The Mythic Dungeons developer API is exposed through the <b><code>MythicDungeonsService</code></b> interface. See <a href=\"#\" class=\"wl\" data-p=\"introduction-to-api\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Introduction-to-API\">Introduction to API</a> for how to install the dependency and grab the service.</p>\n<blockquote><p>_Service FQN: <code>net.playavalon.mythicdungeons.api.MythicDungeonsService</code>_</p></blockquote>\n<hr>\n<h3>Dungeon Methods</h3>\n<h5>isPlayerInDungeon</h5>\n<pre class=\"cb\" data-lang=\"java\"><code>boolean isPlayerInDungeon(Player player)</code></pre>\n<p>Returns <code>true</code> if the player is currently inside any dungeon instance (play or edit mode). Returns <code>false</code> if the player is offline or in the regular server world.</p>\n<h5>getDungeonInstance (by player)</h5>\n<pre class=\"cb\" data-lang=\"java\"><code>AbstractInstance getDungeonInstance(Player player)</code></pre>\n<p>Returns the instance the player is currently in, or <code>null</code> if they aren&#x27;t in one.</p>\n<p>Unlike <code>isPlayerInDungeon</code>, this getter does <b>not</b> self-heal a stale reference. It hands back whatever instance is recorded on the player&#x27;s <code>MythicPlayer</code>, even if that instance no longer lists them. If you need certainty, check <code>instance.getPlayers().contains(mythicPlayer)</code> yourself, or call <code>isPlayerInDungeon</code> first, which validates the reference and clears it when it is stale.</p>\n<h5>getDungeonInstance (by world name)</h5>\n<pre class=\"cb\" data-lang=\"java\"><code>AbstractInstance getDungeonInstance(String worldName)</code></pre>\n<p>Returns the first active instance whose world is named <code>worldName</code>, or <code>null</code> if none matches.</p>\n<p>Two caveats:</p>\n<ul><li>In <b>One-World mode</b> many instances share a single Bukkit world, so the world name does not identify an instance. The match is taken from a <code>ConcurrentHashMap</code> whose iteration order is unspecified, meaning you can get a different instance from one call to the next.</li><li>To resolve a <code>Location</code> to an instance, iterate the active instances and use <code>AbstractInstance.isLocationInInstance(Location)</code> instead. That is bounds-aware and correct in every dungeon mode.</li></ul>\n<h5>initiateDungeonForPlayer</h5>\n<pre class=\"cb\" data-lang=\"java\"><code>boolean initiateDungeonForPlayer(Player player, String dungeonName)</code></pre>\n<p>Queues the player (and their party, if any) for the dungeon identified by <code>dungeonName</code>.</p>\n<blockquote><p>_<b>Warning:</b> this method returns <code>false</code> on <b>every</b> path, including success. It hands off to the internal queue and then returns a hard-coded <code>false</code>, discarding the queue call&#x27;s own result. Do not branch on it. The failure paths (dungeon not found, player already in a different dungeon, missing the <code>dungeons.play</code> permission) message the player themselves before returning. If you need a result you can act on, use <code>forceInitiateDungeonForPlayer</code>._</p></blockquote>\n<p>Re-calling it because the first call &quot;failed&quot; makes things worse: the player is already queued, so the second call rejects them with the <code>commands.play.already-in-queue</code> message.</p>\n<h5>forceInitiateDungeonForPlayer _(v2.0.1+)_</h5>\n<pre class=\"cb\" data-lang=\"java\"><code>boolean forceInitiateDungeonForPlayer(Player player, String dungeonName, boolean bypassReqs, boolean withParty)</code></pre>\n<p>Force-initiate a dungeon for a player, skipping the queue and the ready-check.</p>\n<ul><li><code>bypassReqs</code>: if <code>true</code>, party-size / permission / key / cost checks are all skipped. If <code>false</code>, requirements are still enforced (and cost/keys are consumed); only the queue/ready-check is bypassed.</li><li><code>withParty</code>: if <code>true</code> the player&#x27;s party comes along; if <code>false</code> the player enters solo. There is <b>no leader check</b> on this path. The flag is applied by marking the player for solo entry, and the party is then gathered for anyone who is not marked solo, leader or not. (The leader-only restriction, <code>LeaderOnlyQueue</code>, only applies to the normal queue path.)</li></ul>\n<p>Returns <code>true</code> once the instance load has been scheduled. This is not a completion signal: the load itself continues asynchronously, so a <code>true</code> means &quot;accepted&quot;, not &quot;the player is standing in the dungeon&quot;. The <code>false</code> cases are the dungeon not being found, the player being busy (already in a dungeon, already awaiting one, or already queued), requirements not being met when <code>bypassReqs=false</code>, and the dungeon currently being saved.</p>\n<p>Note that this path does <b>not</b> consult <code>hasAvailableInstances()</code>. The instance caps are enforced on the normal queue path, not here, which is what makes this a force-entry.</p>\n<h5>getAllDungeons</h5>\n<pre class=\"cb\" data-lang=\"java\"><code>Collection&lt;AbstractDungeon&gt; getAllDungeons()</code></pre>\n<p>Returns every registered dungeon on the server.</p>\n<hr>\n<h3>Loot Table Methods</h3>\n<h5>createLootTable</h5>\n<pre class=\"cb\" data-lang=\"java\"><code>LootTable createLootTable(String namespace, List&lt;ItemStack&gt; items, int weight, int minItems, int maxItems)</code></pre>\n<p>Builds a new in-memory <code>LootTable</code> with one entry per item, all sharing the given <code>weight</code> and the <code>minItems</code>/<code>maxItems</code> amount range. <code>null</code> entries in the list are skipped. The returned table is <b>not</b> registered yet.</p>\n<h5>registerLootTable</h5>\n<pre class=\"cb\" data-lang=\"java\"><code>boolean registerLootTable(String id, LootTable table)</code></pre>\n<p>Registers a table under <code>id</code> and persists it to <code>loottables.yml</code>. Returns <code>false</code> if <code>id</code> or <code>table</code> is <code>null</code>, or if that id is already taken; in those cases the call is a no-op. The registry key is independent of the table&#x27;s own namespace.</p>\n<blockquote><p>_<code>LootTable</code> lives in <code>net.playavalon.mythicdungeons.dungeons.rewards</code>, which is outside the two package trees shipped in the published API jar. See <a href=\"#\" class=\"wl\" data-p=\"introduction-to-api\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Introduction-to-API\">Introduction to API</a>._</p></blockquote>\n<hr>\n<h3>Party Methods</h3>\n<blockquote><p>_<b>Read this first.</b> Nine of the eleven party methods below are gated on Mythic Dungeons&#x27; own party system. If <code>General.PartyPlugin</code> in <code>config.yml</code> is anything other than <code>Default</code> or <code>DungeonParties</code>, or parties are disabled entirely, they return <code>false</code> (or <code>null</code> for <code>getParty</code>) without doing anything, and the console logs <code>Cannot process API call. Party system is not enabled or the internal party system is not selected.</code> once per call. The two <code>isPartyQueuedForDungeon</code> overloads are the only ungated ones. If the server runs a third-party party plugin, read the party through <code>MythicPlayer.getDungeonParty()</code>, which returns the <code>IDungeonParty</code> wrapper for whatever plugin is in use._</p></blockquote>\n<h5>createParty</h5>\n<pre class=\"cb\" data-lang=\"java\"><code>boolean createParty(Player target)</code></pre>\n<p>Creates a new party with <code>target</code> as the leader. Returns <code>false</code> if the player is already in a party.</p>\n<h5>removeFromParty</h5>\n<pre class=\"cb\" data-lang=\"java\"><code>boolean removeFromParty(Player target)</code></pre>\n<p>Removes the player from whatever party they&#x27;re in. Returns <code>false</code> if they aren&#x27;t in one.</p>\n<h5>disbandParty</h5>\n<pre class=\"cb\" data-lang=\"java\"><code>boolean disbandParty(MythicParty party)\nboolean disbandParty(Player target)</code></pre>\n<p>Disbands a party, either by passing the party object directly or by passing one of its members. Removes every member.</p>\n<p>The <code>Player</code> overload returns <code>false</code> when that player has no party. The <code>MythicParty</code> overload has no such guard and will throw a <code>NullPointerException</code> if you pass <code>null</code>, so check before calling it.</p>\n<h5>inviteToParty</h5>\n<pre class=\"cb\" data-lang=\"java\"><code>boolean inviteToParty(Player source, Player target)</code></pre>\n<p>Sends a party invite from <code>source</code>&#x27;s party to <code>target</code>. The target then needs to call <code>acceptPartyInvite</code> (or decline). Returns <code>false</code> if <code>source</code> has no party or <code>target</code> is already in one.</p>\n<h5>acceptPartyInvite / declinePartyInvite</h5>\n<pre class=\"cb\" data-lang=\"java\"><code>boolean acceptPartyInvite(Player target)\nboolean declinePartyInvite(Player target)</code></pre>\n<p>Resolves a pending invite for <code>target</code>. Both return <code>false</code> if the player is already in a party or has no invite pending.</p>\n<p><code>acceptPartyInvite</code> returns <code>true</code> on success. <code>declinePartyInvite</code> does <b>not</b>: it clears the invite and then returns <code>false</code> regardless, so callers cannot tell &quot;declined&quot; from &quot;there was nothing to decline&quot;. Treat its return value as meaningless.</p>\n<h5>setPartyLeader</h5>\n<pre class=\"cb\" data-lang=\"java\"><code>boolean setPartyLeader(Player player)</code></pre>\n<p>Promotes <code>player</code> to be the leader of their current party.</p>\n<h5>getParty</h5>\n<pre class=\"cb\" data-lang=\"java\"><code>MythicParty getParty(Player player)</code></pre>\n<p>Returns the party the player is in, or <code>null</code> if none.</p>\n<h5>isPartyQueuedForDungeon</h5>\n<pre class=\"cb\" data-lang=\"java\"><code>@Nullable String isPartyQueuedForDungeon(Player player)\n@Nullable String isPartyQueuedForDungeon(IDungeonParty party)</code></pre>\n<p>Returns the dungeon&#x27;s <b>world name</b> (the folder name under <code>plugins/MythicDungeons/maps</code>, the same string <code>AbstractDungeon.getWorldName()</code> returns), or <code>null</code> if not queued. It is not the coloured display name.</p>\n<p>Neither overload is gated on the internal party system. Both actually look up a single player&#x27;s queue entry rather than a party-wide one: the <code>Player</code> overload uses that player&#x27;s own entry, and the <code>IDungeonParty</code> overload uses the party leader&#x27;s.</p>\n<blockquote><p>_<b>Caution:</b> the <code>IDungeonParty</code> overload throws on a disbanded party. Its <code>party.getLeader() == null</code> guard cannot fire for a <code>MythicParty</code>, because <code>getLeader()</code> dereferences the leader field that <code>disband()</code> has already nulled. It can also throw further down if the leader is not currently tracked. Check the party is live before calling it._</p></blockquote>\n<hr>\n<h3>Reference: types you&#x27;ll encounter</h3>\n<p>These types appear all over the API. The lists below show the most useful getters on each; the source is the canonical reference for the full list.</p>\n<h4><code>AbstractDungeon</code></h4>\n<p>The dungeon definition (config + map). All play and edit instances of this dungeon share the same <code>AbstractDungeon</code>.</p>\n<ul><li><code>String getWorldName()</code>: internal name / world folder.</li><li><code>String getDisplayName()</code>: colored display name from <code>config.yml</code>.</li><li><code>Map&lt;String, DungeonDifficulty&gt; getDifficultyLevels()</code>: every configured difficulty.</li><li><code>List&lt;AbstractInstance&gt; getInstances()</code>: every currently running instance of this dungeon.</li><li><code>boolean hasAvailableInstances()</code>: <code>true</code> if at least one instance slot is free. <code>General.MaxInstances: 0</code> in the <b>dungeon&#x27;s own</b> config means unlimited, so this returns <code>true</code>. Do not confuse that key with the separate server-wide <code>General.MaxInstances</code> in <code>config.yml</code> (default <code>10</code>), which caps how many instances may exist across all dungeons at once.</li><li><code>boolean hasAvailableInstances(String difficulty)</code>: difficulty-aware variant. The base implementation ignores the argument and defers to the no-arg form; continuous dungeons override it to only count instances of the requested difficulty.</li><li><code>Date getNextUnlockTime()</code>: when the dungeon&#x27;s <b>access</b> cooldown next resets, driven by <code>General.AccessCooldown.*</code>. Returns <code>new Date()</code> (now) when the cooldown is disabled, so it is never <code>null</code>. Honours <code>CooldownType</code> <code>TIMER</code> / <code>DAILY</code> / <code>WEEKLY</code> / <code>MONTHLY</code> plus <code>CooldownTime</code> and <code>ResetDay</code>.</li><li><code>Date getNextLootTime()</code>: the same thing for the <b>loot</b> cooldown, driven by <code>General.LootCooldown.*</code>. Also returns <code>new Date()</code> when disabled.</li><li><code>&lt;T extends AbstractDungeon&gt; T as(Class&lt;T&gt; clazz)</code>: casts this dungeon to a concrete dungeon type, or <code>null</code> if it is not that type.</li></ul>\n<blockquote><p>_v2.0.1: see also <code>getEffectiveTimeLimit(DungeonDifficulty)</code>, <code>getEffectivePlayerLives(DungeonDifficulty)</code>, <code>getEffectiveMaxPlayers(DungeonDifficulty)</code>, <code>getEffectiveKeepInventoryOnEnter(DungeonDifficulty)</code>, and <code>getEffectiveInstantRespawnPlayers(DungeonDifficulty)</code> for per-difficulty overrides. Each has a second overload taking an extra <code>InstancePlayable</code>, which is what resolves dynamic expressions in the underlying config value; the one-argument forms pass <code>null</code> and so only see the static value. The keys behind them are <code>General.TimeLimit</code>, <code>General.PlayerLives</code>, <code>General.MaxPlayers</code>, <code>General.KeepInventoryOnEnter</code> (default <code>true</code>) and <code>General.InstantRespawnPlayers</code> (default <code>false</code>). See <a href=\"#\" class=\"wl\" data-p=\"dungeon-config\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Dungeon-Config\">Dungeon Config</a>._</p></blockquote>\n<h4><code>AbstractInstance</code></h4>\n<p>A single live instance of a dungeon. Comes in two main flavors: <b><code>InstancePlayable</code></b> (subclasses include <code>InstanceClassic</code>, <code>InstanceProcedural</code>, <code>InstanceContinuous</code>) and <b><code>InstanceEditable</code></b>.</p>\n<ul><li><code>AbstractDungeon getDungeon()</code>: the parent dungeon definition.</li><li><code>World getInstanceWorld()</code>: the Bukkit world that hosts this instance.</li><li><code>UUID getUuid()</code>: unique identifier for this instance.</li><li><code>List&lt;MythicPlayer&gt; getPlayers()</code>: everyone currently in the instance.</li><li><code>Map&lt;Location, DungeonFunction&gt; getFunctions()</code>: placed functions, keyed by location.</li><li><code>DungeonDifficulty getCurrentDifficulty()</code>: the difficulty applied; <code>null</code> in edit mode. _v2.0.1_</li><li><code>boolean isLoaded()</code> / <code>boolean isDisposing()</code>: load and teardown state.</li></ul>\n<p><b>Navigating the type hierarchy.</b> Rather than casting by hand:</p>\n<ul><li><code>boolean isPlayInstance()</code> / <code>boolean isEditInstance()</code>: which flavour this is.</li><li><code>@Nullable InstancePlayable asPlayInstance()</code> / <code>@Nullable InstanceEditable asEditInstance()</code>: the cast, or <code>null</code>.</li><li><code>@Nullable &lt;T extends AbstractInstance&gt; T as(Class&lt;T&gt; clazz)</code>: cast to a specific subclass such as <code>InstanceProcedural.class</code>, or <code>null</code> if it is not that type.</li></ul>\n<p><b>Locating players and positions.</b></p>\n<ul><li><code>boolean isPlayerInInstance(Player player)</code>: <code>true</code> if the player is tracked by this instance <b>or</b> is standing inside its bounds.</li><li><code>boolean isLocationInInstance(Location loc)</code>: bounds check. In an edit instance (no bounds) it falls back to comparing worlds. This is the correct way to resolve a <code>Location</code> in One-World mode, where the world name alone is ambiguous.</li></ul>\n<h4><code>InstancePlayable</code></h4>\n<p>Subclass of <code>AbstractInstance</code> for play instances (not edit mode).</p>\n<ul><li><code>DungeonDifficulty getDifficulty()</code>: chosen difficulty for this play.</li><li><code>int getTimeElapsed()</code>: seconds since the dungeon started.</li><li><code>int getTimeLeft()</code>: seconds remaining (<code>0</code> = unlimited).</li><li><code>int modifyTimeLeft(int deltaSeconds, boolean clampToZero)</code>: adjust the remaining time by a signed number of seconds, returning the new value. A no-op (returns <code>timeLeft</code> unchanged) when the dungeon has no time limit or has not started. With <code>clampToZero</code> the result floors at <code>1</code>, so the adjustment itself cannot end the run.</li><li><code>String getStatus()</code> / <code>void setStatus(String)</code>: a free-form status string set by <b>Dungeon Status</b> functions. The setter fires <code>DungeonStatusChangeEvent</code> when the value actually changes, and is safe to call off the main thread (the event dispatch is deferred).</li><li><code>void setDifficulty(DungeonDifficulty)</code>: fires <code>DungeonDifficultyChangeEvent</code> on a real change, with the same async safety.</li><li><code>List&lt;MythicPlayer&gt; getLivingPlayers()</code>: players still alive (non-spectating).</li><li><code>Map&lt;UUID, Integer&gt; getPlayerLives()</code>: lives remaining per player.</li><li><code>int getParticipants()</code>: how many players the run started with.</li><li><code>Location getLobbyLoc()</code>: the lobby location, when the dungeon uses one.</li><li><code>boolean isLivesEnabled()</code>: <code>true</code> when the effective <code>PlayerLives</code> is <b>not zero</b>. Negative values therefore count as enabled; only <code>0</code> disables lives.</li><li><code>boolean isStarted()</code>: <code>true</code> once the run has started. With a lobby that is after the ready-check; without one it happens a tick after the instance loads. Procedural dungeons always take the no-lobby path. _(There is no <code>hasStarted()</code>; the accessor is <code>isStarted()</code>.)_</li><li><code>boolean isDungeonFinished()</code>: <code>true</code> after a Finish Dungeon function has been used.</li><li><code>Set&lt;Entity&gt; getEntities()</code>: entities tracked for this run. Backed by a weak set, so it will not by itself keep the world alive. See <a href=\"#\" class=\"wl\" data-p=\"memory-leaks\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Memory-Leaks\">Avoiding Memory Leaks</a>.</li><li><code>Map&lt;UUID, BukkitRunnable&gt; getOfflineTrackers()</code>: the per-player grace-period tasks that run while a member is disconnected.</li><li><code>IVariableHandler&lt;?&gt; getInstanceVariables()</code> / <code>PlayerVariables getPlayerVariables()</code> / <code>BossBarHandler getBossBarHandler()</code>: the dungeon variable and boss bar state for this run.</li></ul>\n<h4><code>MythicPlayer</code></h4>\n<p>Wraps a Bukkit <code>Player</code> with dungeon-specific state.</p>\n<ul><li><code>Player getPlayer()</code>: the underlying Bukkit player.</li><li><code>AbstractInstance getInstance()</code>: the dungeon instance the player is currently in, or <code>null</code>.</li><li><code>IDungeonParty getDungeonParty()</code>: the player&#x27;s party, or <code>null</code>. This is the party-plugin-agnostic view; <code>getMythicParty()</code> returns the built-in <code>MythicParty</code> and is <code>null</code> when a third-party party plugin is in use.</li><li><code>boolean isDead()</code>: <code>true</code> between dying and respawning. It is set on <b>every</b> death, before the lives check, and cleared again on respawn and whenever the player is added to an instance. It is <b>not</b> an &quot;out of lives&quot; flag. For that, test <code>!instance.getLivingPlayers().contains(mythicPlayer)</code>.</li><li><code>boolean isSpectating()</code>: <code>true</code> if the player is in spectator mode in this dungeon.</li><li><code>Hotbar getCurrentHotbar()</code> / <code>getPreviousHotbar()</code>: the editor hotbar menu stack. See <a href=\"#\" class=\"wl\" data-p=\"gui-menus\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/GUI-Menus\">GUI Menus</a>.</li><li><code>List&lt;ItemStack&gt; getRewardsInv()</code>: the player&#x27;s pending rewards. It returns the <b>merge</b> of two sources: the rewards persisted in the player&#x27;s own data file, plus anything the current instance is holding for them. The persisted bag is keyed per MythicRPG profile when MythicRPG is installed, so different characters have different bags. Reward functions route items here when <code>GiveLootAfterCompletion</code> is enabled; the Random Reward function additionally routes here when <code>KeepInventoryOnEnter</code> is <code>false</code>.</li><li><code>Location getDungeonSavePoint(String dungeon)</code>: the player&#x27;s last save point in a particular dungeon, if any.</li><li><code>Location getSavedPosition()</code> / <code>GameMode getSavedGameMode()</code>: where and how the player was before entering, restored on exit.</li><li><code>Location getTargetLocation()</code> / <code>void setTargetLocation(Location)</code>: scratch location used by the in-game element editor.</li></ul>\n<p>To obtain a <code>MythicPlayer</code> from a Bukkit <code>Player</code>:</p>\n<pre class=\"cb\" data-lang=\"java\"><code>MythicPlayer mp = MythicDungeons.inst().getMythicPlayer(player);</code></pre>\n<p>The <code>Player</code> overload creates and registers the wrapper on demand, so it does not return <code>null</code> for an online player. The <code>getMythicPlayer(UUID)</code> overload is a plain registry lookup and returns <code>null</code> for anyone who is not currently tracked, which includes offline players. Null-check that one.</p>\n<h4><code>DungeonDifficulty</code></h4>\n<p>A configured difficulty level. See <a href=\"#\" class=\"wl\" data-p=\"dungeon-config\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Dungeon-Config\">Dungeon Config</a> for the YAML side.</p>\n<ul><li><code>String getNamespace()</code>: config key (e.g. <code>&quot;HARD&quot;</code>, <code>&quot;NIGHTMARE&quot;</code>).</li><li><code>String getDisplay()</code>: colored display name.</li><li><code>double getMobHealthScale()</code> / <code>getMobDamageScale()</code> / <code>getMobSpawnScale()</code>: mob scaling multipliers. Default <code>1</code>.</li><li><code>int getMythicMobLevel()</code>: bonus levels applied to Mythic Mob spawns. Default <code>0</code>.</li><li><code>RangedNumber getBonusLoot()</code>: extra rolls on loot tables.</li><li><code>double[] getBonusLootChances()</code>: the per-roll chances for those extra rolls. Empty by default.</li><li><code>ItemStack getIcon()</code>: menu item representation.</li><li><code>Map&lt;String, Object&gt; getOverrides()</code> / <code>Object getOverride(String key)</code>: per-difficulty config overrides. _v2.0.1_</li><li><code>Map&lt;String, MobOverride&gt; getMobOverrides()</code> / <code>MobOverride getMobOverride(String mobId)</code>: per-mob overrides declared under <code>MobOverrides</code> in the difficulty section.</li></ul>\n<p><b>Static values versus dynamic expressions.</b> The no-arg scale getters return the value parsed once out of the config. Each also has an overload taking the running <code>InstancePlayable</code>:</p>\n<pre class=\"cb\" data-lang=\"java\"><code>double health = difficulty.getMobHealthScale(instance);</code></pre>\n<p>Those overloads resolve dynamic expressions (for example a value written in terms of the current player count) against that instance. If a difficulty uses dynamic values, the no-arg getters give you the fallback, not the live number. The overloads exist for <code>getMobHealthScale</code>, <code>getMobSpawnScale</code>, <code>getMobDamageScale</code>, <code>getMythicMobLevel</code> and <code>getBonusLoot</code>.</p>\n<blockquote><p>_<b>Caution:</b> <code>DungeonDifficulty</code> has a second, single-<code>String</code> constructor intended for API use. Objects built with it have no icon, so <code>getIcon()</code> throws a <code>NullPointerException</code> on them. Only the <code>ConfigurationSection</code> constructor, which Mythic Dungeons uses when reading a dungeon config, populates the icon._</p></blockquote>\n<h4><code>MythicParty</code> / <code>IDungeonParty</code></h4>\n<p>A dungeon party. <code>MythicParty</code> is the built-in implementation of <code>IDungeonParty</code>; third-party party plugins implement the interface directly, and <code>MythicPlayer.getDungeonParty()</code> gives you whichever is in use.</p>\n<p><code>IDungeonParty</code> (the portable view, works with any party plugin):</p>\n<ul><li><code>List&lt;Player&gt; getPlayers()</code>: all members, as Bukkit players.</li><li><code>@NotNull OfflinePlayer getLeader()</code>: the party leader. Note the type: it is an <code>OfflinePlayer</code> on the interface, <b>not</b> a <code>MythicPlayer</code>.</li><li><code>void addPlayer(Player)</code> / <code>void removePlayer(Player)</code>: membership changes.</li><li><code>boolean hasPlayer(Player)</code>: membership test (default method).</li><li><code>void partyMessage(String msg)</code>: send a colourised message to every member (default method).</li><li><code>Location getPartySavePoint(String dungeon)</code>: the leader&#x27;s save point for that dungeon (default method).</li><li><code>void initDungeonParty(Plugin plugin)</code> / <code>void initDungeonParty(String... names)</code>: binds this party onto its members&#x27; <code>MythicPlayer</code>s, but only when the server&#x27;s configured <code>General.PartyPlugin</code> matches the plugin name you pass. This is the hook a party-plugin integration calls. See <a href=\"#\" class=\"wl\" data-p=\"api-parties\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/API-Parties\">Adding Party Support</a>.</li><li><code>void setAwaitingDungeon(boolean)</code>: flags every member as waiting on a dungeon (default method).</li></ul>\n<p><code>MythicParty</code> adds, on top of those:</p>\n<ul><li><code>Player getLeader()</code>: narrows the return type to an online <code>Player</code>. It dereferences the leader field directly, so it throws after <code>disband()</code>.</li><li><code>MythicPlayer getMythicLeader()</code>: the leader as a <code>MythicPlayer</code>.</li><li><code>List&lt;MythicPlayer&gt; getMythicPlayers()</code>: all members as <code>MythicPlayer</code>s. <b>This is the member list</b>; there is no <code>getMembers()</code>.</li><li><code>MythicPlayer getPlayer(String name)</code>: look up a member by name.</li><li><code>void removePlayer(Player player, boolean withAlert)</code> and <code>removePlayer(Player, boolean withAlert, boolean kickFromDungeon)</code>: removal with control over the party message and whether the player is pulled out of the dungeon.</li><li><code>void kickPlayer(Player)</code> / <code>void kickPlayer(String name)</code>: kick with the kick messages and <code>MythicPartyKickEvent</code>.</li><li><code>void disband()</code>: disbands the party and clears every member.</li><li><code>void setMythicLeader(Player)</code> / <code>void changeLeader(Player)</code>: promote a leader. <code>changeLeader</code> refuses players who are not in the party; <code>setMythicLeader</code> does not check.</li><li><code>boolean isPlayerOnline(Player)</code> / <code>void setPlayerOnline(Player, boolean)</code>: online tracking. Taking the last member offline disbands the party.</li><li><code>void sendChatMessage(Player, String)</code> / <code>void leaderMessage(String)</code> / <code>void sendPartyInfo(Player)</code>: party chat and info output.</li></ul>\n<p>There is no <code>size()</code> and no <code>isLeader(...)</code> on either type. For a member count use <code>getPlayers().size()</code> (or <code>getMythicPlayers().size()</code>); to test leadership compare against <code>getLeader()</code>.</p>\n<hr>\n<h3>Beyond the service interface</h3>\n<p><code>MythicDungeonsService</code> is the supported surface, but the plugin class exposes more. Everything in this section is reached through <code>MythicDungeons.inst()</code>, which means it is <b>not</b> available when you compile only against the published API jar. See <a href=\"#\" class=\"wl\" data-p=\"introduction-to-api\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Introduction-to-API\">Introduction to API</a>.</p>\n<h4>Registering custom elements</h4>\n<pre class=\"cb\" data-lang=\"java\"><code>&lt;T extends DungeonFunction&gt;  void registerFunction(Class&lt;T&gt; function)\n&lt;T extends DungeonTrigger&gt;   void registerTrigger(Class&lt;T&gt; trigger)\n&lt;T extends TriggerCondition&gt; void registerCondition(Class&lt;T&gt; condition)\n\nvoid registerFunctions(String functionsPackage)\nvoid registerTriggers(String triggersPackage)\nvoid registerConditions(String conditionsPackage)</code></pre>\n<p>The three single-class calls register one element and also call <code>ConfigurationSerialization.registerClass</code> for you, so you never do that yourself. <code>registerTrigger</code> additionally inserts the new trigger into the already-built picker menu, which is what makes post-startup registration work.</p>\n<p>The three package calls scan the named package with Reflections and register every class carrying the matching <code>@DeclaredFunction</code> / <code>@DeclaredTrigger</code> / <code>@DeclaredCondition</code> annotation. This is the normal way to register a whole package of elements from your own plugin. See <a href=\"#\" class=\"wl\" data-p=\"custom-elements\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Custom-Elements\">Getting Started with Elements</a>.</p>\n<h4>Registering other extension types</h4>\n<pre class=\"cb\" data-lang=\"java\"><code>&lt;T extends AbstractDungeon&gt;  void registerDungeonType(Class&lt;T&gt; type, String name, String... aliases)\n&lt;T extends Layout&gt;           void registerLayout(Class&lt;T&gt; type, String name, String... aliases)\n&lt;T extends InstanceListener&gt; void registerInstanceListener(Class&lt;T&gt; type)</code></pre>\n<p><code>registerInstanceListener</code> wires up every <code>@EventHandler</code> method on the listener class so that it is dispatched per active instance, with each instance&#x27;s own listener as the receiver.</p>\n<h4>Version information</h4>\n<pre class=\"cb\" data-lang=\"java\"><code>String getVersion()      // e.g. &quot;2.0.1&quot;\nString getBuildNumber()  // the build suffix, or &quot;????&quot; when there isn&#x27;t one</code></pre>\n<p>Both derive from the version string in <code>plugin.yml</code>, split on <code>-</code>. <code>getVersion()</code> is the part before the first hyphen.</p>\n<h4>World generators</h4>\n<p>Mythic Dungeons registers chunk generators under its own plugin name, so any world creator can ask for them by id:</p>\n<div class=\"tw\"><table><thead><tr><th>Generator id</th><th>Result</th></tr></thead><tbody><tr><td><code>void</code></td><td>an empty world</td></tr><tr><td><code>block.&lt;MATERIAL&gt;</code></td><td>a world filled with that material, for example <code>block.STONE</code>. An unrecognised material falls back to <code>STONE</code>.</td></tr></tbody></table></div>\n<p>The <code>void</code> id and the <code>block</code> prefix are matched case-insensitively, but the material name after the dot must match the Bukkit <code>Material</code> constant exactly, in upper case. Use <code>MythicDungeons.qualifyGeneratorName(id)</code> to turn a bare id into the <code>MythicDungeons:&lt;id&gt;</code> form that Bukkit&#x27;s <code>WorldCreator.generator(String)</code> expects; a bare id is otherwise parsed as a plugin name and silently ignored.</p>\n<hr>\n<h3>Alternative entry point</h3>\n<p>For convenience, the plugin singleton also implements <code>MythicDungeonsService</code>:</p>\n<pre class=\"cb\" data-lang=\"java\"><code>MythicDungeons md = MythicDungeons.inst();\nmd.isPlayerInDungeon(player); // same as the service lookup</code></pre>\n<p>Use this if you&#x27;ve already added a hard <code>depend</code> on MythicDungeons in your <code>plugin.yml</code> <b>and</b> you compile against the full plugin jar. The <code>MythicDungeons</code> class sits in the root package, which is not shipped in the published API artifact, so this snippet does not compile against that artifact. Prefer the <code>ServicesManager</code> lookup if you support soft-dep / optional integration, since the service won&#x27;t be registered when MD is absent.</p>\n<hr>\n<h3>Other API packages</h3>\n<p>These are part of the published API but are not documented method by method here. Read them from the sources or javadoc jar (see <a href=\"#\" class=\"wl\" data-p=\"introduction-to-api\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Introduction-to-API\">Introduction to API</a>).</p>\n<div class=\"tw\"><table><thead><tr><th>Package</th><th>What lives there</th></tr></thead><tbody><tr><td><code>api.annotations</code></td><td><code>@SavedField</code>, <code>@Hidden</code>, <code>@Relocatable</code>, <code>@DeclaredFunction</code>, <code>@DeclaredTrigger</code>, <code>@DeclaredCondition</code>. See <a href=\"#\" class=\"wl\" data-p=\"custom-elements\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Custom-Elements\">Getting Started with Elements</a>.</td></tr><tr><td><code>api.parents.elements</code></td><td><code>DungeonElement</code>, <code>DungeonFunction</code>, <code>DungeonTrigger</code>, <code>TriggerCondition</code>, <code>FunctionCategory</code>.</td></tr><tr><td><code>api.events</code></td><td>Every Bukkit event Mythic Dungeons fires. See <a href=\"#\" class=\"wl\" data-p=\"events\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Events\">Events</a>.</td></tr><tr><td><code>api.blocks</code></td><td><code>MovingBlock</code>, <code>MovingBlockCluster</code>, <code>PushableBlock</code>, <code>PushableBlockCluster</code>.</td></tr><tr><td><code>api.chunkgenerators</code></td><td><code>VoidGenerator</code>, <code>FullBlockGenerator</code>, <code>DungeonChunkGenerator</code>.</td></tr><tr><td><code>api.config</code></td><td><code>AvalonConfiguration</code>, <code>AsyncConfiguration</code>, <code>AvalonSerializable</code>.</td></tr><tr><td><code>api.exceptions</code></td><td><code>DungeonInitException</code>, <code>DungeonDisposalException</code>.</td></tr><tr><td><code>api.queue</code></td><td><code>QueueData</code>, the state of one queued party.</td></tr><tr><td><code>api.generation</code></td><td>The procedural generator: <code>Layout</code> and its subclasses, <code>StructurePiece</code>, and the <code>rooms</code> types (<code>InstanceRoom</code>, <code>RotatedRoom</code>, <code>Connector</code>, <code>ConnectorDoor</code>, and friends).</td></tr><tr><td><code>api.party</code></td><td><code>IDungeonParty</code>. See <a href=\"#\" class=\"wl\" data-p=\"api-parties\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/API-Parties\">Adding Party Support</a>.</td></tr></tbody></table></div>"},"dungeons:parties":{"h":"<p>Party Plugin Compatibility ========</p>\n<p>Mythic Dungeons ships with its own party system, and can also hand party handling over to another plugin. Which one is used is decided by a single option in <code>plugins/MythicDungeons/config.yml</code>:</p>\n<pre class=\"cb\" data-lang=\"yaml\"><code>General:\n  # What plugin to use for the party system. Defaults to DungeonParties, which is provided by Mythic Dungeons.\n  PartyPlugin: Default</code></pre>\n<p>Write the value with exactly the casing shown in the table below. Most of the plugin compares this name without regard to case, but not all of it does: the value is handed to the server unchanged when the plugin itself is looked up, and the developer-facing party API only accepts <code>Default</code> and <code>DungeonParties</code> spelled exactly like that. If the value names a plugin that is not installed, Mythic Dungeons logs <code>ERROR :: Party plugin is set to &#x27;&lt;name&gt;&#x27;, but no such plugin was found!</code> and party support stays off for the whole server.</p>\n<p>The option is only read while the plugin is starting, so a change needs a full server restart.</p>\n<p>Supported Values</p>\n<hr>\n<div class=\"tw\"><table><thead><tr><th><code>PartyPlugin</code></th><th>Party system used</th><th>Notes</th></tr></thead><tbody><tr><td><code>Default</code></td><td>Mythic Dungeons&#x27; built-in system</td><td>The shipped default. Same as <code>DungeonParties</code>.</td></tr><tr><td><code>DungeonParties</code></td><td>Mythic Dungeons&#x27; built-in system</td><td>Alias of <code>Default</code>.</td></tr><tr><td><code>Heroes</code></td><td>Heroes</td><td></td></tr><tr><td><code>Parties</code></td><td>Parties, by AlessioDP</td><td></td></tr><tr><td><code>MythicRPG</code></td><td>MythicRPG&#x27;s own <code>/party</code></td><td></td></tr><tr><td><code>paf</code></td><td>PartyAndFriendsGUI</td><td>The value is <code>paf</code>, <b>not</b> the plugin&#x27;s name.</td></tr></tbody></table></div>\n<p>Any other value is treated as a custom party provider: Mythic Dungeons checks that a plugin with that name is installed, then waits for that plugin to register its parties through the <a href=\"#\" class=\"wl\" data-p=\"api-parties\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/API-Parties\">IDungeonParty API</a>. It will not create parties on its own.</p>\n<p>The Built-in System</p>\n<hr>\n<p>With <code>Default</code> (or <code>DungeonParties</code>), Mythic Dungeons runs everything itself and registers two extra commands, <code>/party</code> (aliased <code>/dparty</code>) and <code>/p</code> for party chat. Those two commands are only registered under this setting.</p>\n<p>This is also the only setting under which the developer-facing party API (<code>createParty</code>, <code>inviteToParty</code>, <code>disbandParty</code>, <code>getParty</code>, and friends) does anything. With any other provider those calls are refused and log <code>Cannot process API call. Party system is not enabled or the internal party system is not selected.</code> That check is case-sensitive, so <code>PartyPlugin: default</code> in lower case still starts the built-in system and its commands, but every one of those API methods is refused.</p>\n<p>What Each Provider Can Do</p>\n<hr>\n<p>Mythic Dungeons only mutates a third-party party where that plugin&#x27;s API supports it.</p>\n<div class=\"tw\"><table><thead><tr><th></th><th><code>Heroes</code></th><th><code>Parties</code></th><th><code>paf</code></th><th><code>MythicRPG</code></th></tr></thead><tbody><tr><td>Creates a party when a lone player starts a dungeon</td><td>Yes</td><td>Yes</td><td>No</td><td>No</td></tr><tr><td>Adds a player to the party (<code>/recruit</code> joins)</td><td>Yes</td><td>Yes</td><td>No</td><td>No</td></tr><tr><td>Removes a player from the party</td><td>Yes</td><td>Yes</td><td>No</td><td>No</td></tr><tr><td>Disbands the party</td><td>Yes</td><td>Yes</td><td>No</td><td>No</td></tr><tr><td>Reads members, leader, and sends party messages</td><td>Yes</td><td>Yes</td><td>Yes</td><td>Yes</td></tr><tr><td>Reacts to a player leaving the party elsewhere</td><td>Yes</td><td>Yes</td><td>Yes</td><td>No</td></tr></tbody></table></div>\n<p>With <code>paf</code> and <code>MythicRPG</code>, parties are read-only: players must form the party with that plugin&#x27;s own commands before running <code>/md play</code>. <code>/recruit</code> will still create a listing and the listing will still fill up, but joining it does not put the joining player into the underlying party, so they are dropped again on the next party check and are not taken into the dungeon.</p>\n<p>Options That Affect Parties</p>\n<hr>\n<p>In <code>config.yml</code> (see <a href=\"#\" class=\"wl\" data-p=\"config\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Config\">Plugin Config</a> for the full list):</p>\n<ul><li><code>General.PartyChat</code> - enables the built-in party chat. Default <code>false</code>. The <code>/p</code> command it uses is only registered when the built-in system is selected.</li><li><code>General.LeaderOnlyQueue</code> - only the party leader may start a dungeon for the party. Default <code>true</code>.</li><li><code>General.ReadyCheckOnCommand</code>, <code>General.ReadyCheckInQueue</code>, <code>General.ReadyCheckTime</code>, <code>General.StartWithoutUnreadyPlayers</code>, <code>General.ReadyCheckRequireLeader</code> - the ready check that runs before a party is sent in.</li><li><code>General.PartyFinder.ListingBroadcastPeriod</code> - how many minutes there are between broadcasts of a <code>/recruit</code> listing. Default <code>5</code>.</li></ul>\n<p>In a dungeon&#x27;s own <code>config.yml</code> (see <a href=\"#\" class=\"wl\" data-p=\"dungeon-config\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Dungeon-Config\">Dungeon Config</a>):</p>\n<ul><li><code>IgnoreParties</code> - when <code>true</code>, only the player who ran the command enters, even if they are in a party. Default <code>false</code>.</li><li><code>LeaderOnlyQueue</code> - per-dungeon override of the global option.</li><li><code>Requirements.MinPartySize</code> (default <code>1</code>) and <code>Requirements.MaxPartySize</code> (default <code>4</code>). A party outside that range is told so and does not enter.</li><li><code>Requirements.MaxPartyDistance</code> - members further than this many blocks from the party leader are left behind. <code>0</code> (the default) disables the check. Members in a different world always count as too far. The distance is measured from where the leader was standing when the dungeon was started, or from the player who started it if the leader is not online.</li><li><code>Requirements.KickDistantMembers</code> - whether a member left behind by <code>MaxPartyDistance</code> is also removed from the party. Default <code>true</code>.</li></ul>\n<hr>\n<p>_Writing your own party plugin? See <a href=\"#\" class=\"wl\" data-p=\"api-parties\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/API-Parties\">Adding Party Support</a>._</p>"},"dungeons:placeholderapi":{"h":"<p>Placeholder API Compatibility ========</p>\n<p>Mythic Dungeons ships a PlaceholderAPI expansion. It registers itself automatically on startup when the PlaceholderAPI plugin is installed, so there is nothing to download from the eCloud.</p>\n<ul><li><b>Identifier:</b> <code>md</code> (every placeholder starts with <code>%md_</code>)</li><li><b>Author:</b> MarcatoSound</li><li><b>Version:</b> the Mythic Dungeons version, with any build suffix stripped</li></ul>\n<p>Placeholders are also resolved inside the Message, Title, Command, and Boss Bar functions, in spectator teleport menu button names, and on both sides of a Placeholder Check condition.</p>\n<p>For placeholders usable inside Mythic Mobs configs instead, see <a href=\"#\" class=\"wl\" data-p=\"mythic-mobs-integration/placeholders\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Mythic-Mobs-Integration/Placeholders\">Mythic Mobs placeholders</a>.</p>\n<p>When a placeholder can&#x27;t resolve</p>\n<hr>\n<ul><li>An identifier the expansion doesn&#x27;t know returns nothing, so PlaceholderAPI leaves the raw <code>%md_...%</code> text visible. A visible placeholder almost always means a typo.</li><li>Most <code>%md_dungeon_...%</code> placeholders return a single space when the player is not currently inside a dungeon.</li><li>Placeholders that read live instance state only work inside a <b>play</b> instance. Inside an edit session they return nothing.</li></ul>\n<p>Instance Placeholders</p>\n<hr>\n<p>These placeholders can be used when a player is in an instance.</p>\n<h4>Dungeon Info Placeholders</h4>\n<ul><li><code>%md_dungeon_name%</code>   The name of the dungeon the player is currently in. This is the dungeon&#x27;s folder name.</li><li><code>%md_dungeon_display_name%</code>   The formal, presentable display name of the dungeon the player is currently in. (The <code>General.DisplayName</code> config value, colour codes already applied.)</li><li><code>%md_dungeon_unlock_time%</code>   When the player can access the dungeon they are currently in again, as a formatted date. Shows &quot;Unlocked&quot; when there is no cooldown.</li></ul>\n<h4>Instance Info Placeholders</h4>\n<ul><li><code>%md_dungeon_id%</code>   The instance ID of the dungeon. With the standard one-world-per-instance system this is the dungeon name plus a number (for example <code>mydungeon_0</code>). With the <a href=\"#\" class=\"wl\" data-p=\"one-world-dungeons\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/One-World-Dungeons\">same-world dungeon system</a>, every instance shares one world, so this returns that shared world&#x27;s name (for example <code>dungeons_normal_0</code>) and is <b>not</b> unique per instance.</li><li><code>%md_dungeon_players%</code>   The number of players in the dungeon instance, including dead players.</li></ul>\n<p>The rest of this section needs a running dungeon. Inside an edit session they all return nothing.</p>\n<ul><li><code>%md_dungeon_players_living%</code>   The number of _living_ players in the dungeon instance.</li><li><code>%md_dungeon_dead%</code>   The number of players in the instance who are no longer alive.</li><li><code>%md_dungeon_spectators%</code>   How many of the players who started the dungeon are no longer counted as active players.</li><li><code>%md_dungeon_max_players%</code>   The maximum party size configured for the dungeon this instance belongs to.</li><li><code>%md_dungeon_environment%</code>   The dungeon&#x27;s dimension: <code>NORMAL</code>, <code>NETHER</code>, or <code>THE_END</code>.</li><li><code>%md_dungeon_lives_left%</code>   The number of lives the player has left. Shows &quot;Infinite&quot; when the dungeon has lives disabled.</li></ul>\n<h4>Timer Placeholders</h4>\n<ul><li><code>%md_dungeon_time_left%</code>   How many hours, minutes and seconds there are left until the dungeon ends automatically. (For example, 01:35:15 for 1 hour, 35 minutes, and 15 seconds remaining.) The hour part rolls over at 24.</li><li><code>%md_dungeon_time_left_seconds%</code>   The same value as a plain number of seconds.</li><li><code>%md_dungeon_time_elapsed%</code>   How many hours, minutes and seconds have passed since the dungeon started. The hour part rolls over at 24.</li><li><code>%md_dungeon_time_elapsed_seconds%</code>   The same value as a plain number of seconds.</li><li><code>%md_dungeon_time_limit%</code>   The dungeon&#x27;s time limit <b>in minutes</b>, including any per-difficulty override. 0 means no time limit.</li><li><code>%md_dungeon_time_limit_formatted%</code>   The same time limit as an hours:minutes:seconds string. Note that it currently formats the limit as if it were seconds, so a 30 minute limit reads <code>00:00:30</code> instead of <code>00:30:00</code>.</li><li><code>%md_dungeon_progress%</code>   How far through the time limit the run is, as a whole number from 0 to 100. Only meaningful when the dungeon has a time limit: with <code>TimeLimit: 0</code> this reads 100 from the first second onward.</li><li><code>%md_dungeon_progress_fraction%</code>   The same value as a decimal from 0.00 to 1.00. This is the value to feed a Boss Bar progress source. As with <code>%md_dungeon_progress%</code>, it only means anything when the dungeon has a time limit.</li></ul>\n<h4>Procedural Dungeon Placeholders</h4>\n<p>These only return a value inside a procedural (generated) dungeon. Everywhere else they are empty, or 0 for the room counts.</p>\n<ul><li><code>%md_dungeon_room_name%</code>   The template name of the room the player is standing in.</li><li><code>%md_dungeon_room_depth%</code>   How many rooms away from the start room the player&#x27;s current room is. The start room is 1.</li><li><code>%md_dungeon_room_rotation%</code>   The rotation of the player&#x27;s current room in degrees: 0, 90, 180, or 270.</li><li><code>%md_dungeon_rooms_total%</code>   How many rooms the generated dungeon has.</li><li><code>%md_dungeon_rooms_cleared%</code>   How many of those rooms currently contain no MythicMobs.</li><li><code>%md_dungeon_rooms_remaining%</code>   How many rooms still contain at least one MythicMob.</li></ul>\n<h4>Mob Placeholders</h4>\n<ul><li><code>%md_dungeon_mobs%</code>   How many MythicMobs are currently alive in the instance.</li><li><code>%md_dungeon_mobs_[mob]%</code>   How many MythicMobs of one specific type are alive in the instance. Replace [mob] with the mob&#x27;s internal name, for example <code>%md_dungeon_mobs_SkeletalKnight%</code>. The name is matched ignoring case, and unlike the [dungeon] and [player] slots below, it may contain underscores.</li></ul>\n<p>The mob placeholders, along with <code>%md_dungeon_rooms_cleared%</code> and <code>%md_dungeon_rooms_remaining%</code>, ask Mythic Mobs for the answer, so they error out on a server that does not have Mythic Mobs installed.</p>\n<h4>Living Player Placeholders</h4>\n<ul><li><code>%md_alive_member_[##]%</code>   The name of the player corresponding with the provided number, counting only the players still alive in the instance. For example, <code>%md_alive_member_1%</code> shows the name of the first living player. Use this one with care: it currently errors instead of returning a value whenever the player reading it is not inside a dungeon, whenever the number is not a whole number, and whenever the number is 0 or higher than the number of living players.</li></ul>\n<p>Dungeon-Specific Placeholders</p>\n<hr>\n<p>These placeholders can be used by specifying a dungeon name. (Replace [dungeon] with the name of the dungeon.) The name is the dungeon&#x27;s folder name and it is case sensitive. Dungeon names containing an underscore cannot be used here.</p>\n<ul><li><code>%md_dungeon_[dungeon]_lives%</code>   How many lives players will start with in this dungeon. Shows &quot;Infinite&quot; when lives are disabled. This reads the dungeon&#x27;s base <code>General.PlayerLives</code> value only: per-difficulty overrides are ignored, and a value written as a placeholder or math expression also reads as &quot;Infinite&quot;.</li><li><code>%md_dungeon_[dungeon]_min_players%</code>   The minimum number of players needed in a party to enter this dungeon. Defaults to 1 when not configured. May be a placeholder or math expression, resolved when the placeholder is requested.</li><li><code>%md_dungeon_[dungeon]_max_players%</code>   The maximum number of players allowed in a party for this dungeon. Defaults to 4 when not configured. May be a placeholder or math expression.</li><li><code>%md_dungeon_[dungeon]_min_class_level%</code>   Reads a <code>Requirements.ClassLevel</code> config value. No dungeon config has that value and nothing else in the plugin writes it, so this always shows 1. The requirement that is actually enforced is <code>Requirements.PlayerLevel</code>.</li><li><code>%md_dungeon_[dungeon]_max_instances%</code>   How many instances of this dungeon may exist at once.</li><li><code>%md_dungeon_[dungeon]_busy_instances%</code>   How many instances of this dungeon exist right now. This counts every allocated instance, including an open edit session and any empty preloaded instances.</li><li><code>%md_dungeon_[dungeon]_unlock_time%</code>   When the player can access this dungeon again as a formatted date. Shows &quot;Unlocked&quot; when there is no cooldown.</li><li><code>%md_dungeon_[dungeon]_unlock_time_seconds%</code>   How long until the player can access this dungeon again in seconds. Shows 0 when the dungeon is already unlocked.</li><li><code>%md_dungeon_[dungeon]_finished%</code>   Meant to show whether the player has completed this dungeon. It reads a completion list that nothing in the plugin writes to, so it currently always shows &quot;false&quot;, even right after a player finishes the dungeon.</li></ul>\n<p>Party Placeholders</p>\n<hr>\n<p>These placeholders can be used when a player is in a party. When the player has no party, the name placeholders show a blank space, <code>%md_party_size%</code> shows 0, and the true/false ones show false. If you use a third-party party plugin, a player with no one else in their party counts as having no party.</p>\n<ul><li><code>%md_party_leader%</code>   The name of the current party&#x27;s leader.</li><li><code>%md_party_size%</code>   The number of players in the current party.</li><li><code>%md_party_in_dungeon%</code>   &quot;true&quot; if any member of the party is currently inside a dungeon, otherwise &quot;false&quot;.</li><li><code>%md_party_queued%</code>   &quot;true&quot; if any member of the party is currently queued for a dungeon, otherwise &quot;false&quot;.</li><li><code>%md_party_member_[##]%</code>   The name of the player in the party corresponding with the provided number. (##) For example, <code>%md_party_member_1%</code> would show the name of the first player in the party. Shows a blank space if the party has fewer members than that.</li><li><code>%md_party_member_[##]_health%</code>   That same player&#x27;s current health, to one decimal place. A player at full vanilla health shows 20.0. For example, <code>%md_party_member_2_health%</code>.</li></ul>\n<p>Recruitment Placeholders</p>\n<hr>\n<p>These placeholders can be used by specifying a recruiting player&#x27;s name. (Replace [player] with the name of the player.) The player must be online and must currently have a listing. Player names containing an underscore cannot be used here.</p>\n<ul><li><code>%md_recruitment_[player]_host%</code>   The name of the player hosting this recruitment. Which... you should already have if you&#x27;ve gotten this far. But it&#x27;s there.</li><li><code>%md_recruitment_[player]_label%</code>   The label of this player&#x27;s recruitment listing.</li><li><code>%md_recruitment_[player]_description%</code>   The description of this player&#x27;s recruitment listing.</li><li><code>%md_recruitment_[player]_total_players%</code>   How many players this recruitment listing needs in total.</li><li><code>%md_recruitment_[player]_players_present%</code>   How many players this recruitment listing already has.</li><li><code>%md_recruitment_[player]_players_needed%</code>   How many players this recruitment listing still needs. (Basically, the amount of spots still available in the party.)</li><li><code>%md_recruitment_[player]_needs_password%</code>   Meant to show &quot;Yes&quot; or &quot;No&quot; depending on if this listing requires a password to join. It currently always shows &quot;Yes&quot;, including for listings that do not need a password.</li></ul>"},"dungeons:custom-conditions":{"h":"<div class=\"tw\"><table><thead><tr><th><b>DISCLAIMER</b></th></tr></thead><tbody><tr><td><i>The API update will only be available in Mythic Dungeons 1.3.0+!</i></td></tr></tbody></table></div>\n<p>A <b>condition</b> is a gate on a <a href=\"#\" class=\"wl\" data-p=\"custom-triggers\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Custom-Triggers\">trigger</a>. A trigger can carry any number of conditions, and it only fires when every one of them passes. Conditions are where &quot;but only if the player is holding a key&quot; and &quot;but only 25% of the time&quot; live.</p>\n<p>This page assumes you have read <a href=\"#\" class=\"wl\" data-p=\"custom-elements\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Custom-Elements\">Getting Started with Elements</a> and <a href=\"#\" class=\"wl\" data-p=\"gui-menus\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/GUI-Menus\">GUI Menus</a>, which cover the annotations, the two required constructors, the saved-field system and the hotbar menu. Everything below is specific to conditions.</p>\n<h3>The skeleton</h3>\n<p>Extend <code>TriggerCondition</code>, annotate the class with <code>@DeclaredCondition</code>, and implement three abstract methods.</p>\n<pre class=\"cb\" data-lang=\"java\"><code>@DeclaredCondition\npublic class ConditionChance extends TriggerCondition {\n\n    @SavedField private double chance = 1.0;\n\n    public ConditionChance() {\n        super(&quot;Chance&quot;);\n    }\n\n    public ConditionChance(Map&lt;String, Object&gt; config) {\n        super(&quot;Chance&quot;, config);\n    }\n\n    @Override\n    public boolean check(TriggerFireEvent event) { return true; }\n\n    @Override\n    public MenuButton buildMenuButton() { return null; }\n\n    @Override\n    public void buildHotbarMenu() { }\n}</code></pre>\n<div class=\"tw\"><table><thead><tr><th>Method</th><th>Purpose</th></tr></thead><tbody><tr><td><code>boolean check(TriggerFireEvent event)</code></td><td>Your test. Return <code>true</code> to let the trigger through.</td></tr><tr><td><code>MenuButton buildMenuButton()</code></td><td>The item shown in the condition browser. Must be declared on this class, and must not return <code>null</code>.</td></tr><tr><td><code>void buildHotbarMenu()</code></td><td>The in-game options for this condition. Add items to the existing <code>menu</code> field; do not create a new one.</td></tr></tbody></table></div>\n<p>Conditions have no category, so the constructor is usually just the <code>super(...)</code> call. The <code>String</code> is the condition&#x27;s namespace, and it is what appears in the misconfiguration warnings described below.</p>\n<h3>Writing <code>check</code></h3>\n<p><code>check</code> is called by the trigger, once per firing attempt, for each of its conditions in order. The first <code>false</code> stops the trigger.</p>\n<p>Two rules:</p>\n<ul><li><b>Do not implement inversion yourself.</b> Builders can flip any condition in-game with the base invert toggle. Inversion is applied to your return value for you, after <code>check</code> runs. Write the positive test and nothing else.</li><li><b>Do not assume there is a player.</b> <code>TriggerFireEvent</code> can be raised without one, for instance by a trigger with <code>hasTarget</code> set to <code>false</code>. <code>event.getDPlayer()</code> is then <code>null</code>, and <code>event.getPlayer()</code> (the Bukkit player, already null-safe) returns <code>null</code> too. Check before you dereference either.</li></ul>\n<p>The condition&#x27;s own <code>instance</code>, <code>trigger</code> and <code>location</code> fields are populated before the first <code>check</code>, so you can read the running instance from <code>check</code> without passing anything in. <code>location</code> is the location of the trigger the condition is attached to.</p>\n<h3>Half-configured conditions</h3>\n<p>A condition that the builder placed but never filled in is a real situation, and returning <code>false</code> from <code>check</code> in that case would silently block the trigger forever with nothing in the console to explain it.</p>\n<p>Override <code>isConfigured()</code> instead. Returning <code>false</code> makes the trigger <b>skip</b> the condition entirely (treating it as absent, not as failed) and log a warning once per dungeon run, naming the dungeon, the trigger and the block coordinates. Override <code>misconfiguredReason()</code> to make that warning specific:</p>\n<pre class=\"cb\" data-lang=\"java\"><code>    @Override\n    public boolean isConfigured() {\n        return itemToMatch != null;\n    }\n\n    @Override\n    protected String misconfiguredReason() {\n        return &quot;no item has been set on it&quot;;\n    }</code></pre>\n<p>The default <code>isConfigured()</code> returns <code>true</code>, so a condition with no required fields needs neither override.</p>\n<h3>Optional overrides</h3>\n<div class=\"tw\"><table><thead><tr><th>Method</th><th>When it runs</th></tr></thead><tbody><tr><td><code>void onEnable()</code></td><td>The condition is bound to its trigger and instance.</td></tr><tr><td><code>void onDisable()</code></td><td>The instance is tearing down. <b>Cancel here whatever you started in <code>onEnable()</code></b>, or you will leak the dungeon world. See <a href=\"#\" class=\"wl\" data-p=\"memory-leaks\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Memory-Leaks\">Avoiding Memory Leaks</a>.</td></tr><tr><td><code>boolean isConfigured()</code> / <code>String misconfiguredReason()</code></td><td>As above.</td></tr><tr><td><code>void initLegacyFields()</code> / <code>void initLegacyFields(Map&lt;String, Object&gt; config)</code> / <code>void initAdditionalFields()</code></td><td>Field loading. Conditions may override all three, unlike functions and triggers. See <a href=\"#\" class=\"wl\" data-p=\"custom-elements\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Custom-Elements\">Getting Started with Elements</a>.</td></tr></tbody></table></div>\n<p><code>init()</code>, <code>enable(...)</code>, <code>disable()</code>, <code>initMenu()</code> and <code>warnMisconfigured()</code> are <code>final</code>.</p>\n<blockquote><p>_<b>Conditions do not receive events.</b> Unlike functions and triggers, condition registration does no <code>@EventHandler</code> wiring, so an event handler declared on a condition is never called. If your condition needs to observe something over time, cache it from a task started in <code>onEnable()</code>, and cancel that task in <code>onDisable()</code>._</p></blockquote>\n<h3>The hotbar menu</h3>\n<p>The base class has already placed two buttons before your <code>buildHotbarMenu()</code> runs: Back, and the invert toggle. That leaves seven slots. See <a href=\"#\" class=\"wl\" data-p=\"gui-menus\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/GUI-Menus\">GUI Menus</a> for what to do when you need more.</p>\n<h3>A worked example</h3>\n<p>The built-in <b>Chance</b> condition, trimmed to its essentials. (The real one looks its display strings up from Mythic Dungeons&#x27; own language file with <code>LangUtils</code>; literal strings are used here since your plugin has no entries in that file.)</p>\n<pre class=\"cb\" data-lang=\"java\"><code>@DeclaredCondition\npublic class ConditionChance extends TriggerCondition {\n\n    @SavedField private double chance = 1.0;\n\n    public ConditionChance(Map&lt;String, Object&gt; config) {\n        super(&quot;Chance&quot;, config);\n    }\n\n    public ConditionChance() {\n        super(&quot;Chance&quot;);\n    }\n\n    @Override\n    public boolean check(TriggerFireEvent event) {\n        return MathUtils.getRandomBoolean(chance);\n    }\n\n    @Override\n    public MenuButton buildMenuButton() {\n        MenuButton conditionButton = new MenuButton(Material.ENDER_EYE);\n        conditionButton.setDisplayName(&quot;&amp;aChance&quot;);\n        conditionButton.addLore(&quot;&amp;ePasses at random, with the&quot;);\n        conditionButton.addLore(&quot;&amp;econfigured probability.&quot;);\n\n        return conditionButton;\n    }\n\n    @Override\n    public void buildHotbarMenu() {\n        menu.addMenuItem(new ChatMenuItem() {\n\n            @Override\n            public void buildButton() {\n                button = new MenuButton(Material.ENDER_EYE);\n                button.setDisplayName(&quot;&amp;d&amp;lSet Chance&quot;);\n                button.addLore(&quot;&amp;eCurrent chance: &amp;6&quot; + chance);\n            }\n\n            @Override\n            public void onSelect(Player player) {\n                MessageUtils.sendMessage(player, Util.fullColorModern(&quot;&amp;eWhat is the percent chance of this trigger running? (0.0-1.0)&quot;));\n                MessageUtils.sendMessage(player, Util.fullColorModern(&quot;&amp;eCurrent chance is: &amp;6&quot; + chance));\n            }\n\n            @Override\n            public void onInput(Player player, String message) {\n                Optional&lt;Double&gt; value = StringUtils.readDoubleInput(player, message);\n                chance = value.orElse(chance);\n\n                if (value.isPresent()) MessageUtils.sendMessage(player, Util.fullColorModern(&quot;&amp;aSet success chance to &#x27;&amp;6&quot; + chance + &quot;&amp;a&#x27;&quot;));\n            }\n        });\n    }\n}</code></pre>\n<p>Two details worth copying:</p>\n<ol><li><code>check</code> is one line and has no side effects. A condition should answer a question, not change the dungeon.</li><li>The chance is read with <code>StringUtils.readDoubleInput</code>, which messages the player itself on bad input and returns an empty <code>Optional</code>, so the old value is kept. Do not let a typo write a broken value into a saved field.</li></ol>\n<h3>Registering it</h3>\n<p>From your own plugin&#x27;s <code>onEnable()</code>:</p>\n<pre class=\"cb\" data-lang=\"java\"><code>    MythicDungeons.inst().registerConditions(&quot;com.example.myplugin.conditions&quot;);</code></pre>\n<p>or, one class at a time:</p>\n<pre class=\"cb\" data-lang=\"java\"><code>    MythicDungeons.inst().registerCondition(ConditionChance.class);</code></pre>\n<p>Add <code>MythicDungeons</code> to <code>depend</code> in your <code>plugin.yml</code> so it loads first. The condition browser is built once during Mythic Dungeons&#x27; startup, so register on enable: a condition registered later still works, but may not appear in the browser until the next restart. See <a href=\"#\" class=\"wl\" data-p=\"custom-elements\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Custom-Elements\">Getting Started with Elements</a> for the full registration rules.</p>\n<p><a href=\"#\" class=\"wl\" data-p=\"api-parties\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/API-Parties\">**Next up:** Adding Party Support --&gt;</a></p>"},"dungeons:custom-triggers":{"h":"<div class=\"tw\"><table><thead><tr><th><b>DISCLAIMER</b></th></tr></thead><tbody><tr><td><i>The API update will only be available in Mythic Dungeons 1.3.0+!</i></td></tr></tbody></table></div>\n<p>A <b>trigger</b> is the &quot;when&quot; half of a dungeon element. It watches for something to happen (a player types in chat, a mob dies, a redstone signal changes) and then fires, which runs the <a href=\"#\" class=\"wl\" data-p=\"custom-functions\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Custom-Functions\">function</a> it is attached to.</p>\n<p>This page assumes you have read <a href=\"#\" class=\"wl\" data-p=\"custom-elements\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Custom-Elements\">Getting Started with Elements</a> and <a href=\"#\" class=\"wl\" data-p=\"gui-menus\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/GUI-Menus\">GUI Menus</a>, which cover the annotations, the two required constructors, the saved-field system and the hotbar menu. Everything below is specific to triggers.</p>\n<h3>The skeleton</h3>\n<p>Extend <code>DungeonTrigger</code>, annotate the class with <code>@DeclaredTrigger</code>, and implement two abstract methods.</p>\n<pre class=\"cb\" data-lang=\"java\"><code>@DeclaredTrigger\npublic class TriggerChat extends DungeonTrigger {\n\n    @SavedField private String text = &quot;DEFAULT&quot;;\n\n    public TriggerChat() {\n        super(&quot;Chat Message&quot;);\n        setCategory(TriggerCategory.PLAYER);\n        setHasTarget(true);\n    }\n\n    public TriggerChat(Map&lt;String, Object&gt; config) {\n        super(&quot;Chat Message&quot;, config);\n        setCategory(TriggerCategory.PLAYER);\n        setHasTarget(true);\n    }\n\n    @Override\n    public MenuButton buildMenuButton() { return null; }\n\n    @Override\n    public void buildHotbarMenu() { }\n}</code></pre>\n<div class=\"tw\"><table><thead><tr><th>Method</th><th>Purpose</th></tr></thead><tbody><tr><td><code>MenuButton buildMenuButton()</code></td><td>The item shown in the trigger picker. Must be declared on this class, and must not return <code>null</code>.</td></tr><tr><td><code>void buildHotbarMenu()</code></td><td>The in-game options for this trigger. Add items to the existing <code>menu</code> field; do not create a new one.</td></tr></tbody></table></div>\n<p>Notice what is <b>not</b> in that list: there is no abstract &quot;check&quot; method. A trigger is not polled. It watches for its own condition and calls <code>trigger(...)</code> when it sees it, usually from a Bukkit event handler.</p>\n<p>Unlike a function, the <code>String</code> you pass to <code>super(...)</code> is both the trigger&#x27;s namespace and its display name, and it is the name shown on the Trigger button in the attached function&#x27;s hotbar menu.</p>\n<h3>Configuring the trigger</h3>\n<p>Set these from <b>both</b> constructors, since neither runs when the other does.</p>\n<div class=\"tw\"><table><thead><tr><th>Call</th><th>What it controls</th></tr></thead><tbody><tr><td><code>setCategory(TriggerCategory)</code></td><td>Which tab of the trigger picker your trigger appears in. One of <code>DUNGEON</code>, <code>PLAYER</code>, <code>GENERAL</code>, <code>META</code>, <code>ROOM</code>.</td></tr><tr><td><code>setHasTarget(boolean)</code></td><td>Default <code>false</code>. Set it to <code>true</code> if your trigger fires with a specific player attached. This is what lets the attached function use the <code>PLAYER</code> target type; when it is <code>false</code>, that option is skipped and a <code>PARTY</code> target is displayed as &quot;ALL PLAYERS&quot;.</td></tr><tr><td><code>waitForConditions = true</code></td><td>Assign the field directly. See <span class=\"ilink\">Firing</span> below. Off by default.</td></tr></tbody></table></div>\n<p><code>allowRetrigger</code> is not yours to set from the constructor. It is seeded from the attached function&#x27;s <code>isAllowRetriggerByDefault()</code> when the trigger is initialised, and the builder can change it in-game.</p>\n<h3>Firing</h3>\n<p>Call one of these when your trigger&#x27;s condition is met:</p>\n<pre class=\"cb\" data-lang=\"java\"><code>    trigger();                          // no player attached\n    trigger(MythicPlayer aPlayer);      // this player is the trigger&#x27;s target\n    trigger(MythicPlayer aPlayer, boolean disable);</code></pre>\n<p>The shorter forms default <code>disable</code> to <code>!allowRetrigger</code>, which is almost always what you want: a one-shot trigger disables itself after firing, a repeatable one does not. Only pass the flag explicitly if you need to override that.</p>\n<p>Firing is not immediate. The trigger&#x27;s configured delay (<code>delayTicks</code>) is applied first: the whole check runs that many ticks later. Then the conditions are evaluated, then <code>TriggerFireEvent</code> is posted so other plugins can cancel it, and only then does the attached function execute. If the trigger is set to wait for its conditions and retriggering is off, the check repeats once a second until the conditions pass.</p>\n<p>That last part is what <code>waitForConditions</code> selects. With it off (the default), the check runs once after the delay and is dropped if the conditions fail. With it on, and retriggering off, the trigger keeps re-checking every second until its conditions are satisfied. Triggers for one-off moments (a mob dying, a player leaving) use it, so that a condition which is not yet true does not throw the event away.</p>\n<p>You do not check conditions yourself, post the event yourself, or call the function yourself. <code>trigger(...)</code> does all of that.</p>\n<blockquote><p>_In a <b>continuous</b> dungeon, each player may only fire a given trigger once, and the trigger is never disabled by firing. Both are handled for you inside <code>trigger(...)</code>._</p></blockquote>\n<h3>Listening for the event</h3>\n<p>Declare a Bukkit <code>@EventHandler</code> directly on the trigger class. Do not implement <code>Listener</code> and do not register anything: Mythic Dungeons wires it up at registration time and calls your method on every copy of the trigger in every running dungeon. That last part is why the first line of the handler always has to be a scope check.</p>\n<pre class=\"cb\" data-lang=\"java\"><code>    @EventHandler(priority = EventPriority.LOW)\n    public void onChat(AsyncPlayerChatEvent event) {\n        Player player = event.getPlayer();\n        if (!instance.isPlayerInInstance(player)) return;\n\n        if (!event.getMessage().equalsIgnoreCase(text)) return;\n        if (!matchesRoom(player.getLocation())) return;\n\n        event.setCancelled(true);\n        trigger(MythicDungeons.inst().getMythicPlayer(player));\n    }</code></pre>\n<p>If your trigger cannot be expressed as an event (a timer, a poll), start it in <code>onEnable()</code> instead and cancel it in <code>onDisable()</code>.</p>\n<h3>Room limiting</h3>\n<p>In a procedural dungeon the same trigger exists once per copy of the room. Without a room check, a chat message typed in one copy fires the trigger in all of them.</p>\n<p>Call <code>addRoomLimitToggleButton()</code> at the end of your <code>buildHotbarMenu()</code> to give builders the &quot;limit to room&quot; toggle (it costs one hotbar slot), then honour the resulting <code>limitToRoom</code> field before you fire:</p>\n<pre class=\"cb\" data-lang=\"java\"><code>    private boolean matchesRoom(Location origin) {\n        InstanceProcedural inst = instance.as(InstanceProcedural.class);\n        if (inst == null) return true;   // not a procedural dungeon, nothing to limit\n        if (!limitToRoom) return true;   // the builder turned it off\n\n        return inst.getRoom(origin) == inst.getRoom(location);\n    }</code></pre>\n<p><code>location</code> is the trigger&#x27;s own position, inherited from <code>DungeonElement</code>.</p>\n<h3>Optional overrides</h3>\n<div class=\"tw\"><table><thead><tr><th>Method</th><th>When it runs</th></tr></thead><tbody><tr><td><code>void onEnable()</code></td><td>The trigger is bound to a running instance. Start timers here.</td></tr><tr><td><code>void onDisable()</code></td><td>The instance is tearing down, or a one-shot trigger has fired. <b>Cancel here whatever you started in <code>onEnable()</code></b>, or you will leak the dungeon world. See <a href=\"#\" class=\"wl\" data-p=\"memory-leaks\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Memory-Leaks\">Avoiding Memory Leaks</a>.</td></tr><tr><td><code>void onTrigger(TriggerFireEvent event)</code></td><td>After the attached function has executed. For side effects that belong to the trigger rather than the function.</td></tr><tr><td><code>void initLegacyFields(Map&lt;String, Object&gt; config)</code></td><td>Field loading, for renamed saved fields. See <a href=\"#\" class=\"wl\" data-p=\"custom-elements\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Custom-Elements\">Getting Started with Elements</a>.</td></tr></tbody></table></div>\n<p><code>disable()</code>, <code>checkConditions(...)</code> and the two-argument <code>trigger(...)</code> are <code>final</code>. <code>init()</code> and <code>enable(...)</code> are managed for you and should not need overriding.</p>\n<h3>A worked example</h3>\n<p>The built-in <b>Chat Message</b> trigger, trimmed to its essentials. (The real one looks its display strings up from Mythic Dungeons&#x27; own language file with <code>LangUtils</code>, and offers case-sensitivity, exact-match and delay options; literal strings and a single option are used here.)</p>\n<pre class=\"cb\" data-lang=\"java\"><code>@DeclaredTrigger\npublic class TriggerChat extends DungeonTrigger {\n\n    @SavedField private String text = &quot;DEFAULT&quot;;\n\n    public TriggerChat(Map&lt;String, Object&gt; config) {\n        super(&quot;Chat Message&quot;, config);\n        setCategory(TriggerCategory.PLAYER);\n        setHasTarget(true);\n    }\n    public TriggerChat() {\n        super(&quot;Chat Message&quot;);\n        setCategory(TriggerCategory.PLAYER);\n        setHasTarget(true);\n    }\n\n    @Override\n    public MenuButton buildMenuButton() {\n        MenuButton triggerButton = new MenuButton(Material.NAME_TAG);\n        triggerButton.setDisplayName(&quot;&amp;aChat Message&quot;);\n        triggerButton.addLore(&quot;&amp;eFires when a player types&quot;);\n        triggerButton.addLore(&quot;&amp;ethe required text in chat.&quot;);\n\n        return triggerButton;\n    }\n\n    @EventHandler(priority = EventPriority.LOW)\n    public void onChat(AsyncPlayerChatEvent event) {\n        Player player = event.getPlayer();\n        if (!instance.isPlayerInInstance(player)) return;\n\n        if (!event.getMessage().equalsIgnoreCase(text)) return;\n        if (!matchesRoom(player.getLocation())) return;\n\n        event.setMessage(&quot;&quot;);\n        player.playSound(player.getLocation(), &quot;minecraft:entity.experience_orb.pickup&quot;, 0.5F, 1.2F);\n        event.setCancelled(true);\n\n        trigger(MythicDungeons.inst().getMythicPlayer(player));\n    }\n\n    private boolean matchesRoom(Location origin) {\n        InstanceProcedural inst = instance.as(InstanceProcedural.class);\n        if (inst == null) return true;\n        if (!limitToRoom) return true;\n\n        return inst.getRoom(origin) == inst.getRoom(location);\n    }\n\n    @Override\n    public void buildHotbarMenu() {\n        menu.addMenuItem(new ToggleMenuItem() {\n            @Override\n            public void buildButton() {\n                button = new MenuButton(Material.REDSTONE_TORCH);\n                button.setDisplayName(&quot;&amp;d&amp;lAllow Retriggering&quot;);\n                button.setEnchanted(allowRetrigger);\n            }\n\n            @Override\n            public void onSelect(Player player) {\n                allowRetrigger = !allowRetrigger;\n            }\n        });\n\n        menu.addMenuItem(new ChatMenuItem() {\n            @Override\n            public void buildButton() {\n                button = new MenuButton(Material.PAPER);\n                button.setDisplayName(&quot;&amp;d&amp;lEdit Required Text&quot;);\n            }\n\n            @Override\n            public void onSelect(Player player) {\n                MessageUtils.sendMessage(player, Util.fullColorModern(&quot;&amp;eWhat should the player type in chat?&quot;));\n                MessageUtils.sendMessage(player, Util.fullColorModern(&quot;&amp;eCurrent text: &amp;6&quot; + text));\n            }\n\n            @Override\n            public void onInput(Player player, String message) {\n                text = message;\n\n                MessageUtils.sendMessage(player, Util.fullColorModern(&quot;&amp;aSet required text to &#x27;&amp;6&quot; + message + &quot;&amp;a&#x27;&quot;));\n            }\n        });\n\n        addRoomLimitToggleButton();\n    }\n}</code></pre>\n<h3>Registering it</h3>\n<p>From your own plugin&#x27;s <code>onEnable()</code>:</p>\n<pre class=\"cb\" data-lang=\"java\"><code>    MythicDungeons.inst().registerTriggers(&quot;com.example.myplugin.triggers&quot;);</code></pre>\n<p>or, one class at a time:</p>\n<pre class=\"cb\" data-lang=\"java\"><code>    MythicDungeons.inst().registerTrigger(TriggerChat.class);</code></pre>\n<p>Add <code>MythicDungeons</code> to <code>depend</code> in your <code>plugin.yml</code> so it loads first. Triggers registered after Mythic Dungeons has built its menus are inserted into the trigger picker in place, so a late registration still shows up without a restart. See <a href=\"#\" class=\"wl\" data-p=\"custom-elements\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Custom-Elements\">Getting Started with Elements</a> for the full registration rules.</p>\n<p><a href=\"#\" class=\"wl\" data-p=\"custom-conditions\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Custom-Conditions\">**Next up:** Custom Conditions --&gt;</a></p>"},"dungeons:custom-functions":{"h":"<div class=\"tw\"><table><thead><tr><th><b>DISCLAIMER</b></th></tr></thead><tbody><tr><td><i>The API update will only be available in Mythic Dungeons 1.3.0+!</i></td></tr></tbody></table></div>\n<p>A <b>function</b> is the &quot;what happens&quot; half of a dungeon element: send a message, open a door, hand out loot. It does nothing on its own. A <a href=\"#\" class=\"wl\" data-p=\"custom-triggers\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Custom-Triggers\">trigger</a> decides when it runs, and the function decides what happens and to whom.</p>\n<p>This page assumes you have read <a href=\"#\" class=\"wl\" data-p=\"custom-elements\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Custom-Elements\">Getting Started with Elements</a> and <a href=\"#\" class=\"wl\" data-p=\"gui-menus\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/GUI-Menus\">GUI Menus</a>, which cover the annotations, the two required constructors, the saved-field system and the hotbar menu. Everything below is specific to functions.</p>\n<h3>The skeleton</h3>\n<p>Extend <code>DungeonFunction</code>, annotate the class with <code>@DeclaredFunction</code>, and implement three abstract methods.</p>\n<pre class=\"cb\" data-lang=\"java\"><code>@DeclaredFunction\npublic class FunctionMessage extends DungeonFunction {\n\n    @SavedField private String message = &quot;DEFAULT&quot;;\n\n    public FunctionMessage() {\n        super(&quot;Message&quot;);\n        targetType = FunctionTargetType.PARTY;\n        setCategory(FunctionCategory.PLAYER);\n    }\n\n    public FunctionMessage(Map&lt;String, Object&gt; config) {\n        super(&quot;Message&quot;, config);\n        targetType = FunctionTargetType.PARTY;\n        setCategory(FunctionCategory.PLAYER);\n    }\n\n    @Override\n    public void runFunction(TriggerFireEvent triggerEvent, List&lt;MythicPlayer&gt; targets) { }\n\n    @Override\n    public MenuButton buildMenuButton() { return null; }\n\n    @Override\n    public void buildHotbarMenu() { }\n}</code></pre>\n<div class=\"tw\"><table><thead><tr><th>Method</th><th>Purpose</th></tr></thead><tbody><tr><td><code>void runFunction(TriggerFireEvent triggerEvent, List&lt;MythicPlayer&gt; targets)</code></td><td>Your actual behaviour. <code>targets</code> has already been resolved for you from the function&#x27;s target type.</td></tr><tr><td><code>MenuButton buildMenuButton()</code></td><td>The item shown in the function browser. Must be declared on this class, and must not return <code>null</code>.</td></tr><tr><td><code>void buildHotbarMenu()</code></td><td>The in-game options for this function. Add items to the existing <code>menu</code> field; do not create a new one.</td></tr></tbody></table></div>\n<p>The <code>String</code> passed to <code>super(...)</code> is the function&#x27;s <b>namespace</b>. It also seeds the element&#x27;s display name, which is what appears on the function&#x27;s in-world label in edit mode and in the listings of meta elements such as the multi-function. It is not the name players see in the function browser; that comes from the display name you set on your menu button.</p>\n<h3>Configuring the function</h3>\n<p>These are all set from the constructors. Set the same values in <b>both</b> constructors, since neither runs when the other does.</p>\n<div class=\"tw\"><table><thead><tr><th>Call</th><th>What it controls</th></tr></thead><tbody><tr><td><code>setCategory(FunctionCategory)</code></td><td>Which tab of the function browser your function appears in, and its display colour. One of <code>DUNGEON</code>, <code>PLAYER</code>, <code>LOCATION</code>, <code>META</code>, <code>ROOM</code>. The colour is taken from the category, so call this rather than setting the colour yourself.</td></tr><tr><td><code>setRequiresTrigger(boolean)</code></td><td>Default <code>true</code>. A function that requires a trigger cannot be placed without one, and is given the Dungeon Start trigger automatically if the builder does not pick one. Set it to <code>false</code> for functions that are passive rules rather than events (the built-in Allow Block Place/Break and Door Control do this).</td></tr><tr><td><code>setRequiresTarget(boolean)</code></td><td>Default <code>false</code>. When <code>true</code>, the target type can never be <code>NONE</code>.</td></tr><tr><td><code>setAllowChangingTargetType(boolean)</code></td><td>Default <code>true</code>. When <code>false</code>, the Target Type button is left out of the hotbar menu entirely, freeing a slot. Use it when your function&#x27;s target only ever makes sense one way.</td></tr><tr><td><code>setAllowRetriggerByDefault(boolean)</code></td><td>Default <code>false</code>. Seeds the attached trigger&#x27;s &quot;allow retrigger&quot; option, so a function that is naturally repeatable starts out repeatable.</td></tr><tr><td><code>targetType = FunctionTargetType.X</code></td><td>The starting target type. Assign the field directly, as above.</td></tr></tbody></table></div>\n<h3>Targets</h3>\n<p><code>runFunction</code> is handed the list of players the function should act on. Which players those are depends on the function&#x27;s <code>FunctionTargetType</code>:</p>\n<ul><li><code>NONE</code> - no targets. The list is empty; act on the dungeon itself.</li><li><code>PLAYER</code> - the single player attached to the trigger. The list is empty if the trigger fired without a player.</li><li><code>PARTY</code> - that player&#x27;s party members who are still in this instance; the single player alone if they have no party; every player in the instance when the trigger had no player.</li><li><code>ROOM</code> - in a procedural dungeon, the players standing inside the room the function is placed in. Outside a procedural instance it resolves to nothing, and the ROOM option cannot even be selected outside a procedural edit session.</li></ul>\n<p>Before <code>runFunction</code> runs, Mythic Dungeons has already checked that the fire event belongs to this instance and that the firing trigger is the same class as this function&#x27;s trigger. You do not need to re-check either.</p>\n<blockquote><p>_<b>If you delay:</b> the target list is captured at fire time. If your function schedules work for later, a target may have left the dungeon by the time it runs. Re-filter with the protected helper <code>activeTargets(targets)</code>, which returns only those still in this instance._</p></blockquote>\n<h3>Optional overrides</h3>\n<div class=\"tw\"><table><thead><tr><th>Method</th><th>When it runs</th></tr></thead><tbody><tr><td><code>void onEnable()</code></td><td>The function is bound to a running instance. Start timers here.</td></tr><tr><td><code>void onDisable()</code></td><td>The instance is tearing down. <b>Cancel here whatever you started in <code>onEnable()</code></b>, or you will leak the dungeon world. See <a href=\"#\" class=\"wl\" data-p=\"memory-leaks\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Memory-Leaks\">Avoiding Memory Leaks</a>.</td></tr><tr><td><code>void onExecute(TriggerFireEvent triggerEvent)</code></td><td>Just before targets are resolved, on every execution. Useful for work that is not per-target.</td></tr><tr><td><code>void initLegacyFields(Map&lt;String, Object&gt; config)</code></td><td>Field loading, for renamed saved fields. See <a href=\"#\" class=\"wl\" data-p=\"custom-elements\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Custom-Elements\">Getting Started with Elements</a>.</td></tr></tbody></table></div>\n<p><code>init()</code>, <code>enable(...)</code>, <code>disable()</code> and <code>execute(...)</code> are managed for you; <code>execute</code> is <code>final</code>.</p>\n<h3>A worked example</h3>\n<p>The built-in <b>Message</b> function, trimmed to its essentials. (The real one looks its display strings up from Mythic Dungeons&#x27; own language file with <code>LangUtils</code>; literal strings are used here since your plugin has no entries in that file.)</p>\n<pre class=\"cb\" data-lang=\"java\"><code>@DeclaredFunction\npublic class FunctionMessage extends DungeonFunction {\n\n    @SavedField @Setter private String message = &quot;DEFAULT&quot;;\n\n    public FunctionMessage(Map&lt;String, Object&gt; config) {\n        super(&quot;Message&quot;, config);\n        targetType = FunctionTargetType.PARTY;\n        setCategory(FunctionCategory.PLAYER);\n    }\n    public FunctionMessage() {\n        super(&quot;Message&quot;);\n        targetType = FunctionTargetType.PARTY;\n        setCategory(FunctionCategory.PLAYER);\n    }\n\n    @Override\n    public void runFunction(TriggerFireEvent triggerEvent, List&lt;MythicPlayer&gt; targets) {\n        InstancePlayable instance = this.instance.asPlayInstance();\n        if (instance == null) return;\n\n        for (MythicPlayer dPlayer : targets) {\n            Player player = dPlayer.getPlayer();\n            String message = Util.parseVars(instance, this.message);\n            MessageUtils.sendMessage(player, Util.fullColorModern(message));\n        }\n    }\n\n    @Override\n    public MenuButton buildMenuButton() {\n        MenuButton functionButton = new MenuButton(Material.PAPER);\n        functionButton.setDisplayName(&quot;&amp;aMessage Sender&quot;);\n        functionButton.addLore(&quot;&amp;eSends a chat message&quot;);\n        functionButton.addLore(&quot;&amp;eto the target player(s).&quot;);\n\n        return functionButton;\n    }\n\n    @Override\n    public void buildHotbarMenu() {\n        menu.addMenuItem(new ChatMenuItem() {\n            @Override\n            public void buildButton() {\n                button = new MenuButton(Material.PAPER);\n                button.setDisplayName(&quot;&amp;d&amp;lEdit Message&quot;);\n            }\n\n            @Override\n            public void onSelect(Player player) {\n                MessageUtils.sendMessage(player, Util.fullColorModern(&quot;&amp;eWhat should the message say?&quot;));\n                MessageUtils.sendMessage(player, Util.fullColorModern(&quot;&amp;eCurrent message: &amp;6&quot; + message));\n            }\n\n            @Override\n            public void onInput(Player player, String message) {\n                FunctionMessage.this.message = message;\n\n                MessageUtils.sendMessage(player, Util.fullColorModern(&quot;&amp;aSet message to &#x27;&amp;6&quot; + message + &quot;&amp;a&#x27;&quot;));\n            }\n        });\n    }\n}</code></pre>\n<p>Three things in there are worth copying:</p>\n<ol><li><code>this.instance.asPlayInstance()</code> with a <code>null</code> check. The same function class is also constructed in edit sessions, where there is no run to act on.</li><li><code>Util.parseVars(instance, message)</code> runs the dungeon&#x27;s variable placeholders through the message.</li><li>The message is edited through a <code>ChatMenuItem</code>, not through some external command. Everything a builder can configure should be reachable from the hotbar menu.</li></ol>\n<h3>Registering it</h3>\n<p>From your own plugin&#x27;s <code>onEnable()</code>:</p>\n<pre class=\"cb\" data-lang=\"java\"><code>    MythicDungeons.inst().registerFunctions(&quot;com.example.myplugin.functions&quot;);</code></pre>\n<p>or, one class at a time:</p>\n<pre class=\"cb\" data-lang=\"java\"><code>    MythicDungeons.inst().registerFunction(FunctionMessage.class);</code></pre>\n<p>Add <code>MythicDungeons</code> to <code>depend</code> in your <code>plugin.yml</code> so it loads first. See <a href=\"#\" class=\"wl\" data-p=\"custom-elements\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Custom-Elements\">Getting Started with Elements</a> for the full registration rules, including the two things that make registration fail loudly: a non-public no-argument constructor, and a <code>buildMenuButton()</code> that is inherited rather than declared on your class.</p>\n<p><a href=\"#\" class=\"wl\" data-p=\"custom-triggers\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Custom-Triggers\">**Next up:** Custom Triggers --&gt;</a></p>"},"dungeons:config":{"h":"<p>Mythic Dungeons comes with a default config that includes lots of comments to explain all the different config options. But just in case you need to reference the original config, or if there&#x27;s been updates with new config options, you can always retrieve the config from here!</p>\n<h5>Mythic Dungeons config.yml</h5>\n<pre class=\"cb\" data-lang=\"yaml\"><code>General:\n  # The language file to load from the lang/ folder (without the .yml extension).\n  # &#x27;en&#x27; uses lang/en.yml. To add another language, copy lang/en.yml to lang/&lt;code&gt;.yml\n  # (for example lang/tr.yml), translate the values, then set this to that code.\n  # Any keys you don&#x27;t translate fall back to English automatically.\n  Language: en\n  # What plugin to use for the party system. Defaults to DungeonParties, which is provided by Mythic Dungeons.\n  PartyPlugin: Default\n  # Enable or disable the built-in party-chat system\n  PartyChat: false\n  # The max number of instances that can be run on this server.\n  MaxInstances: 10\n  # The interval between autosaves of the dungeon while in edit mode in seconds. 0 disables autosave. (NOT RECOMMENDED)\n  AutoSaveInterval: 300\n  # What the function builder item&#x27;s material is. Must be a valid Spigot material.\n  # Useful if you have another plugin that also uses FEATHER for a tool, like goPaint\n  FunctionBuilderItem: FEATHER\n  # Whether the `/md stuck` command kills the player.\n  StuckKillsPlayer: false\n  # Whether only the party leader is allowed to start a dungeon for the party.\n  LeaderOnlyQueue: true\n  # Whether to require all party members confirm they&#x27;re ready before entering a dungeon after the play command is run.\n  # Note: This check is used if there is no queue to get into the dungeon.\n  ReadyCheckOnCommand: true\n  # Whether to require all party members confirm they&#x27;re ready before entering a dungeon after waiting in the queue.\n  ReadyCheckInQueue: true\n  # How long in seconds the ready check (from when a dungeon becomes available) will wait for all players to be ready.\n  ReadyCheckTime: 45\n  # Whether to send all ready players into the dungeon when the above timer expires.\n  StartWithoutUnreadyPlayers: false\n  # Whether to require the party leader to be ready when starting without all players.\n  ReadyCheckRequireLeader: true\n  PartyFinder:\n    # Whether to automatically broadcast party recruitments regularly.\n    AutoBroadcast: true\n    # How many minutes there should be between each broadcast of a recruitment listing.\n    ListingBroadcastPeriod: 5\n  # What Multiverse-Inventories group to use for all dungeons.\n  MultiverseGroup: default\n# Settings related to procedural (room-based) dungeon generation.\nGenerator:\n  # How long the generator will spend trying to create a valid dungeon layout in seconds. If you have particularly complex dungeons, consider increasing this.\n  # NOTE: This is handled async and will NOT lock up your server! Yay! This is useful if you have very large dungeons or rooms with lots of restrictive rules.\n  Timeout: 5\n# These config options have potential to cause unforeseen issues and strangeness.\nExperimental:\n  # Whether entities standing on moving blocks will move with the block.\n  # WARNING :: Upwards movement will not work unless `allow-flight` in server.properties is set to TRUE! Use at your own risk!\n  # WARNING 2 :: Jumping from a moving block DOES NOT WORK due to ping latency! We are investigating if this can be fixed but it seems unlikely due to vanilla limitations.\n  MovingBlocksMoveEntities: false\n  # Whether to keep all dungeons in a master world to avoid constantly loading and unloading worlds. SIGNIFICANTLY reduces risks of memory leaks.\n  # WARNING :: This does NOT load all dungeons into your existing survival worlds!! It creates a SEPARATE world that will host all the dungeons!\n  OneWorldDungeons: false\n  # Baseline world options for the shared OneWorldDungeons world. These apply to all dungeons.\n  # Dungeons can still override time and weather per-player when they differ from these values.\n  # Only the gamerules you list are changed; anything omitted keeps its vanilla default.\n  OneWorldDungeonsWorld:\n    # PEACEFUL, EASY, NORMAL, or HARD.\n    Difficulty: NORMAL\n    # Ticks. Only fixed when doDaylightCycle is false.\n    WorldTime: 6000\n    # CLEAR or DOWNFALL. Only fixed when doWeatherCycle is false.\n    WorldWeather: CLEAR\n    Gamerule:\n      keepInventory: false\n      doDaylightCycle: true\n      doWeatherCycle: true\n      mobGriefing: true\n      randomTickSpeed: 0\nSpectator:\n  # Whether to use the legacy spectator system, modern one requires packetevents.\n  UseLegacy: true\n  Hotbar:\n    Teleport:\n      Material: COMPASS\n      Slot: 0\n      Name: &#x27;&lt;green&gt;Teleport to Player&lt;/green&gt;&#x27;\n      Lore:\n        - &#x27;&lt;gray&gt;Click to open the teleport gui.&lt;/gray&gt;&#x27;\n    Settings:\n      Material: COMPARATOR\n      Slot: 4\n      Name: &#x27;&lt;aqua&gt;Options&lt;/aqua&gt;&#x27;\n      Lore:\n        - &#x27;&lt;gray&gt;Click to change some spectator options.&lt;/gray&gt;&#x27;\n    Leave:\n      Material: RED_BED\n      Slot: 8\n      Name: &#x27;&lt;red&gt;Leave Dungeon&lt;/red&gt;&#x27;\n      Lore:\n        - &#x27;&lt;gray&gt;Click to leave the dungeon.&lt;/gray&gt;&#x27;\n  GUI:\n    Teleport:\n      Title: &#x27;&lt;gray&gt;Teleport to Player&lt;/gray&gt;&#x27;\n      ItemName: &#x27;&lt;green&gt;Teleport to %player_name%&lt;/green&gt;&#x27;\n      ItemLore:\n        - &#x27;&lt;gray&gt;Click to teleport to %player_name%.&lt;/gray&gt;&#x27;\n    Settings:\n        Title: &#x27;&lt;gray&gt;Options&lt;/gray&gt;&#x27;\n        NightVision:\n          EnabledName: &#x27;&lt;gold&gt;Night Vision&lt;/gold&gt;&#x27;\n          DisabledName: &#x27;&lt;red&gt;Night Vision&lt;/red&gt;&#x27;\n          EnabledMaterial: GLOWSTONE\n          DisabledMaterial: GUNPOWDER\n          Slot: 13\n          Lore:\n            - &#x27;&lt;gray&gt;Click to toggle night vision.&lt;/gray&gt;&#x27;\n        Speed:\n            Name: &#x27;&lt;white&gt;Speed: {speed}&lt;/white&gt;&#x27;\n            Material: FEATHER\n            Slot: 11\n            Lore:\n                - &#x27;&lt;gray&gt;Cycle fly speed, right click to increase, left click to reset&lt;/gray&gt;&#x27;</code></pre>\n<h5>General options</h5>\n<div class=\"tw\"><table><thead><tr><th>Option</th><th>Type</th><th>Default</th><th>What it does</th></tr></thead><tbody><tr><td><code>Language</code></td><td>text</td><td><code>en</code></td><td>Which file in the <code>lang/</code> folder is loaded, without the <code>.yml</code>. <code>en</code> loads <code>lang/en.yml</code>.</td></tr><tr><td><code>PartyPlugin</code></td><td>text</td><td><code>Default</code></td><td>Which plugin provides the party system.</td></tr><tr><td><code>PartyChat</code></td><td>true/false</td><td><code>false</code></td><td>Enables the built-in party chat. While it is on, a player who has party chat toggled on has their chat sent to their party instead of public chat.</td></tr><tr><td><code>MaxInstances</code></td><td>number</td><td><code>10</code></td><td>Server-wide cap on how many dungeon instances can be running at once. It only stops a <i>new</i> instance from being created.</td></tr><tr><td><code>AutoSaveInterval</code></td><td>number (seconds)</td><td><code>300</code></td><td>How often a dungeon in edit mode autosaves. <code>0</code> (or less) turns autosave off.</td></tr><tr><td><code>FunctionBuilderItem</code></td><td>material name</td><td><code>FEATHER</code></td><td>The material of the function builder tool.</td></tr><tr><td><code>StuckKillsPlayer</code></td><td>true/false</td><td><code>false</code></td><td>When <code>true</code>, <code>/md stuck</code> kills the player so they respawn. When <code>false</code>, it teleports them to their last checkpoint. The <code>/md stuck</code> help line is always shown, only its wording changes.</td></tr><tr><td><code>LeaderOnlyQueue</code></td><td>true/false</td><td><code>true</code></td><td>Whether only the party leader may start a dungeon for the party.</td></tr><tr><td><code>ReadyCheckOnCommand</code></td><td>true/false</td><td><code>true</code></td><td>Whether a ready check runs after the play command when there is no queue to wait in.</td></tr><tr><td><code>ReadyCheckInQueue</code></td><td>true/false</td><td><code>true</code></td><td>Whether a ready check runs when the dungeon becomes available after queueing.</td></tr><tr><td><code>ReadyCheckTime</code></td><td>number (seconds)</td><td><code>45</code></td><td>How long a ready check waits for everyone to confirm.</td></tr><tr><td><code>StartWithoutUnreadyPlayers</code></td><td>true/false</td><td><code>false</code></td><td>Whether the players who did confirm are sent in when the ready-check timer expires.</td></tr><tr><td><code>ReadyCheckRequireLeader</code></td><td>true/false</td><td><code>true</code></td><td>Whether the party leader must be ready for a start without all players.</td></tr><tr><td><code>PartyFinder.AutoBroadcast</code></td><td>true/false</td><td><code>true</code></td><td>Nothing. The plugin never reads this option, see &quot;Options that are not currently applied&quot; below.</td></tr><tr><td><code>PartyFinder.ListingBroadcastPeriod</code></td><td>number (minutes)</td><td><code>5</code></td><td>How long between broadcasts of a party recruitment listing.</td></tr><tr><td><code>MultiverseGroup</code></td><td>text</td><td><code>default</code></td><td>The Multiverse-Inventories world group a dungeon world joins when it loads and leaves when it is disposed.</td></tr></tbody></table></div>\n<h5>Generator options</h5>\n<div class=\"tw\"><table><thead><tr><th>Option</th><th>Type</th><th>Default</th><th>What it does</th></tr></thead><tbody><tr><td><code>Generator.Timeout</code></td><td>number (seconds)</td><td><code>5</code></td><td>How long the plugin will spend trying to build a valid procedural layout before giving up. The same value also limits the safe spawn location search. Read fresh every time it is used.</td></tr></tbody></table></div>\n<h5>Experimental options</h5>\n<div class=\"tw\"><table><thead><tr><th>Option</th><th>Type</th><th>Default</th><th>What it does</th></tr></thead><tbody><tr><td><code>MovingBlocksMoveEntities</code></td><td>true/false</td><td><code>false</code></td><td>Whether entities standing on a moving block move with it.</td></tr><tr><td><code>OneWorldDungeons</code></td><td>true/false</td><td><code>false</code></td><td>Whether every dungeon instance runs inside one shared world instead of a world of its own. See <a href=\"#\" class=\"wl\" data-p=\"one-world-dungeons\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/One-World-Dungeons\">One World Dungeons</a>.</td></tr><tr><td><code>OneWorldDungeonsWorld.Difficulty</code></td><td>text</td><td><code>NORMAL</code></td><td>Difficulty of the shared world: <code>PEACEFUL</code>, <code>EASY</code>, <code>NORMAL</code> or <code>HARD</code>. An unrecognised value is logged and the difficulty is left alone.</td></tr><tr><td><code>OneWorldDungeonsWorld.WorldTime</code></td><td>number (ticks)</td><td><code>6000</code></td><td>The fixed time of day for the shared world, applied only while <code>doDaylightCycle</code> is <code>false</code>. It is also the baseline that per-player time overrides are compared against.</td></tr><tr><td><code>OneWorldDungeonsWorld.WorldWeather</code></td><td>text</td><td><code>CLEAR</code></td><td><code>CLEAR</code> or <code>DOWNFALL</code>, applied only while <code>doWeatherCycle</code> is <code>false</code>. Anything that is not <code>DOWNFALL</code> clears both rain and thunder. Also the baseline for per-player weather overrides.</td></tr><tr><td><code>OneWorldDungeonsWorld.Gamerule.keepInventory</code></td><td>true/false</td><td><code>false</code></td><td>Applied as a real gamerule on the shared world.</td></tr><tr><td><code>OneWorldDungeonsWorld.Gamerule.doDaylightCycle</code></td><td>true/false</td><td><code>true</code></td><td>Applied as a gamerule, and gates whether <code>WorldTime</code> is held fixed.</td></tr><tr><td><code>OneWorldDungeonsWorld.Gamerule.doWeatherCycle</code></td><td>true/false</td><td><code>true</code></td><td>Applied as a gamerule, and gates whether <code>WorldWeather</code> is held fixed.</td></tr><tr><td><code>OneWorldDungeonsWorld.Gamerule.mobGriefing</code></td><td>true/false</td><td><code>true</code></td><td>Applied as a real gamerule on the shared world.</td></tr><tr><td><code>OneWorldDungeonsWorld.Gamerule.randomTickSpeed</code></td><td>number</td><td><code>0</code></td><td>Applied as a real gamerule on the shared world.</td></tr><tr><td><code>OneWorldDungeonsWorld.Gamerule.</code><i>(any other rule)</i></td><td>true/false or number</td><td>not shipped</td><td>The <code>Gamerule</code> section is not a fixed list. Add any valid gamerule name and it is applied. An unknown name or a value of the wrong type is skipped with a console warning.</td></tr></tbody></table></div>\n<h5>Spectator options</h5>\n<p><code>UseLegacy</code> and the whole <code>Hotbar</code> block are read once when the plugin starts. The <code>GUI</code> block is only read when the modern spectator system is active, so with legacy spectating those options are never loaded at all.</p>\n<div class=\"tw\"><table><thead><tr><th>Option</th><th>Type</th><th>Default</th><th>What it does</th></tr></thead><tbody><tr><td><code>UseLegacy</code></td><td>true/false</td><td><code>true</code></td><td><code>true</code> puts spectators in plain spectator mode. <code>false</code> uses the reworked system with the custom hotbar and menus below.</td></tr><tr><td><code>Hotbar.Teleport.Material</code></td><td>material name</td><td><code>COMPASS</code></td><td>Material of the teleport item.</td></tr><tr><td><code>Hotbar.Teleport.Slot</code></td><td>number</td><td><code>0</code></td><td>Hotbar slot the teleport item is placed in.</td></tr><tr><td><code>Hotbar.Teleport.Name</code></td><td>text</td><td><code>&#x27;&lt;green&gt;Teleport to Player&lt;/green&gt;&#x27;</code></td><td>Display name of the teleport item.</td></tr><tr><td><code>Hotbar.Teleport.Lore</code></td><td>list of text</td><td>one line</td><td>Lore of the teleport item.</td></tr><tr><td><code>Hotbar.Settings.Material</code></td><td>material name</td><td><code>COMPARATOR</code></td><td>Material of the options item.</td></tr><tr><td><code>Hotbar.Settings.Slot</code></td><td>number</td><td><code>4</code></td><td>Hotbar slot the options item is placed in.</td></tr><tr><td><code>Hotbar.Settings.Name</code></td><td>text</td><td><code>&#x27;&lt;aqua&gt;Options&lt;/aqua&gt;&#x27;</code></td><td>Display name of the options item.</td></tr><tr><td><code>Hotbar.Settings.Lore</code></td><td>list of text</td><td>one line</td><td>Lore of the options item.</td></tr><tr><td><code>Hotbar.Leave.Material</code></td><td>material name</td><td><code>RED_BED</code></td><td>Material of the leave item.</td></tr><tr><td><code>Hotbar.Leave.Slot</code></td><td>number</td><td><code>8</code></td><td>Hotbar slot the leave item is placed in.</td></tr><tr><td><code>Hotbar.Leave.Name</code></td><td>text</td><td><code>&#x27;&lt;red&gt;Leave Dungeon&lt;/red&gt;&#x27;</code></td><td>Display name of the leave item.</td></tr><tr><td><code>Hotbar.Leave.Lore</code></td><td>list of text</td><td>one line</td><td>Lore of the leave item.</td></tr><tr><td><code>GUI.Teleport.Title</code></td><td>text</td><td><code>&#x27;&lt;gray&gt;Teleport to Player&lt;/gray&gt;&#x27;</code></td><td>Nothing. The code reads <code>Spectator.GUI.Teleport.GuiTitle</code> instead, which is not shipped. See &quot;Options that are not currently applied&quot; below.</td></tr><tr><td><code>GUI.Teleport.ItemName</code></td><td>text</td><td><code>&#x27;&lt;green&gt;Teleport to %player_name%&lt;/green&gt;&#x27;</code></td><td>Name of each player head in the teleport menu. <code>%player_name%</code> is replaced, and PlaceholderAPI placeholders work when that plugin is installed.</td></tr><tr><td><code>GUI.Teleport.ItemLore</code></td><td>list of text</td><td>one line</td><td>Lore of each player head. <code>%player_name%</code> is <b>not</b> replaced here, so the shipped line shows the placeholder as written.</td></tr><tr><td><code>GUI.Settings.Title</code></td><td>text</td><td><code>&#x27;&lt;gray&gt;Options&lt;/gray&gt;&#x27;</code></td><td>Nothing. The code reads <code>Spectator.GUI.Settings.GuiTitle</code> instead, which is not shipped.</td></tr><tr><td><code>GUI.Settings.NightVision.EnabledName</code></td><td>text</td><td><code>&#x27;&lt;gold&gt;Night Vision&lt;/gold&gt;&#x27;</code></td><td>Name of the night vision button while night vision is on.</td></tr><tr><td><code>GUI.Settings.NightVision.DisabledName</code></td><td>text</td><td><code>&#x27;&lt;red&gt;Night Vision&lt;/red&gt;&#x27;</code></td><td>Name of the night vision button while night vision is off.</td></tr><tr><td><code>GUI.Settings.NightVision.EnabledMaterial</code></td><td>material name</td><td><code>GLOWSTONE</code></td><td>Material of the night vision button while it is on.</td></tr><tr><td><code>GUI.Settings.NightVision.DisabledMaterial</code></td><td>material name</td><td><code>GUNPOWDER</code></td><td>Material of the night vision button while it is off.</td></tr><tr><td><code>GUI.Settings.NightVision.Slot</code></td><td>number</td><td><code>13</code></td><td>Nothing. The code reads <code>NightVision.ItemSlot</code> instead, which is not shipped, so the button stays on slot 13.</td></tr><tr><td><code>GUI.Settings.NightVision.Lore</code></td><td>list of text</td><td>one line</td><td>Nothing. The code reads <code>NightVision.ItemLore</code> instead, which is not shipped, so the button has no lore.</td></tr><tr><td><code>GUI.Settings.Speed.Name</code></td><td>text</td><td><code>&#x27;&lt;white&gt;Speed: {speed}&lt;/white&gt;&#x27;</code></td><td>Nothing. The code reads <code>Settings.FlySpeed.ItemName</code> instead, which is not shipped, so the button is always named &quot;Fly Speed&quot;.</td></tr><tr><td><code>GUI.Settings.Speed.Material</code></td><td>material name</td><td><code>FEATHER</code></td><td>Nothing. The code reads <code>Settings.FlySpeed.ItemMaterial</code> instead, which is not shipped, so the button is always a feather.</td></tr><tr><td><code>GUI.Settings.Speed.Slot</code></td><td>number</td><td><code>11</code></td><td>Nothing. The code reads <code>Settings.FlySpeed.ItemSlot</code> instead, which is not shipped, so the button stays on slot 11.</td></tr><tr><td><code>GUI.Settings.Speed.Lore</code></td><td>list of text</td><td>one line</td><td>Nothing. The code reads <code>Settings.FlySpeed.ItemLore</code> instead, which is not shipped, so the button has no lore.</td></tr></tbody></table></div>\n<h5>Notes on individual options</h5>\n<ul><li><b>Language</b> is a file name in the <code>lang/</code> folder without the <code>.yml</code>. If the file does not exist, the plugin logs a notice and uses English. Keys you leave untranslated fall back to English one key at a time, so a partial translation is fine.</li><li><b>PartyPlugin</b> recognises <code>Default</code>, <code>DungeonParties</code>, <code>Heroes</code>, <code>Parties</code>, <code>MythicRPG</code> and <code>PAF</code> (which also matches a plugin named <code>PartyAndFriendsGUI</code>), matched without regard to case. Only <code>Default</code> and <code>DungeonParties</code> register the <code>/p</code> and <code>/party</code> commands. Any other value must match an installed plugin&#x27;s name, otherwise the console logs <code>Party plugin is set to &#x27;&lt;name&gt;&#x27;, but no such plugin was found!</code> and party support stays off.</li><li><b>MaxInstances</b> only stops a <i>new</i> instance from being created. A player can still join a running instance that has room. Each dungeon can also set its own <code>General.MaxInstances</code>, where <code>0</code> means unlimited. See <a href=\"#\" class=\"wl\" data-p=\"dungeon-config\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Dungeon-Config\">Dungeon Config</a>.</li><li><b>FunctionBuilderItem</b> is case-sensitive and must be the exact uppercase Spigot material name, for example <code>FEATHER</code>, not <code>feather</code>. A name that does not match exactly is <i>not</i> replaced by the default, and handing out the function builder tool then fails.</li><li><b>AutoSaveInterval</b> is read when an edit session starts, so a change only affects edit sessions opened after it.</li><li><b>MultiverseGroup</b> only does anything when Multiverse-Inventories is installed. A group name that does not exist is ignored silently.</li><li><b>Queue and ready-check options</b> (<code>LeaderOnlyQueue</code>, <code>ReadyCheckOnCommand</code>, <code>ReadyCheckInQueue</code>, <code>ReadyCheckTime</code>, <code>StartWithoutUnreadyPlayers</code>, <code>ReadyCheckRequireLeader</code>) can each be overridden per dungeon (2.0.1+) by adding the same key to that dungeon&#x27;s own <code>config.yml</code>. Put it at the <b>top level</b> of that file, not inside its <code>General:</code> section, or nothing will read it. Omit a key to inherit the global value. Per dungeon, <code>ReadyCheckTime</code> also accepts a placeholder or a math expression instead of a plain number; placeholders need MythicMobs installed and math needs MythicMobs Premium, and without them the value falls back to the global setting.</li><li><b>OneWorldDungeonsWorld</b> is only used when <code>OneWorldDungeons</code> is <code>true</code>, and it is only applied when the shared dungeon world is created, so changes need a restart. Because every dungeon shares that one world, these are the settings for <i>all</i> of them. <code>Gamerule</code> is an open list: add any valid gamerule name and it is applied, while unknown names and wrong value types are skipped with a console warning. <code>WorldTime</code> is only held fixed while <code>doDaylightCycle</code> is <code>false</code>, and <code>WorldWeather</code> only while <code>doWeatherCycle</code> is <code>false</code>. A dungeon whose own time or weather differs from these values gets a per-player override instead. When a dungeon has gamerules that a shared world cannot honour, the console lists them and points you at this section. See <a href=\"#\" class=\"wl\" data-p=\"one-world-dungeons\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/One-World-Dungeons\">One World Dungeons</a>.</li><li><b>Spectator.UseLegacy</b> needs <b>both</b> PacketEvents and MythicMobs for the modern system. If either plugin is missing, the legacy system is used no matter what this is set to. (The comment in the shipped file mentions only PacketEvents.)</li><li><b>Spectator text</b> in <code>Name</code>, <code>Lore</code>, <code>ItemName</code> and <code>ItemLore</code> accepts MiniMessage tags (as shipped) and legacy <code>&amp;</code> colour codes.</li><li><b>Spectator materials</b> must be valid material names. An invalid one is not replaced by a default, it throws an error during startup and the plugin ends up disabled. The <code>Hotbar</code> materials are also case-sensitive, so use <code>RED_BED</code>, not <code>red_bed</code>.</li></ul>\n<h5>Options that are not currently applied</h5>\n<p>These options ship in <code>config.yml</code> but no code reads them. Changing them has no effect:</p>\n<ul><li><code>General.PartyFinder.AutoBroadcast</code>. Recruitment listings always broadcast. Use <code>ListingBroadcastPeriod</code> to change how often, and note that the first broadcast fires as soon as the listing is posted.</li><li><code>Spectator.GUI.Teleport.Title</code> and <code>Spectator.GUI.Settings.Title</code>. The code reads <code>GuiTitle</code> under each of those sections, not <code>Title</code>.</li><li><code>Spectator.GUI.Settings.NightVision.Slot</code> and <code>.Lore</code>. The code reads <code>ItemSlot</code> and <code>ItemLore</code>, not <code>Slot</code> and <code>Lore</code>.</li><li><code>Spectator.GUI.Settings.Speed.Name</code>, <code>.Material</code>, <code>.Slot</code> and <code>.Lore</code>. The code reads them under <code>Settings.FlySpeed</code>, not <code>Settings.Speed</code>, and as <code>ItemName</code> / <code>ItemMaterial</code> / <code>ItemSlot</code> / <code>ItemLore</code>. The button ends up a feather in slot 11 named &quot;Fly Speed&quot;, with no lore and no live speed number.</li></ul>\n<p>The names the code reads instead are all in the next section, and none of them can be added by hand.</p>\n<h5>Options the plugin reads but does not ship</h5>\n<p>Eleven option names exist in the code but are not in the bundled <code>config.yml</code>. Because the config sync below deletes any key that is not in the bundle, and it does so before the plugin reads the file, adding them by hand does not work. They are listed here only so you know what their fixed behaviour is:</p>\n<ul><li><code>General.RoomEditorItem</code>. The room editor tool is always a <code>GOLDEN_AXE</code>.</li><li><code>General.CommandFunctionAsOp</code>. Always off, so a Command function&#x27;s sender only cycles between <code>CONSOLE</code> and <code>PLAYER</code>. The third option, <code>PLAYER_FORCED</code>, which temporarily ops the player so the command runs without their permission, cannot be selected.</li><li><code>Experimental.OneWorldDungeonsKeepInv</code>. Superseded by <code>Experimental.OneWorldDungeonsWorld.Gamerule.keepInventory</code>, which is what you should set.</li><li><code>Spectator.GUI.Teleport.GuiTitle</code>. Title of the teleport menu. Always <code>&amp;8Teleport to Player</code>.</li><li><code>Spectator.GUI.Settings.GuiTitle</code>. Title of the spectator settings menu. Always <code>&amp;8Spectator Settings</code>.</li><li><code>Spectator.GUI.Settings.NightVision.ItemSlot</code>. Always <code>13</code>.</li><li><code>Spectator.GUI.Settings.NightVision.ItemLore</code>. Always empty.</li><li><code>Spectator.GUI.Settings.FlySpeed.ItemName</code>. Always <code>&amp;8Fly Speed</code>. This is the only name a <code>{speed}</code> token would be substituted into (with the fly speed times ten), and the fixed default does not contain one, which is why the button never shows a number.</li><li><code>Spectator.GUI.Settings.FlySpeed.ItemLore</code>. Always empty.</li><li><code>Spectator.GUI.Settings.FlySpeed.ItemMaterial</code>. Always <code>FEATHER</code>.</li><li><code>Spectator.GUI.Settings.FlySpeed.ItemSlot</code>. Always <code>11</code>.</li></ul>\n<h5>How config.yml is kept up to date</h5>\n<p><code>config.yml</code> repairs itself every time the server starts and every time you run <code>/md reload</code>:</p>\n<ul><li>Any option that exists in the new version but is missing from your file is added with its default value.</li><li>Any option that is <b>not</b> in the plugin&#x27;s bundled config is <b>deleted</b> from your file. Keys you add by hand do not survive, and they are removed before the plugin reads the file, so they never take effect at all.</li><li>The <code>#</code> help text above each option is refreshed from the bundle whenever anything was added or removed, so your own comments on those options are replaced.</li><li>Every change is logged, one line per key: <code>[config.yml] Added missing key &lt;key&gt;</code> and <code>[config.yml] Removed obsolete key &lt;key&gt;</code>.</li></ul>\n<p>This applies to <code>config.yml</code> only:</p>\n<ul><li><code>lang/en.yml</code> gets new keys added but nothing is removed, and other <code>lang/&lt;code&gt;.yml</code> files are left completely alone.</li><li><code>maps/default-config.yml</code> and <code>maps/default-generator.yml</code> are copied out once when they are missing and never updated again.</li><li>Every dungeon&#x27;s own <code>config.yml</code> is a one-time copy of <code>maps/default-config.yml</code> made when the dungeon is created, and is never synced afterwards. New per-dungeon options use their built-in defaults until you add them by hand. See <a href=\"#\" class=\"wl\" data-p=\"dungeon-config\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Dungeon-Config\">Dungeon Config</a>.</li></ul>\n<h5>What <code>/md reload</code> picks up</h5>\n<p><code>/md reload</code> (see <a href=\"#\" class=\"wl\" data-p=\"commands-and-permissions\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Commands-and-Permissions\">Commands and Permissions</a>) re-reads <code>Language</code>, <code>FunctionBuilderItem</code> and <code>StuckKillsPlayer</code>, and reloads the language files and loot tables.</p>\n<p>These take effect on their next use without any reload, because they are read fresh each time: <code>MaxInstances</code>, <code>AutoSaveInterval</code> (next edit session), <code>MultiverseGroup</code>, all six queue and ready-check options, <code>PartyChat</code>, <code>PartyFinder.ListingBroadcastPeriod</code> (next listing) and <code>Generator.Timeout</code>.</p>\n<p>These need a <b>full server restart</b>: <code>PartyPlugin</code>, <code>MovingBlocksMoveEntities</code>, <code>OneWorldDungeons</code>, everything under <code>OneWorldDungeonsWorld</code> and everything under <code>Spectator</code>.</p>"},"dungeons:api-parties":{"h":"<div class=\"tw\"><table><thead><tr><th><b>DISCLAIMER</b></th></tr></thead><tbody><tr><td><i>The following is a feature only available in Mythic Dungeons 1.3.0+!</i></td></tr></tbody></table></div>\n<p>Mythic Dungeons provides a simple way to add compatibility for other party plugins. After following the instructions outlined in the <a href=\"#\" class=\"wl\" data-p=\"introduction-to-api\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Introduction-to-API\">introduction page</a>, there are only two steps needed to make your party plugin compatible.</p>\n<p>After that, <b>server owners need to tell Mythic Dungeons to use your plugin as a party provider!</b> This is done through the <code>PartyPlugin</code> option in the Mythic Dungeons config.</p>\n<p>The option is <code>General.PartyPlugin</code> in <code>plugins/MythicDungeons/config.yml</code>, and its value must also be the name of an installed plugin, otherwise Mythic Dungeons disables party support entirely and logs <code>ERROR :: Party plugin is set to &#x27;&lt;name&gt;&#x27;, but no such plugin was found!</code>. See <a href=\"#\" class=\"wl\" data-p=\"parties\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Parties\">Parties</a> for the values Mythic Dungeons supports natively.</p>\n<p>Because your plugin is not one of the natively-supported providers, Mythic Dungeons will never build a party on its own. A player who has not been passed to <code>initDungeonParty</code> is treated as playing solo. It also means the built-in party API methods (<code>createParty</code>, <code>inviteToParty</code>, <code>disbandParty</code>, and so on) are refused while your provider is selected; they only operate on the built-in system.</p>\n<h3>Step 1 - Implement the IDungeonParty Interface</h3>\n<p>Mythic Dungeons provides an interface that comes with all the necessary methods to make a party system compatible. All you have to do is implement the IDungeonParty interface into your existing party object and ensure it includes the necessary methods:</p>\n<ul><li><code>void addPlayer(Player)</code> - Allows Mythic Dungeons to add players to the party.</li><li><code>void removePlayer(Player)</code> - Allows Mythic Dungeons to remove players from the party.</li><li><code>List&lt;Player&gt; getPlayers()</code> - Allows Mythic Dungeons to retrieve a list of players currently in the party.</li><li><code>@NotNull OfflinePlayer getLeader()</code> - Allows Mythic Dungeons to retrieve the current owner of the party. <b>This must never return null</b>, even when the leader is offline. Mythic Dungeons calls it during the ready-check countdown and when resolving a party&#x27;s checkpoint, and dereferences the result. If your leader may be offline, fall back to any remaining member.</li></ul>\n<p>The interface also provides these methods with working defaults. Override them if your party system can answer them more cheaply or more correctly:</p>\n<ul><li><code>boolean hasPlayer(Player)</code> - defaults to a scan of <code>getPlayers()</code>. That scan compares the <code>UUID</code> objects by identity rather than by value, so a stale <code>Player</code> reference (one kept across a relog, for example) can make it answer <code>false</code>. Override it if your party can answer membership directly.</li><li><code>void partyMessage(String)</code> - defaults to messaging every player from <code>getPlayers()</code>.</li><li><code>Location getPartySavePoint(String dungeon)</code> - defaults to the leader&#x27;s checkpoint for that dungeon.</li><li><code>void setAwaitingDungeon(boolean)</code> - flags every member as waiting to enter a dungeon.</li><li><code>void initDungeonParty(Plugin)</code> and <code>void initDungeonParty(String...)</code> - see Step 2 below. These are the only two you are expected to call yourself rather than override.</li></ul>\n<p>_See the party class below for a full example._</p>\n<h3>Step 2 - Call the initDungeonParty Method</h3>\n<p>In order for Mythic Dungeons to track the party, it needs to be informed that it exists upon creation. This can be achieved by simply calling <code>initDungeonParty(Plugin)</code>. The plugin is used to verify that the server owner has configured their Mythic Dungeons to use your party system. (You can also use <code>initDungeonParty(String...)</code> if you want to provide multiple possible names for your party system.)</p>\n<p>Both overloads compare the configured <code>General.PartyPlugin</code> value against your plugin&#x27;s name (or against each name you pass) without regard to case, and do nothing when none of them match. When one does match, every player currently returned by <code>getPlayers()</code> is pointed at this party object, which is why the call belongs at the end of your constructor, after the member list is populated.</p>\n<h3>Example Party Class</h3>\n<p><b>Example Party Class (click to expand)</b></p>\n<pre class=\"cb\" data-lang=\"java\"><code>class ExampleParty implements IDungeonParty {\n\n    private UUID leader;\n    private List&lt;UUID&gt; playerUUIDs = new ArrayList&lt;&gt;();\n\n    public ExampleParty(Player leader) {\n        this.leader = leader.getUniqueId();\n\n        // REQUIRED!! Init the party with your plugin instance here, or enter a list of names for the party system. \n        // It&#x27;s recommended to do this after you&#x27;ve finished everything else in your constructor.\n        initDungeonParty(plugin);\n    }\n\n    // Allows Mythic Dungeons to easily add players to your party object.\n    @Override\n    public void addPlayer(Player player) {\n        playerUUIDs.add(player.getUniqueId());\n    }\n\n    // Allows Mythic Dungeons to easily remove players from your party object.\n    @Override\n    public void removePlayer(Player player) {\n        playerUUIDs.remove(player.getUniqueId());\n    }\n\n    // Allows Mythic Dungeons to easily retrieve a list of players in the party.\n    // NOTE: It&#x27;s advised to store players using UUIDs and then convert them to Players in the getPlayers method.\n    @Override\n    public List&lt;Player&gt; getPlayers() {\n        List&lt;Player&gt; players = new ArrayList&lt;&gt;();\n\n        for (UUID uuid : playerUUIDs) {\n\n            Player player = Bukkit.getPlayer(uuid);\n            if (player == null) continue;\n\n            players.add(player);\n\n        }\n\n        return players;\n    }\n\n    // Allows Mythic Dungeons to easily retrieve the leader or owner of the party object.\n    // Must never return null. Bukkit declares getOfflinePlayer(UUID) as @NotNull, so this is\n    // safe even when the leader has logged out.\n    @Override\n    public @NotNull OfflinePlayer getLeader() {\n        return Bukkit.getOfflinePlayer(leader);\n    }\n\n}</code></pre>\n<hr>\n<p>And that&#x27;s it! This system is still relatively experimental, but should allow easy integration with Mythic Dungeons using your own party system.</p>\n<p>_DISCLAIMER: The <code>/recruit</code> party-finder works with your provider only for players who already have a party registered through <code>initDungeonParty</code>. For those players Mythic Dungeons adds a joining player by calling your <code>addPlayer(Player)</code>, so make sure that method really joins the player to the underlying party. A player with no party who runs <code>/recruit</code> is not currently handled for custom providers; create the party through your own plugin first._</p>"},"dungeons:betonquest":{"h":"<p>BetonQuest Compatibility ========</p>\n<p>Mythic Dungeons features very basic BetonQuest compatibility by adding one action and one objective. In the future, if there is demand for it, functions that can call BetonQuest actions may be considered.</p>\n<p><b>BetonQuest 3.0 or higher is required.</b> On an older BetonQuest, Mythic Dungeons logs <code>WARNING :: Incompatible BetonQuest version! You must use version 3.0 or higher!</code> and registers neither element.</p>\n<p>In both elements below, <code>[dungeon name]</code> is the dungeon&#x27;s <b>folder name</b> inside <code>plugins/MythicDungeons/maps/</code>, not its <code>DisplayName</code>. The comparison is case-sensitive. Each element takes that one argument and nothing else.</p>\n<p>Mythic Dungeons Objectives</p>\n<hr>\n<p><b>Finish Dungeon</b> <code>finishdungeon [dungeon name]</code> Completed when the player finishes the specified dungeon. It is triggered by the <b>Finish Dungeon</b> function and by the <a href=\"#\" class=\"wl\" data-p=\"mythic-mobs-integration/mechanics/dungeoncomplete\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Mythic-Mobs-Integration/Mechanics/DungeonComplete\"><code>dungeoncomplete</code> Mythic Mobs mechanic</a>, both of which fire once per player, so every member of a party completes their own objective. Any <code>conditions:</code> you put on the objective still apply.</p>\n<p>Mythic Dungeons Actions</p>\n<hr>\n<p><b>Play Dungeon</b> <code>playdungeon [dungeon name]</code> Sends the player (and their party if they have one) to the specified dungeon. In BetonQuest 3.x this goes in the <code>actions:</code> section of your quest package, not in <code>events:</code>.</p>\n<p>Every check Mythic Dungeons normally runs before letting someone in still runs here: party size limits, access keys, the entry cost, permission requirements, access cooldowns, and any dungeons that must be completed first. When one of them refuses, the player gets the usual chat message and is not sent anywhere, and BetonQuest still treats the action as having succeeded. Only an unknown dungeon name raises a BetonQuest error.</p>\n<p>If every check passes but no instance is free, the player is put in the queue and told the instances are full, and they enter when one frees up.</p>\n<p>There is no difficulty argument. The action always asks for the difficulty named <code>DEFAULT</code>, so unless the dungeon actually has a difficulty level by that name, it runs on its base configuration, and the difficulty selection menu is never shown. The action also does not check the <code>dungeons.play</code> permission.</p>"},"dungeons:september-8th,-2025-changes-(courtesy-of-fate-sealed)":{"h":"<p>The following dev build comes courtesy of volunteer contributor to Mythic Dungeons @bedwarshurts Big shout out to him and his hard work in this build!</p>\n<h3>Dungeon Variables</h3>\n<p>If mythic is installed, dungeon (instance) variables are stored in Mythic&#x27;s Variable Registry instead of the <code>net.playavalon.mythicdungeons.dungeons.variables.VariableHolder</code> class. Player variables still use Mythic Dungeons&#x27; own holder. Adds <code>- setdungeonvariable{key=;value=;type=;}</code> (alias <code>setdungvar</code>) to set a dungeon variable from Mythic Configs. <code>key</code> also accepts <code>k</code>/<code>variable</code>/<code>var</code>, <code>value</code> also accepts <code>val</code>/<code>v</code>, and <code>type</code> also accepts <code>t</code> and may be left out entirely (INT, DOUBLE or STRING is detected from the value). Adds <code>&lt;dungeon.var.key&gt;</code> to use a dungeon variable in mythic configs. This way dungeon variables can be used both in dungeon functions and mythic configs.</p>\n<h3>Spectator Mode</h3>\n<p>If PacketEvents <b>and</b> Mythic Mobs are installed and <code>Spectator.UseLegacy</code> is set to <code>false</code> the new reworked spectator mode will be used. The player is only placed server side on spectator gamemode while client side he is in survival mode, this is achieved by sending a packet using packetevents. In this &quot;fake&quot; survival mode the player can interact with the spectator hotbar items given to him to teleport to other players, change flight speed etc. Additionally a mythic trigger <code>~onSpectatorInteract</code> was added. It is registered as <code>SPECTATOR_INTERACT</code> with the aliases <code>SPECTATORINTERACT</code>, <code>DUNGEONSPECTATORINTERACT</code> and <code>SPECINTERACT</code>, so <code>~onSpecInteract</code> and <code>~onDungeonSpectatorInteract</code> work as well. This way users can do some wild stuff if spectators interact with Mythic entities.</p>\n<h3>Other Additions</h3>\n<ul><li>A <code>general.date-format: &quot;EEE, MMM d, hh:mm aaa z&quot;</code> key in <code>lang/en.yml</code> (the language file is <code>lang/&lt;code&gt;.yml</code>, not <code>lang.yaml</code>)   _Known issue: no code currently reads this key. Every date is still formatted with the hardcoded <code>EEE, MMM d, hh:mm aaa z</code> pattern, so changing it has no effect._</li></ul>\n<p>Also adds in the dungeon config the following options:</p>\n<ul><li><code>General.InstantRespawnPlayers</code> which respawns the players instantly</li><li><code>Rules.AllowSpawnEggs</code> which allows (or not) the usage of mob spawn eggs inside the dungeon</li></ul>\n<p>Also adds</p>\n<ul><li><code>IsInDungeonCondition</code> A mythic condition which checks if a player is in a dungeon closes #488. Use it as <code>isindungeon</code>, <code>indungeon</code> or <code>isd</code>; <code>dungeonis</code> / <code>isindungeonnamed</code> checks for a specific dungeon</li><li><code>%md_dungeon_time_left_seconds%</code> and <code>%md_dungeon_time_elapsed_seconds%</code> papi placeholders (every Mythic Dungeons placeholder uses the <code>md_</code> prefix)</li><li>Partial support for the Party and friends plugin (unfortunately the spigot api version doesn&#x27;t provide methods to add / remove a player from the party). Set <code>General.PartyPlugin</code> to <code>paf</code>; the plugin itself must be <code>PartyAndFriendsGUI</code></li><li><code>%md_alive_member_N%</code> papi placeholder, where <code>N</code> starts at 1   _Known issue: it is not bounds checked. Asking for a member index that doesn&#x27;t exist, or using the placeholder outside a dungeon, throws instead of returning an empty value._</li></ul>\n<h3>Bug Fixes</h3>\n<ul><li>Fixes the radius argument not working in the Mythic <code>- dsignal</code> mechanic</li><li>Fixes #371</li><li>Fixes empty space in front of all messages if the prefix field is empty</li><li>Fix dungeon not ending if CloseDungeonWhenAllSpectating is true and last living player leaves with /leave command (and at least one spectating player remains)</li><li>Fixes #491 inventory not being properly restored on player leave or server restart in dungeons with seperate inventories</li><li>Fixes #523 FunctionGiveItem not working with items that didnt have a custom name</li><li>Fixes #471 Smuggling items into dungeons with seperate inventories</li><li>Fixes #525 Gamerules not applying on procedural dungeons</li></ul>"},"dungeons:dxl-import":{"h":"<p>Since the two plugins follow a similar format, Mythic Dungeons includes a command that allows you to import maps from the DungeonsXL plugin, including signs and functionality. _However_, this is an experimental feature and is not a perfect one-to-one conversion.</p>\n<h4>Importing a Dungeon</h4>\n<p>DXL dungeons are imported from the <code>plugins/DungeonsXL/maps</code> folder, so even if DXL is not currently installed on your server, as long as the folders are present, you can still import your dungeons. The map folder must still contain DungeonsXL&#x27;s <code>DXLData.data</code> file. Without it the import aborts, because that file is what records where the signs are.</p>\n<p>Use <code>/md dxlimport &lt;dxl map&gt;</code> to import a DXL dungeon (the name is the folder name under <code>plugins/DungeonsXL/maps</code>, and the command needs <code>dungeons.admin</code>). The import always produces a <code>classic</code> dungeon with the same name. Watch the console for any issues the importer encountered that you may need to fix yourself.</p>\n<p>Once the import finishes, open the dungeon with <code>/md edit &lt;dungeon name&gt;</code> and run <code>/md cleansigns</code> to strip the leftover DungeonsXL signs. The plugin suggests this itself after a successful import.</p>\n<p>_Known issue: on Minecraft 26.1 and newer the importer stages the DXL map in the server&#x27;s old world container folder, while the server now loads worlds from a per-dimension folder inside the main world. The result is that a freshly generated world is scanned instead of your map, so no signs are converted even though the import still reports success. This was found by reading the source rather than on a live 26.1 server, so check the console output and the imported dungeon before assuming your signs came across._</p>\n<h5>What gets converted</h5>\n<p>If the map folder contains a DXL <code>config.yml</code>, these settings are converted for you:</p>\n<div class=\"tw\"><table><thead><tr><th>DungeonsXL setting</th><th>Becomes</th></tr></thead><tbody><tr><td><code>worldEnvironment</code></td><td><code>General.Dimension</code></td></tr><tr><td><code>title.title</code></td><td><code>General.DisplayName</code></td></tr><tr><td><code>gameMode</code></td><td><code>General.Gamemode</code></td></tr><tr><td><code>initialLives</code></td><td><code>General.PlayerLives</code></td></tr><tr><td><code>keepInventoryOnEnter</code></td><td><code>General.KeepInventoryOnEnter</code></td></tr><tr><td><code>timeUntilKickOfflinePlayer</code></td><td><code>General.KickOfflinePlayers</code> and <code>General.KickOfflinePlayersDelay</code></td></tr><tr><td><code>isLobbyDisabled</code></td><td><code>General.Lobby.Enabled</code> (inverted)</td></tr><tr><td><code>requirements.permission</code></td><td><code>Requirements.Permissions</code></td></tr><tr><td><code>requirements.groupSize.minimum</code> / <code>.maximum</code></td><td><code>Requirements.MinPartySize</code> / <code>Requirements.MaxPartySize</code></td></tr><tr><td><code>requirements.finishedDungeons</code></td><td><code>Requirements.DungeonsComplete</code></td></tr><tr><td><code>breakBlocks</code> / <code>placeBlocks</code></td><td><code>Rules.AllowBreakBlocks</code> / <code>Rules.AllowPlaceBlocks</code></td></tr></tbody></table></div>\n<p>These sign types are turned into Mythic Dungeons functions:</p>\n<div class=\"tw\"><table><thead><tr><th>DXL sign</th><th>Becomes</th></tr></thead><tbody><tr><td><b>[msg]</b>, <b>[actionbar]</b></td><td>Message function</td></tr><tr><td><b>[title]</b></td><td>Title function</td></tr><tr><td><b>[start]</b></td><td>The map&#x27;s world spawn</td></tr><tr><td><b>[lobby]</b></td><td>The dungeon&#x27;s lobby spawn</td></tr><tr><td><b>[ready]</b></td><td>Start Dungeon function on a Right-Click trigger</td></tr><tr><td><b>[end]</b></td><td>Finish Dungeon function on a Right-Click trigger</td></tr><tr><td><b>[leave]</b></td><td>Leave Dungeon function on a Right-Click trigger</td></tr><tr><td><b>[lives]</b></td><td>Lives function</td></tr><tr><td><b>[trigger]</b></td><td>Signal Sender function</td></tr><tr><td><b>[interact]</b></td><td>Signal Sender function on a Right-Click trigger</td></tr><tr><td><b>[mob]</b>, <b>[externalmob]</b></td><td>Mob Spawner function</td></tr><tr><td><b>[door]</b></td><td>Door Control function</td></tr><tr><td><b>[redstone]</b></td><td>Redstone Block function</td></tr><tr><td><b>[soundmsg]</b></td><td>Sound function</td></tr><tr><td><b>[teleport]</b></td><td>Teleporter function</td></tr><tr><td><b>[place]</b></td><td>Block Control function on a Dungeon Start trigger</td></tr><tr><td><b>[block]</b></td><td>Block Editor function</td></tr><tr><td><b>[drop]</b></td><td>Item Dispenser function</td></tr><tr><td><b>[checkpoint]</b></td><td>Checkpoint function</td></tr></tbody></table></div>\n<p>The trigger code on the sign&#x27;s fourth line is converted too:</p>\n<div class=\"tw\"><table><thead><tr><th>DXL code</th><th>Becomes</th></tr></thead><tbody><tr><td><code>D&lt;n&gt;</code></td><td>Player Detector trigger</td></tr><tr><td><code>P&lt;n&gt;</code></td><td>Player Detector trigger, running once for each player</td></tr><tr><td><code>R</code></td><td>Redstone Receiver trigger</td></tr><tr><td><code>T&lt;name&gt;</code></td><td>Signal Receiver trigger</td></tr><tr><td><code>I&lt;name&gt;</code></td><td>Signal Receiver trigger, with the name prefixed by <code>I</code></td></tr><tr><td><code>M&lt;mob&gt;</code></td><td>Mob Death trigger (the Mythic Mobs version if Mythic Mobs is installed)</td></tr><tr><td><code>U&lt;material&gt;</code></td><td>Key Item trigger (falls back to <code>STICK</code> if the material isn&#x27;t recognised)</td></tr></tbody></table></div>\n<p>Several codes separated by commas become an AND gate, and anything the importer doesn&#x27;t recognise falls back to a Dungeon Start trigger.</p>\n<h5>Incompatibilities</h5>\n<h6>Signs</h6>\n<p>Some specific kinds of signs will not be imported. <code>[dungeonchest]</code>, <code>[chest]</code>, <code>[rewardchest]</code> and <code>[command]</code> signs are reported to the console with their coordinates. Every other unsupported sign is only logged as &quot;Unrecognized sign type&quot; with no location, so search your map for them yourself. The following signs will not be converted:</p>\n<ul><li><b>[reward]/[chest]/[rewardchest]/[dungeonchest]</b> - Mythic Dungeons uses a fundamentally different system for handling rewards. As a result, you&#x27;ll need to re-configure any rewards systems you had set up in your DXL dungeon.</li><li><b>[command]</b> - Mythic Dungeons currently does not import any DungeonsXL scripts. As a result, you&#x27;ll need to set up command functions manually.</li><li><b>[classes]</b> - Mythic Dungeons doesn&#x27;t have an equivalent to DXL&#x27;s class loadouts yet, however player loadouts are planned for a future version.</li><li><b>[BossShop]</b> - Mythic Dungeons doesn&#x27;t have support for BossShop at this time, though if you want it, let us know in the issue tracker and we&#x27;ll consider implementing it in a future version.</li><li><b>[resourcepack]</b> - Mythic Dungeons doesn&#x27;t yet have support for sending players a resource pack, though it is planned and may be here sooner than you think!</li><li><b>[hologram]</b> - Mythic Dungeons has its own Hologram function, but the importer does not convert DXL hologram signs. You&#x27;ll need to place them again with the function builder.</li><li><b>[script]</b> - DungeonsXL uses its own scripting system, and Mythic Dungeons has no equivalent for it. You will need to replicate your script using functions or Mythic Mobs if you have it installed.</li></ul>\n<h6>Multi-Floor Dungeons</h6>\n<p>The DXL importer currently does not support DXL&#x27;s multi-floor dungeons, nor does Mythic Dungeons have an equivalent feature at this time. However, this feature is planned for a future update, so stayed tuned and keep an eye on the development builds!</p>\n<h6>Multi-Team Dungeons</h6>\n<p>Mythic Dungeons currently does not support dungeons that involve multiple parties or teams joining the same instance and/or competing against each other. _This feature isn&#x27;t currently planned, however if there is enough demand it will be considered, so make sure to let us know!_</p>"},"dungeons:mythic-dungeons-2.0":{"h":"<div class=\"tw\"><table><thead><tr><th><b>HISTORICAL PAGE</b></th></tr></thead><tbody><tr><td><i>Kept for reference as of 2026-08-11. This is the original 2.0 announcement. The features it describes have shipped, and it is no longer an accurate description of how Mythic Dungeons currently works. For current documentation see <a href=\"#\" class=\"wl\" data-p=\"dungeon-types\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Dungeon-Types\">Dungeon Types</a>, <a href=\"#\" class=\"wl\" data-p=\"procedural-dungeons\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Procedural-Dungeons\">Procedural Dungeons</a> and <a href=\"#\" class=\"wl\" data-p=\"how-it-works\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/How-it-Works\">How it all Works</a>.</i></td></tr></tbody></table></div>\n<hr>\n<p>Mythic Dungeons 2.0 is a major overhaul of the Mythic Dungeons plugin. It is primarily dedicated to introducing the dungeon types system, the procedural dungeon generator, and improving the API and code quality to make the creation of add-ons easier.</p>\n<p>With the arrival of 2.0, the Mythic Dungeons plugin will be going up in price from <b>$20 to $30</b></p>\n<h3>Dungeon Types</h3>\n<p>2.0.0 introduces &quot;Dungeon Types&quot;, which can change the foundational behaviour of your dungeon. While this is mostly an API feature, Mythic Dungeons provides some new dungeon types of its own.</p>\n<p><b>Continuous</b> A dungeon type that, when players attempt to play the dungeon, will send them into the first available ongoing dungeon instance. For example, if Player A starts player the dungeon, and after a while Player B tries to play the dungeon as well, Player B will be sent to Player A&#x27;s dungeon.</p>\n<p><i>NOTE: This is NOT a replacement for world managers like Multiverse and MyWorlds!</i> The dungeon will be unloaded once all players have left it. This is useful for hosting scripted multiplayer events, or specialized dungeon types that are compatible with multiple parties.</p>\n<p><i>An formal extension of this dungeon type for multi-party dungeons such as minigames is currently planned.</i></p>\n<p><b>Procedural/Generated</b> A dungeon type that allows randomly generating a dungeon from a configurable collection of rooms. Learn more on the <a href=\"#\" class=\"wl\" data-p=\"procedural-dungeons\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Procedural-Dungeons\">dungeon generator page.</a></p>\n<h3>Misc Features</h3>\n<ul><li><b>Dungeons now support custom chunk generators, including those provided by other plugins.</b> Set them with <code>General.ChunkGenerator</code> in the dungeon&#x27;s config.</li><li>Mythic Dungeons now includes a void chunk generator, <code>mythicdungeons:void</code>.</li><li>Mythic Dungeons now includes a single-block chunk generator for generating chunks full of a specific block, <code>mythicdungeons:block.&lt;MATERIAL&gt;</code> (for example <code>mythicdungeons:block.STONE</code>). The material name must be spelled in upper case; a lower-case one is silently replaced with <code>STONE</code>.</li></ul>\n<h3>API Changes Advisory</h3>\n<p>There are several major changes to how Mythic Dungeons are loaded and instantiated, as well as a lot of refactored classes - especially utilities. Further details will be shared here at a later date.</p>\n<p><b>Dungeon &amp; Instance Objects</b> The most breaking change is that dungeon and instance objects have been restructured. Most methods now return a <code>AbstractDungeon</code> and <code>AbstractInstance</code> object now as part of our type system. Furthermore, <code>Dungeon</code> has been renamed to <code>DungeonClassic</code>, and <code>Instance</code> has been renamed to <code>InstanceClassic</code>.</p>\n<p>The new abstract objects have utility methods in them for casting to a specific dungeon type. These methods are safe to use, but will return <code>null</code> if the dungeon or instance type doesn&#x27;t match the requested type. (For example, if you call <code>DungeonProcedural#asClassic()</code>, it will return null, because <code>DungeonProcedural</code> is not a classic dungeon.)</p>\n<p><b>Utility Classes</b> A lot of Mythic Dungeons utility classes and methods got moved around and renamed. This page will do its best to provide a comprehensive list of classes that were renamed and methods that have been moved. The goal with this is to make the utilities much more organized, and there&#x27;s no better time for that than with version 2.0.</p>\n<p><b>Utility Changes</b></p>\n<p><b>Util.class</b></p>\n<ul><li>Changes will go here.</li></ul>\n<h3>FAQ</h3>\n<p><b>Will I need to buy Mythic Dungeons again?</b> <b>No.</b> If you have already purchased Mythic Dungeons before this update, you will not need to pay for it again.</p>"},"dungeons:mythic-dungeons-2.0-testing":{"h":"<div class=\"tw\"><table><thead><tr><th><b>HISTORICAL PAGE</b></th></tr></thead><tbody><tr><td><i>Kept for reference as of 2026-08-11. This was the private test-build announcement for Mythic Dungeons 2.0 and no longer describes the current state. 2.0 has shipped, and the checklist below is a snapshot of the test build at the time. For current documentation see <a href=\"#\" class=\"wl\" data-p=\"dungeon-types\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Dungeon-Types\">Dungeon Types</a>, <a href=\"#\" class=\"wl\" data-p=\"procedural-dungeons\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Procedural-Dungeons\">Procedural Dungeons</a> and <a href=\"#\" class=\"wl\" data-p=\"how-it-works\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/How-it-Works\">How it all Works</a>.</i></td></tr></tbody></table></div>\n<h3>Reference Links</h3>\n<ul><li><a href=\"#\" class=\"wl\" data-p=\"mythic-dungeons-2.0\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Mythic-Dungeons-2.0\">Overview of Mythic Dungeons 2.0.0</a></li><li><a href=\"#\" class=\"wl\" data-p=\"procedural-dungeons\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Procedural-Dungeons\">Dungeon Generator Info</a></li><li><a href=\"#\" class=\"wl\" data-p=\"generation-config\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Generation-Config\">Default <code>generation.yml</code></a></li><li><a href=\"#\" class=\"wl\" data-p=\"dungeon-rooms\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Dungeon-Rooms\">Dungeon Room Settings</a></li><li><a href=\"#\" class=\"wl\" data-p=\"common-generation-problems\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Common-Generation-Problems\">Common Generation Problems</a></li><li><a href=\"#\" class=\"wl\" data-p=\"generator-tips\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Generator-Tips\">Generation Tips &amp; Tricks</a></li></ul>\n<h3>Private Test Build Checklist</h3>\n<ul><li><b>Plugin Refactor</b><ul><li>Overhaul dungeon objects into abstract and dungeon types</li><li>Overhaul instance objects into abstract and instance types</li><li>Refactor and repackage various classes for better organization</li></ul></li><li><b>Dungeon Generator</b><ul><li>Core generator functionality and config file</li><li>Custom structure piece handling</li><li>Minecrafty dungeon layout mode</li><li>Branching dungeon layout mode</li><li>Room count/limit rule</li><li>Room depth rule</li><li>Room editor interface</li><li>Connector editor interface</li></ul></li><li>Continuous dungeon type allowing multiple parties to enter the same dungeon map at any time</li><li>New YAML serializer for cleaner YAML data saving and loading</li><li>Custom chunk generator option for all dungeon types</li><li>Native void chunk generator</li></ul>"},"dungeons:memory-leaks":{"h":"<p>Mythic Dungeons loads and unloads worlds on a regular basis in order to create private experiences for every party. This is a natively supported function of Bukkit (and consequently Spigot and its forks, such as Paper and Purpur.)</p>\n<p>However, this is a system that is quite infrequently used. As such, there are a lot of plugins out there that don&#x27;t properly work with it safely. This can cause major memory leaks, where Mythic Dungeons attempts to unload the world, but another plugin on the server keeps that world in memory.</p>\n<h3>Troubleshooting Memory Leaks</h3>\n<p>Most memory leaks in recent versions of Mythic Dungeons are caused by other plugins. We have done everything we can to mitigate them, but some plugins still keep the dungeon worlds alive. It can be difficult to diagnose, but here&#x27;s some guidance on how to investigate the cause.</p>\n<h4>Read the console first</h4>\n<p>Before you take a heap dump, check the console. Mythic Dungeons reports a failed unload itself, and the message tells you which situation you are in:</p>\n<pre class=\"cb\" data-lang=\"yaml\"><code>ERROR :: Failed to unload dungeon world &lt;name&gt; - it is STILL LOADED. Leaving its files alone; this world and its name stay in use until the next restart.\n-- Nobody is in the world, so another plugin cancelled WorldUnloadEvent.</code></pre>\n<p>That second line is the one you want. It means the world was empty and some other plugin cancelled <code>WorldUnloadEvent</code>, which is a plugin conflict, not a Mythic Dungeons bug. If instead the console lists players:</p>\n<pre class=\"cb\" data-lang=\"yaml\"><code>-- Still inside the world: &lt;playername&gt;</code></pre>\n<p>then somebody (or something holding a player reference) is still in the world, and the unload was correctly refused.</p>\n<p>There is a third variant, printed when the unload throws:</p>\n<pre class=\"cb\" data-lang=\"yaml\"><code>ERROR :: Error encountered while unloading dungeon world &lt;name&gt;\n-- The dungeon world is STILL LOADED! Please share this error with the developers!</code></pre>\n<p>That one comes with a stack trace. Share it.</p>\n<p>Note that when an unload fails, Mythic Dungeons deliberately leaves the world&#x27;s files on disk and keeps the instance name reserved until the next restart, rather than deleting files out from under a world the server still has open.</p>\n<h4>A note on timing</h4>\n<p>An empty instance is not disposed instantly, and that is by design:</p>\n<ul><li>The <code>General.CleanupDelay</code> option in the dungeon&#x27;s config delays disposal by that many ticks after the last player leaves. A non-zero value there means an idle world is expected to hang around for a while.</li><li>A safety-net sweep runs every 5 seconds and disposes any instance that is loaded, has had at least one player, and is now empty. Instances that have never had a player are skipped, so a warm preloaded instance is not churned.</li><li>Disposal also refuses to run at all while any player is still in the instance.</li></ul>\n<p>So a dungeon world that lingers for a few seconds is normal. One that never goes away is worth investigating.</p>\n<h4>Another plugin can block disposal outright</h4>\n<p><code>DungeonDisposeEvent</code> is cancellable. If any plugin cancels it, Mythic Dungeons puts the instance back and stops: the world is never unloaded and the files are never cleaned up. If you are chasing a world that refuses to disappear and the console is silent, check whether something on the server listens to that event. See <a href=\"#\" class=\"wl\" data-p=\"events\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Events\">Events</a>.</p>\n<h4>&quot;Memory leak protection is limited&quot;</h4>\n<p>If you see this on startup:</p>\n<pre class=\"cb\" data-lang=\"yaml\"><code>WARNING: Memory leak protection is limited on this Minecraft version!\n-- You should be fine, but if there are major memory leaks, submit an issue on the issue tracker and include the Minecraft version of your server!</code></pre>\n<p>it means Mythic Dungeons could not locate the internal fields it uses to clear a player&#x27;s combat tracker on this server version. That tracker records the damage sources that hurt the player, which hold entity references, which in turn hold a reference to their world (see the entity section further down). The message is exactly what it says: usually harmless, but worth mentioning on the issue tracker along with your Minecraft version if you are also seeing dungeon worlds stay in memory.</p>\n<h4>Taking a heap dump</h4>\n<p>_You will need to use Paper or a fork of Paper!_</p>\n<p>If your server has been online for a while and has accumulated a lot of RAM usage, run the command <code>/paper heap</code> and it will create what we call a heap dump in your server&#x27;s &#x27;dumps&#x27; folder. Heap dumps contain tons of valuable information for developers about how different plugins and systems are communicating with each other. They are often several GB large though, so make sure you&#x27;ve got plenty of disk space!</p>\n<p>From here you have two options:</p>\n<ol><li>Inspect the heap dump yourself using <a href=\"https://eclipse.dev/mat/\" target=\"_blank\" rel=\"noopener\">Eclipse Memory Analyzer</a></li><li>Upload the heap dump to a file hosting site and share it on <a href=\"https://git.lumine.io/mythiccraft/MythicDungeons/-/issues\" target=\"_blank\" rel=\"noopener\">the issue tracker.</a></li></ol>\n<h4>Inspecting Heap Dumps</h4>\n<p>Inspecting heap dumps is a VERY valuable skill to have, and is the recommended way to troubleshoot memory leaks caused by dungeon worlds! You&#x27;ll need Eclipse Memory Analyzer (linked above). It&#x27;s very easy to install and there are detailed instructions below on how to search for memory leaks.</p>\n<p>_NOTE: You may need a large amount of memory on your computer to inspect particularly big heap dumps!_</p>\n<p>First, you need to load up your heap dump. Open Eclipse Memory Analyzer, then from the top drop-down choose <code>file</code> and then <code>open heap dump</code>. Navigate to where you&#x27;ve saved it on your computer and open it.</p>\n<p><a href=\"https://i.imgur.com/AxRAme9.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></p>\n<p>It may take some time for your heap dump to finish loading, but once it does, just close the window the pops up. From here, you&#x27;ll want to click the <code>Open Dominator tree</code> button indicated here.</p>\n<p><a href=\"https://i.imgur.com/xlC4J4h.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></p>\n<p>Once that&#x27;s open, you&#x27;ll see a massive list like a spreadsheet. At the top will be a section labeled <code>&lt;Regex&gt;</code>. Click on it and type <code>net.minecraft.server.level.ServerLevel</code>, then press enter. You should see a list of worlds now.</p>\n<p>If that returns nothing, search for <code>net.minecraft.server.level.WorldServer</code> instead. Which of the two names your dump contains depends on how your server is mapped, not on a single version cutoff: <code>ServerLevel</code> is the Mojang name and <code>WorldServer</code> is the older Spigot-mapped one. Mythic Dungeons&#x27; own reflection handles this the same way, trying the Mojang name first and falling back to the Spigot name; the source notes that from MC 26.1 the server jar is fully unobfuscated and Paper dropped its plugin remapper, so from that version the Spigot-mapped names no longer resolve at all. If you are unsure, search <code>net.minecraft.server.level.</code> and look at what comes back.</p>\n<p><a href=\"https://i.imgur.com/GMeep7W.png\" target=\"_blank\" rel=\"noopener\">🖼 이미지</a></p>\n<p>Right-click one of them and choose <code>Path to GC Roots &gt; exclude weak references</code>. This will show you what is holding on to the world. The main thing you&#x27;re looking for in the various drop-downs is other plugins. If you&#x27;re having trouble identifying what the cause it, feel free to take a screenshot of the page and share it in the support channel on Discord.</p>\n<p>If you identify a plugin as the cause of the memory leak, it&#x27;s recommended to submit an issue to the developer of that plugin, as it&#x27;s very unlikely Mythic Dungeons can do anything about it.</p>\n<h4>Sharing with a developer</h4>\n<p>If you are having trouble inspecting the heap dump yourself, you _may_ share the heap dump on the issue tracker and a developer will attempt to inspect it when they have time.</p>\n<h3>[DEVELOPERS] How to not create memory leaks with your plugin</h3>\n<p>There are a few tricks to avoiding accidentally creating memory leaks due to the dynamic worlds that Bukkit/Spigot allows. Here&#x27;s a little overview of things to watch out for.</p>\n<h4>Catch the &quot;WorldUnloadEvent&quot;!!</h4>\n<p>If you&#x27;re tracking world data, stop tracking it when the world is unloaded! This is a VERY easy and simple way to make sure you don&#x27;t keep holding on to worlds that no longer exists, and can be done just with a standard event handler provided by the Spigot API.</p>\n<h4>Avoid hard-referencing the <code>World</code> object!</h4>\n<p>If you reference a world object anywhere, that will keep it in memory. For example, if you have an object with a World field such as: <code>private World world;</code> The world will remain in memory until you set <code>world</code> to null.</p>\n<p>A great way to combat this is to use something called a WeakReference. A weak reference will allow the world to still be unloaded from memory. Here&#x27;s how to use it:</p>\n<pre class=\"cb\" data-lang=\"java\"><code>public class MyClass {\n\t// Here&#x27;s our weak reference.\n\tprivate WeakReference&lt;World&gt; worldRef;\n\t\n\tpublic MyClass(World world) {\n\t\tworldRef = new WeakReference&lt;&gt;(world); // You create a WeakReference with `new` and pass the world as the constructor argument.\n\t}\n\t\n\tpublic World getWorld() {\n\t\treturn worldRef.get(); // The `.get()` call returns the world stored at the weak reference. If the world is unloaded, it&#x27;ll return null.\n\t}\n}</code></pre>\n<h4>Avoid hard-referencing entities!</h4>\n<p>Entities contain a hard reference to the World object! This will ALSO keep worlds alive in memory, so if you need to hold on to an entity, use a <code>WeakReference&lt;Entity&gt;</code>. (A WeakHashMap is also a good choice if you need a map, as it will allow the key to be unloaded.)</p>\n<h4>Watch out for Mythic Dungeons&#x27; own types</h4>\n<p>If you are writing an integration or a <a href=\"#\" class=\"wl\" data-p=\"custom-elements\" data-x=\"https://git.mythiccraft.io/mythiccraft/MythicDungeons/-/wikis/Custom-Elements\">custom element</a>, remember that some Mythic Dungeons objects are themselves a path to a world:</p>\n<ul><li><b><code>AbstractInstance</code> holds a hard <code>World</code> field.</b> Keeping an instance in a field of your own therefore keeps its world in memory for exactly as long as you keep the instance. Store the instance&#x27;s <code>UUID</code> (<code>getUuid()</code>) and look the instance back up when you need it, or clear your reference when the dungeon ends.</li><li><b>Every dungeon element holds its instance.</b> <code>DungeonElement</code> has an <code>instance</code> field, so holding a <code>DungeonFunction</code>, <code>DungeonTrigger</code> or <code>TriggerCondition</code> transitively holds a world too.</li><li><b>Anything you start in an element&#x27;s <code>onEnable()</code> must be cancelled in its <code>onDisable()</code>.</b> A repeating task whose body captures <code>this</code> keeps the element alive, which keeps the instance alive, which keeps the world alive. This is the single easiest way to leak a dungeon world from a custom element.</li></ul>\n<p>Mythic Dungeons uses the same techniques internally where it can: an instance&#x27;s tracked entities live in a set backed by a <code>WeakHashMap</code>, precisely so that holding the entity set does not pin the world.</p>"}});
